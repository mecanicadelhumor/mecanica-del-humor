# Rango, no versión exacta. El 22/03/2026 la 7.2.x cambió el valor por defecto
# de «boundary» a SentenceBoundary y eso dejó tres vídeos sin subtítulos: sin
# tope, un cambio así vuelve a entrar solo. Y sin suelo, una versión vieja no
# aceptaría el parámetro «boundary» que ahora pasa voz.py. Rango abierto dentro
# de la major porque edge-tts necesita actualizarse cuando Microsoft cambia el
# esquema de tokens: fijarla exacta congelaría el canal el día que eso pase.
edge-tts>=7,<8
playwright
pillow
matplotlib
requests
google-api-python-client
google-auth-oauthlib

# C7 (voz.py --motor gemini, versión 5.1/6 del plan). Solo se importa cuando
# se pide ese motor (import perezoso en voz.py), así que no afecta a las
# producciones que siguen en edge-tts. Falta además, y eso NO se arregla
# aquí, que producir.yml exponga el secreto GEMINI_API_KEY al paso de voz.py
# (fichero de workflows, protegido: lo añade el codirector antes del 14/09).
google-genai

# C50 (visual.py, caras() — detección de caras con Haar de OpenCV, 23/09/2026).
# «headless»: corre en GitHub Actions, sin entorno gráfico; la variante normal
# arrastra dependencias de Qt que no hacen falta aquí y pueden fallar en CI.
# Faltaba en este fichero el mismo 23/09: cv2 se importaba desde otra cosa del
# entorno, sin los bindings reales, y cv2.CascadeClassifier no existía. Cada
# candidato de cada plano de MDS-024 y MDS-025 murió con «no se pudo mirar» en
# visual.py y los dos salieron enteros en tarjeta de marca el primer día de
# C50 (revisión diaria, 23/09). caras() ya no deja que esto tire el plano
# entero, pero sin el paquete de verdad tampoco hay detección de caras.
opencv-python-headless
