# Revisión diaria — 19 de septiembre de 2026, 11:30 (sábado)

Sesión en la nube, sin puente `mcp__remote-devices__*` (no existe en este modo, y no ha
existido nunca). No se ha pedido ningún permiso ni confirmación en toda la sesión. Clono
`origin/main`, trabajo en el contenedor, entrego con `.tar.gz`.

**Comprobación de entregas pendientes (primera acción).** `git log --oneline -8` sobre
`origin/main`: el último commit es `d55be06` (registro y expediente de MDS-017, 05:58 UTC),
y entre ayer y hoy están además el precacheo de voz de hoy, el nuevo directorio
`08_comunicacion/`, el banco de imágenes con créditos, canciones nuevas y el duplicado del
commit de `validar_bibliografía`. **Hoy es sábado, no viernes**: la regla de «paquete de
planificación del jueves a medias» ya se comprobó ayer (`4b275a5`, aplicado) y no vuelve a
aplicar hasta el próximo jueves/viernes. **No hay entrega pendiente bloqueando
`05_calendario/`.**

También comprobado: los guiones `MDS-021` a `MDS-025` que la planificación del jueves 17
escribió fueron **reescritos enteros por la dirección el viernes 18** (commit `8262b7b`,
C38: duración contra la serie), y `01_bibliografia/data/semillas.json` recibió ese mismo
viernes una sincronización grande (C39, ver más abajo). Parto de esa base, no de lo que
recuerdo de ayer.

**Leído `08_comunicacion/novedades.md`** (carpeta nueva de ayer): banco de imágenes con
créditos ya aptas y catálogo de música ampliado, los dos con sus créditos. Informativo, no
me toca actuar sobre ello hoy — no es guion, no es código de los que reviso, y `02_marca/`
no es mío.

## Paso 1 — revisión editorial

Guiones españoles sin producir (`parrilla.json` contra `registro_publicaciones.json`):
`MDH-007` (20/09), `MDS-021` a `MDS-025` (21 al 25/09) y `MDH-008` (26/09). `MDS-017`
publica hoy y ya está producido — su revisión va en el paso 3.

- `python3 04_agentes/validar_guion.py` sobre los siete: **sin errores graves.** Solo
  avisos ya conocidos: C38 exime a `MDS-017` (concedido el 18/09, no aplica aquí);
  duración de escena > 10s en varias escenas de los dos episodios largos (esperado, es la
  pantalla fija del formato, no un defecto); C17 en `MDH-008` (A01, A05, L01) — mismo
  razonamiento que ayer, sigue sin ser hallazgo, ya escrito en `revisiones/MDH-008.md`.
- **Los cinco Shorts reescritos por la dirección (`MDS-021`-`025`), releídos enteros por
  ser una versión nueva.** Regla de los dos canales escena por escena: sin hallazgos.
  Concretamente, **la escena 2 de `MDS-025` ya no dice «grupo de control»**: la
  reescritura del viernes cambió el `pie` a «el grupo que vio *comedia*», que es
  exactamente la corrección que pedía mi nota de ayer (`revisiones/MDS-025.md`, que por
  eso ya no está en el repositorio — la dirección resolvió la escena reescribiéndola
  entera, no aplicando la nota literal, y el resultado es correcto: verificado contra la
  narración de esa misma escena). No hace falta nada más ahí.
- **`MDH-008`**: sin cambios desde ayer. La nota de `revisiones/MDH-008.md` (escena 11,
  personaje `rie` mientras la narración dice dos veces que nadie se ríe) sigue en pie y
  sigue sin aplicar — publica el 26/09, fuera de toda ventana de 48 horas, le toca a la
  planificación del jueves 24. Comprobado que el guion no ha cambiado esa escena.
- **`MDH-007`**: sin cambios desde el 18/09, ya revisado entero los días 15, 16 y 18. No
  lo repito completo hoy. Publica **mañana domingo a las 12:00** — la caché de voz sigue
  siendo lo único por vigilar (ver paso 4).
- Ningún guion tocado. Ninguna excepción de 48 horas necesaria hoy.

### La bibliografía: un agujero real en la sincronización C39, encontrado y cerrado hoy

Al abrir el DOI corregido ayer de `E02` (Provine, 1993) contra Wiley Online Library con
`WebSearch` —la comprobación que me toca cuando alguien deja una ficha en
«por verificar automaticamente»— confirmé que `10.1111/j.1439-0310.1993.tb00478.x` es el
DOI correcto (el título que devuelve Wiley es exactamente el de la ficha). Al ir a marcarlo
como verificado, comparé `BIBLIOGRAFIA_CURADA.md` contra lo que `generar_md.py` produciría
de verdad hoy — no me fié de que «pasa `validar_bibliografia.py`» quisiera decir «regenerar
es seguro» — y no lo era:

- **`validar_bibliografia.py` decía «los dos ficheros dicen lo mismo»** (comprueba título,
  autores, año y DOI) **y aun así regenerar borraba contenido real** de nueve fichas:
  `C05`, `E02`, `E06`, `F03`, `F04`, `F05`, `G03`, `G05`, `G06`. En seis de ellas
  (`C05`, `E02`, `E06`, `G05`, `G06`) el `.md` tenía, escrito a mano y verificado por la
  dirección o por mí en sesiones anteriores, un párrafo largo de «qué dice el artículo y
  qué no» (o volumen/página en la cita) que **no vivía en ningún campo de
  `semillas.json`**, así que la próxima regeneración lo habría borrado sin aviso — el
  mismo desastre que motivó C39 el 18/09, solo que en fichas distintas a las que la
  dirección ya había sincronizado ese día (`C05`, `F04`, `F05`, `G03`; `E02` se dejó
  deliberadamente sin sincronizar porque tenía dos DOI candidatos sin comprobar — ver su
  bitácora del 18/09). En las otras tres (`F03`, `F04`, `F05`) la única diferencia era que
  el generador **pisaba con «⚠️ por verificar» cualquier nota de `doi_confianza`, incluida
  una que decía justo lo contrario** («verificado a mano el 15/09/2026»): una ficha ya
  comprobada salía marcada como si no lo estuviera.

**Lo que he hecho, en orden, y verificado en cada paso:**

1. `01_bibliografia/scripts/generar_md.py`: `doi_confianza` ya no se trata como booleano.
   Si sigue en el valor por defecto (`SIN_VERIFICAR`, «por verificar automaticamente»), se
   avisa; si trae cualquier otro texto, es la nota de quien lo comprobó y se imprime tal
   cual. Sin DOI (caso `G03`), la nota va detrás de «Tipo». Nuevo campo opcional
   `nota_ampliada`: el párrafo largo, cuando exista, se imprime después de `por_que` — es
   el campo que faltaba para que esas seis fichas se pudieran regenerar sin perder nada.
2. `01_bibliografia/data/semillas.json`: llevado el contenido verificado del `.md` a los
   campos correspondientes de `C05`, `E02`, `E06`, `G03`, `G05` y `G06` (volumen/página en
   `fuente`, la nota real en `doi_confianza`, el párrafo largo en `nota_ampliada` donde
   corresponde). `E02` queda con nota fechada hoy y la comprobación contra Wiley por
   `WebSearch`. `F03`/`F04`/`F05` **no los toco**: no están verificados de verdad, así que
   ahora sí muestran correctamente «⚠️ por verificar» — antes lo escondía el propio error
   del generador.
3. Regenerado `01_bibliografia/BIBLIOGRAFIA_CURADA.md` y comparado línea a línea contra la
   versión de `origin/main`: **ningún dato se pierde**, solo reformatea (párrafos que
   estaban partidos a mano en varias líneas salen en una) y corrige las nueve fichas de
   arriba.
4. `04_agentes/validar_bibliografia.py`: añadida la comprobación de `fuente` (volumen y
   páginas), que es la que habría cazado esto sin salir a la red. Probado con un caso
   negativo (desincronicé `fuente` de `E06` a mano, el validador lo detectó y devolvió
   código de salida 1; luego deshecho el cambio con `git checkout`). Documentado en su
   propia cabecera lo que sigue sin poder comprobar: si alguien vuelve a escribir un
   `nota_ampliada` a mano solo en el `.md`, este validador no lo vería — no hay forma de
   comprobarlo carácter a carácter sin reimplementar el propio generador.
5. `python3 04_agentes/validar_bibliografia.py` final: **pasa.**
   `python3 04_agentes/validar_guion.py` sobre los siete guiones pendientes: sigue pasando
   (la bibliografía no afecta al validador de guiones, pero es la comprobación de cierre
   habitual del paso 1).

No lo cuento como el «un cambio de código» de la sesión (paso 4): son tres arreglos de
defecto real, encontrados verificando lo que ya tenía que verificar hoy — la regla dice que
esos nunca han consumido ranura. Lo dejo aparte, con este detalle, para que quien lo lea
pueda auditarlo sin tener que repetir el trabajo.

## Paso 2 — comprobar que la producción salió

`registro_publicaciones.json` con `_estado_leido_utc` del **18/09 13:10:55 UTC** — el sync
de hoy (`sincroniza_registro.yml`, crons a las 08:50 y 09:10 UTC) todavía no se refleja a la
hora de esta revisión (≈09:50 UTC). Comprobado en el código de `metricas.py`
(`sincronizar_registro()`): el fichero **solo se reescribe si hubo algo que corregir**, así
que esta fecha parada no prueba por sí sola que el workflow no haya corrido hoy — puede
simplemente no haber encontrado nada nuevo que YouTube no supiera ya. No lo trato como
fallo: es exactamente el mismo caso que ayer, y ayer se resolvió solo por la tarde.

- **`MDS-017`** (hoy): `private`, `publicar_en: 2026-09-19T17:00:00Z` (19:00 España),
  `video_id: mPwvmnp1ZNE`, `subido_utc: 2026-09-15T01:38:46Z`. Patrón automático normal.
  **No es incidencia** — ver el paso 4 sobre el porqué de esa fecha de subida, que a
  primera vista parecía mentir.
- **`MDS-020`** (18/09): sigue `private` con `publicar_en` ya pasado (18/09 17:00 UTC).
  Patrón correcto en modo automático; el registro no se entera hasta que se sincronice.
  No hay nada que investigar.
- **`MDS-019`** (17/09): **ya confirmado `public`** en el registro (se corrigió ayer por la
  tarde, después de mi revisión). Es el último vídeo con estado confirmado.
- **`MDS-017`, el caso `rehacer_video_id`**: la parrilla movió este guion al martes al
  sábado (nota en `parrilla.json`) porque la producción del 15/09 no consiguió voz de
  Gemini para las seis escenas a tiempo y la subida de esa madrugada (`9H2xEZnFeHA`, con
  dos voces mezcladas) se retiró. El registro de hoy trae un `video_id` **distinto**
  (`mPwvmnp1ZNE`): la re-producción de hoy sí subió un vídeo nuevo, correcto. **No es el
  caso «yo no lo sé» de MDS-011.**

## Paso 3 — revisión del vídeo (MDS-017, producido esta madrugada)

`05_calendario/qa/MDS-017.es/` — los seis fotogramas que coinciden con
`ficha.json.fotogramas_s` (hay fotogramas viejos de sesiones anteriores mezclados en la
misma carpeta, de la producción retirada; los ignoro y trabajo solo con los seis que la
ficha actual declara).

- **`ficha.json`:** `lufs -14.5` (OK), `pico_dbtp -1.26` (OK), `fragmento_antes_de_la_narracion:
  false`, `quemados: false` (correcto), `sincronia_voz.desfase_s: 0.25` (< 0.5, OK).
  `voz_mezclada: false`. `modelo_voz: gemini-3.1-flash-tts-preview` en las seis escenas.
  `origen_voz`: 4 de caché, 2 de llamada — coincide exactamente con lo que la nota de
  `parrilla.json` decía que iba a pasar («las escenas 1-4 ya están en la caché de voz»).
  `escenas_ritmo_fuera_de_rango`: escena 1 a 1,57 palabras/s, justo por debajo de 1,6 —
  aviso, no error, mismo tratamiento que a avisos de ritmo equivalentes en días anteriores.
- **Los seis fotogramas, los dos canales:** correlato exacto entre pantalla y narración en
  las seis escenas, comprobado además contra el guion. Escena 1: icono `i-muelle` + tres
  palabras («Cosquillas a ratas.»), cumple C19. Escena 3 (`dato`): cifra «50 kHz» y pie «lo
  que chilla una rata con cosquillas», los dos dichos en la narración de esa misma escena.
  Escena 4 (`comparacion`): «un chiste» / «hay que entenderlo» frente a «unas cosquillas» /
  «no hay nada que entender», correlato exacto con la narración. Expresiones: `rie` en la
  escena 2 (la rata pide más cosquillas — no niega la risa, la provoca), `piensa` en la
  escena de dato (permitido, no es cara triste), `entiende` en la síntesis de la escena 5,
  `no` en el cierre (correcto: ahí es donde la narración dice que falla). Resaltado: un
  ámbar por escena, ninguno en `narracion`, sin cian; la escena de cierre usa ámbar sobre
  «decirlo» (no hay marcado de coral disponible en `escena.html` — ver nota abajo). Sin
  texto cortado, sin elementos tapados, sin faltas.
- **Geometría personaje/texto** con `muestrear_geometria.py`: peor hueco del lote,
  181px (escena 3) — muy por encima del umbral de riesgo, con el aviso de siempre sobre
  las tipografías de reserva de este contenedor.
- **Sin incidencia.**

**Nota aparte, no es hallazgo de guion:** revisando el resaltado de la escena de cierre until
me fijé en que `00_estrategia/tareas/revision-diaria.md` describe tres colores de resaltado
(«ámbar = acento», «cian = término del oficio», «coral = lo que falla, reservado al
cierre»), pero `escena.html` (`rico()`/`ricoSVG()`) solo sabe interpretar `*así*` → ámbar y
`_así_` → cian. No existe ninguna sintaxis en el guion para producir coral: la clase CSS
`.cor` está definida pero nada la aplica nunca, y `guionista_corto.md` (reescrito el
12/09) tampoco menciona el coral en ningún sitio — solo ámbar y cian. No sé si es una
característica pendiente de construir o una que se abandonó al reescribir los prompts del
12/09 sin actualizar `revision-diaria.md`. No lo toco hoy (no es mío decidir qué prompt
manda, y añadir una tercera sintaxis de marcado es un cambio de código nuevo, no un arreglo
de un defecto de hoy) pero lo dejo escrito para que la dirección decida: o se implementa el
coral en `escena.html`, o se quita la mención de `revision-diaria.md` para que no describa
una regla que ningún guion puede cumplir.

## Paso 4 — código y encargos

**El «un cambio de código» de hoy: `03_produccion/pipeline/registrar.py`.**

Al revisar por qué `MDS-017` mostraba `subido_utc: 2026-09-15T01:38:46Z` con un `video_id`
que es de **hoy** (`mPwvmnp1ZNE`, distinto del `9H2xEZnFeHA` retirado), encontré la causa en
el propio código: `registrar.py` heredaba `subido_utc` y `revisado` de la entrada vieja
siempre que ya existiera una con ese `id`, sin comprobar si el `video_id` había cambiado.
Cuando `cola.py` rehace una producción (el mecanismo de `rehacer_video_id` que trajo el
19/09 a este mismo `MDS-017`), el registro se queda diciendo que el vídeo nuevo se subió —y
que ya se revisó— hace días, cuando el vídeo de verdad es otro. Es exactamente el tipo de
dato que el paso 2 usa para saber si ya pasaron los tres intentos del cron y que este mismo
paso 3 usa para no repasar un vídeo dos veces.

**Arreglo:** un `video_id` distinto del que ya había en el registro cuenta como subida
nueva; solo se hereda lo viejo cuando el `video_id` no cambia (el caso idempotente que el
propio script ya decía cubrir). Probado con un script de prueba fuera del repositorio
(simulando `plan.json` + `build/`): confirmado que un `video_id` nuevo resetea
`subido_utc` a la hora real y `revisado` a `false`, y que relanzar la misma producción con
el mismo `video_id` sigue sin tocar ninguno de los dos. No afecta a
`05_calendario/registro_publicaciones.json` en sí —ese fichero no lo toco, lo escribe
Actions— solo al script que lo escribe.

**Bibliografía:** ver el apartado dedicado en el paso 1 (tres ficheros de código, no
consume esta ranura por ser arreglo de defecto).

**Resto de la cola, repasada, sin cambios respecto a ayer:**

- **Encargo 8 (`ajustarTamano()` + banda del personaje).** Sigue bloqueado por lo mismo:
  este contenedor no tiene las tipografías de marca instaladas (Archivo Black, Inter,
  JetBrains Mono; comprobado otra vez con `fc-list`), así que los píxeles de
  `muestrear_geometria.py` no son definitivos para decidir un umbral nuevo. La dirección sí
  corrió la herramienta ayer con las tipografías reales sobre los cinco Shorts reescritos
  (215px de peor caso, según su propia bitácora) — sigo sin esos números para el resto del
  repositorio.
- **P1 (profundidad, opacidad de la retícula).** Sigue sin respuesta en
  `07_pruebas/P1-profundidad/P1-profundidad.md`. Nada nuevo que decidir sola.
- **C34 (banco de imágenes, duotono ámbar).** Movido al lunes 21 por la propia dirección
  (su bitácora de ayer): sigue bloqueado por los enlaces de origen de las fotos, pedidos el
  15/09. El banco de imágenes que llegó hoy (`02_marca/banco/`, ver comunicación) trae
  `creditos_pixabay.csv` pero no resuelve ese bloqueo por sí solo — no es mi fichero
  (`02_marca/` no está en la tabla de propiedad) y no lo toco.
- **C36 (una llamada por vídeo).** Sigue sin diseño escrito, movido también al lunes 21.
- **Encargos 1–7, 10, 11, 12 de la cola numerada:** sin cambios de estado desde ayer (ya
  entregados o bloqueados por lo mismo que ayer).

**Caché de voz de `MDH-007` — lo que hay que mirar hoy, tal como quedó anotado ayer.**
Publica mañana domingo 12:00. Sin commit de precacheo hoy con fecha de hoy en
`origin/main` a la hora de esta revisión (mismo aviso que con el registro: los crons de
Actions se retrasan). Si el conteo de escenas en caché sigue igual que ayer (18 de 41)
cuando se compruebe mañana antes de las 12:00, el margen es justo pero, según el propio
cálculo de la dirección en su bitácora del 18/09 (~20 en la pasada de las 01:13 UTC del
domingo y el resto en la de las 08:23), **el circuito llega**. No toco `parrilla.json` ni
el workflow.

## Y una nota sobre `07_pruebas/`

Revisadas las carpetas. Sin novedad desde ayer salvo lo ya anotado en P1. Nada dirigido a
mí sin contestar aparte de esa pregunta.

## Paso 6 — ESTADO.md

Ver `05_calendario/ESTADO.md`, reescrito entero.

---

**Ficheros tocados esta sesión:**
- `03_produccion/pipeline/registrar.py` (arreglo de defecto: `subido_utc`/`revisado` no se
  reseteaban al cambiar el `video_id` de una entrada ya existente)
- `01_bibliografia/scripts/generar_md.py` (arreglo de defecto: `doi_confianza` se trataba
  como booleano y borraba notas de verificación reales; nuevo campo `nota_ampliada`)
- `01_bibliografia/data/semillas.json` (sincronizadas `C05`, `E02`, `E06`, `G03`, `G05`,
  `G06` con el contenido verificado que solo vivía en el `.md`; `E02` verificado hoy contra
  Wiley con `WebSearch`)
- `01_bibliografia/BIBLIOGRAFIA_CURADA.md` (regenerado desde `semillas.json`, sin pérdida de
  contenido verificado)
- `04_agentes/validar_bibliografia.py` (añadida la comprobación de `fuente`, documentado el
  punto ciego que queda)
- `05_calendario/ESTADO.md` (reescrito)
- `05_calendario/bitacora/2026-09-19-revision.md` (este fichero, nuevo)

Ningún guion tocado. Ningún fichero de `05_calendario/parrilla.json`, `CALENDARIO.md`,
`demanda.json`, `registro_publicaciones.json`, `qa/`, guiones, `publicaciones/` ni
`.github/workflows/` tocado. `02_marca/` no tocado (no es mío).
