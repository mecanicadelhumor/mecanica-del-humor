# Revisión diaria — 14 de septiembre de 2026, 11:30 (lunes)

Sesión en la nube, sin acceso al ordenador del codirector (no existe puente
`mcp__remote-devices__*` en este modo, y no ha existido nunca). Clono
`origin/main`, trabajo en el contenedor, entrego con `.tar.gz`.

**Comprobación de entregas pendientes (primera acción).** `git log --oneline
-8` sobre `origin/main`: los ocho últimos son `5b3bcf9` (métricas de hoy),
`e922c0a` (registro y expediente MDS-016), `fe049ef` (mi propia sesión de
ayer, aplicada — `voz_precache` para C27-B), `81a3c9d` (métricas de hoy,
primera pasada), `3aa9a16` (registro y expediente MDS-016, primera pasada),
y los tres del sábado de la dirección. `3814c55` (planificación del jueves
10/09, aplicada el viernes 11) sigue siendo el último commit que toca
`05_calendario/guiones/`, sin nada posterior que lo pise. **No hay entrega
pendiente bloqueando nada.** Se procede con normalidad.

Nota sobre los commits automáticos: hoy aparecen DOS rondas de «Registro y
expediente de calidad: MDS-016» (01:33 y 06:54 UTC) y DOS de «métricas
2026-09-14» (05:32 y 08:48 UTC). Es coherente con lo documentado: el cron de
producción tiene varios pasos (subida, `qa.py` después de subir) y hoy es
lunes, el día en que corre `metricas.py`; no es una señal de nada roto —
`control_c26` en `metricas.json` se actualizó correctamente en la segunda
pasada (`mediana_vistas_48h: 10, n_videos: 15`).

## Paso 1 — revisión editorial

Guiones españoles sin producir (`parrilla.json` contra
`registro_publicaciones.json`, que ahora llega hasta `MDS-016.es`):
`MDS-017` a `MDS-020` (15–18/09) y `MDH-007` (19/09) — cinco, uno menos que
ayer porque `MDS-016` ya se produjo esta madrugada. Sin cambios en el
contenido desde `3814c55` (`git log` sobre `05_calendario/guiones/` lo
confirma). `python3 04_agentes/validar_guion.py` sobre los cinco: sin
errores ni avisos.

Releídos escena por escena, con los campos que se ven (`icono`, `texto`,
`personaje`, `fuente`) cruzados contra su propia `narracion`:

- **Los dos canales.** MDS-017 esc. 3 (`cifra: "50 kHz"` ↔ «cincuenta
  kilohercios» en la narración), MDS-018 esc. 0 (`«viernes»` en pantalla ↔
  «cada viernes» en la narración — el caso que vigila C28), MDS-019 esc. 2
  (`«Atención sí»` ↔ «la atención la gana casi siempre» de Eisend), MDS-020
  esc. 2 (`«20 cómicos»` ↔ «veinte cómicos profesionales» de Mirowski). Sin
  hallazgos.
- **La cara (regla 14.3).** `no` solo en los cierres, y solo donde la
  narración dice «falla» explícitamente (las cinco lo dicen: «aquí falla»,
  «dónde falla todo esto», etc.). `piensa` en las escenas `dato` (presentan
  una cifra, no van tristes) y `rie` en la escena 1 de remate del
  planteamiento inicial. Ninguna escena de presentación lleva cara triste.
  Sin hallazgos.
- **C19 (escena 1).** Las cuatro Shorts arrancan con icono
  (`i-muelle`, `i-publico` ×2, `i-bocadillos`) y 3-4 palabras; ninguna es
  tarjeta de texto.
- **Cifras con fuente.** Las cuatro escenas `dato` de los Shorts llevan
  `fuente` (E06, G06, G05, K02/K03). Sin veto que aplicar.
- **C17 (no repetirse).** Fuentes centrales sin cambios respecto a ayer
  (E06, G06, G05, K02/K03, H05/H06/J01/J02); cruzadas otra vez contra el
  resto del repositorio, ninguna se repite como fuente central. Sin
  conflicto.
- **Resaltado.** Un ámbar por escena en los campos vistos; cero apariciones
  de `*`/`_` dentro de `narracion` en los cinco guiones.
- **`MDH-007`.** Nota abierta de `revisiones/MDH-007.md` (regla 2, escenas
  19 y 31 con fuente floja/ausente) sigue vigente y sin cambios; publica el
  19/09, la planificación del 17 tiene margen. No añado hallazgos nuevos.

Sin hallazgos nuevos hoy; no se escribe ningún `revisiones/<ID>.md` nuevo.

## Paso 2 — ¿salió la producción?

`registro_publicaciones.json`: última entrada `MDS-016.es`, subida
`2026-09-14T01:32:59Z`, `estado: "private"`, `publicar_en:
"2026-09-14T17:00:00Z"` (19:00 hora de España) — exactamente el patrón
correcto en modo automático. Nada que investigar: ni ausencia (la entrada
del día está, y a las tres pasadas del cron ya se ha subido de sobra) ni
`private` sin `publicar_en`.

Comprobado también el `_estado_leido_utc` que `metricas.py` añadió hoy a
este mismo fichero (`2026-09-14T08:48:...Z` tras la segunda pasada de
métricas): no corrigió ningún estado (`corregidos == 0` — no aparece el
mensaje «registro corregido» en el commit, y el diff de `e922c0a`/`5b3bcf9`
no toca `estado` de ningún vídeo salvo el crecimiento normal de filas de
`metricas.json`), así que no hay ningún caso tipo MDS-011 hoy.

**MDS-011 — sigue cerrado, no se vuelve a abrir** (regla del 12/09, cerrado
por el codirector en `PROMPT_DIRECCIÓN.md` del 9/9). No aparece en
`ESTADO.md`.

## Paso 3 — revisión del vídeo del día

Vídeo del día: `MDS-016` («Por qué la ironía no se entiende por WhatsApp»),
publica hoy a las 19:00. Sigue en `private` con `publicar_en` correcto —
dentro de la ventana normal entre subida y publicación.

- **`ficha.json`:** `audio.lufs` −14.42 (dentro de ±1), `pico_dbtp` −1.31
  (≤ −1.0, OK), `arranque.fragmento_antes_de_la_narracion` en `false`,
  `subtitulos.quemados` en `false` (correcto), `lineas_ass` 106 (> 0, motor
  `edge` — el canario funciona). `sincronia_voz.desfase_s` 0.05 (< 0.5).
  `motor_voz_pedido: "edge"` — se produjo ANTES del cambio de motor de hoy
  (ver Paso 4), así que no es evidencia de que Gemini funcione o no; es la
  última producción con el motor antiguo.
- **Las seis capturas de `05_calendario/qa/MDS-016.es/`:** texto siempre
  dentro de márgenes, sin cortes ni palabras partidas, buen contraste,
  sin faltas de ortografía. Nada cae en las zonas que tapa la interfaz de
  YouTube. El personaje del cierre (escena 5) lleva la cara `no` — boca
  plana, coherente con «y aquí falla: solo sabemos cómo suena» — y las
  escenas de presentación (comparación, dato) llevan cara neutra o
  `piensa`, nunca triste.
- **Los dos canales sobre el vídeo ya producido:** cada fotograma tiene
  sentido por sí solo (texto autoexplicativo) y coincide con lo que dice la
  `narracion` de su escena — comprobado ya en el guion en el Paso 1 y
  confirmado visualmente en las capturas.

**Sin incidencia.** `MDS-016` está listo para publicarse a su hora sin que
yo tenga nada que avisar.

## Paso 4 — código y encargos

**El cambio de código de la sesión (regla 11.1): el motor de voz de los
Shorts pasa a Gemini por defecto, tal como estaba programado para hoy.**

`03_produccion/pipeline/voz.py`, línea del `argparse` de `--motor`:
`default="edge"` → `default="gemini"`. Es exactamente el cambio que el
propio comentario del código (escrito el 07-09/09) dejaba preparado para
«el lunes 14», con dos condiciones previas que he verificado antes de
tocar nada:

1. **`producir.yml` ya expone `GEMINI_API_KEY`** al paso «Sintetizar
   narración» (`grep` confirma la línea 197, dentro del `env:` de ese paso
   y de ningún otro) — confirmado ya el 12 y el 13/09, reconfirmado hoy.
2. **`google-genai` está en `requirements.txt`** (línea 20) — ya lo estaba.
3. **La condición de la propia regla 6 del comentario** («si ese día
   `ESTADO.md` no dice OK, se vuelve a "edge"»): hoy no hay incidencia
   (Pasos 1-3 limpios), así que se cumple y el cambio sigue adelante.

También he actualizado los tres bloques de comentario de `voz.py` que
hablaban del cambio en futuro («el lunes 14... una línea, aquí abajo») para
que digan que ya está hecho — dejar un comentario que anuncia un cambio
futuro después de haberlo hecho es la clase de cosa que confunde a quien
lea el fichero la semana que viene. `python3 -m py_compile` sobre el
fichero: sin errores.

**No he tocado nada más de código hoy** (regla 11.1, un cambio por sesión).
En concreto, dejo para mañana:

- **C31 (la función suelta en `metricas.py` + el diseño del workflow diario
  en `07_pruebas/`)** — es el siguiente de la cola y no tiene impedimento
  ninguno hoy; se ha quedado fuera solo porque la ranura de hoy tenía fecha
  fija (el motor de Gemini, «el lunes 14», con el codirector esperando
  oírlo desde el día 7). Mañana es la prioridad número uno de la cola.
- **Encargo 8 (`escena.html`: `lista` + personaje)** y **encargo 12 (CSV de
  Studio)**: sin cambios, ya resuelto este último — ver nota abajo.
- **C18 (música):** bloqueado por red, no reintentado.

**Comprobación de dos encargos que la cola seguía listando como abiertos y
ya no lo están, para no repetir trabajo mañana por error:**

- **Encargo 5 (`metricas.py`: la mediana de C26).** Ya está. `metricas.json`
  trae `control_c26` con `mediana_vistas_48h`, `n_videos`,
  `sobre_100_vistas` y `sobre_50_vistas` (commit `de66d6f`, «feat: mediana
  de métricas»). No hace falta tocar nada.
- **Encargo 12 (CSV de Studio, glob recursivo).** Ya está. `leer_export_studio()`
  en `metricas.py` busca con `EXPORTES / "**" / "*.csv"` y `recursive=True`,
  y se queda con el CSV cuya cabecera tenga a la vez columna de contenido y
  de impresiones (`_cabecera_sirve`), no con el primero por orden
  alfabético. No hace falta tocar nada.

**`07_pruebas/`:** revisado `LEEME.md` y la fecha de modificación de sus
carpetas; nada nuevo dirigido a mí desde ayer. `00_estrategia/PROMPT_DIRECCIÓN.md`
sin entradas posteriores al 12/09, ya leídas y aplicadas ayer.

## Paso 5 — esta bitácora

Este fichero.

## Para el codirector

1. **Lo que esperabas desde el día 7 ya está: el motor de voz de los Shorts
   ha pasado a Gemini por defecto hoy, lunes 14 de septiembre.** El de hoy
   (`MDS-016`) ya estaba producido con `edge` antes del cambio; el primero
   con Gemini de verdad será `MDS-017`, mañana. Si algo suena mal o falla,
   el respaldo automático a `edge-tts` está activo escena a escena y se ve
   en `ficha.json` (`motores_por_escena`); y si prefieres volver a `edge`
   mientras se ajusta, es una sola línea en `voz.py` (documentado en el
   propio fichero).
2. **Sin incidencias hoy.** `MDS-016` está limpio y publica a las 19:00
   como estaba previsto.
3. Nada nuevo pendiente de ti.

## Paquete

Ficheros nuevos o tocados en esta sesión:

- `03_produccion/pipeline/voz.py` (editado — C7: `--motor` pasa a `gemini`
  por defecto, y los comentarios que anunciaban el cambio actualizados)
- `05_calendario/bitacora/2026-09-14-revision.md` (nuevo, este fichero)
- `05_calendario/ESTADO.md` (reescrito — OK)

No lleva `registro_publicaciones.json` ni `05_calendario/qa/`. No lleva
`parrilla.json`, `CALENDARIO.md`, `demanda.json`, `demanda_bruta.json`,
`metricas.json` ni ningún fichero de `.github/workflows/`. No lleva ningún
guion de `05_calendario/guiones/` ni de `publicaciones/` — no hubo excepción
de 48 horas hoy (sin hallazgos editoriales) y ningún guion pendiente se
produce antes del jueves 17. No toca `validar_guion.py`, `escena.html` ni
`metricas.py` — el único cambio de código de la sesión es el de `voz.py`
descrito arriba.
