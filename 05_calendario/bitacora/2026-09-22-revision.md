# Revisión diaria — 22 de septiembre de 2026, 11:30 (martes)

Sesión en la nube, sin puente `mcp__remote-devices__*` (no existe en este modo, y así lo
dicen todas las bitácoras anteriores). No se ha pedido ninguna autorización, permiso ni
confirmación en toda la sesión — incluido un intento de `WebFetch` sobre la página de
Actions de GitHub que el propio sistema bloqueó pidiendo una aprobación que nadie iba a
contestar; no se ha reintentado. Clono `origin/main`, trabajo en el contenedor, entrego
con `.tar.gz` por `SendUserFile`.

## Antes de nada: `08_comunicacion/`

Leídos `08_comunicacion/novedades.md` (del codirector, sin tocar) y los tres ficheros con
fecha de los últimos siete días:

- **`2026-09-19-revision-diaria.md`** (mía): el pendiente sobre el resaltado «coral» sin
  sintaxis en `escena.html` sigue sin resolver. No lo toco hoy: sería un cambio de
  presentación y esta semana (21-25) siguen sin tocarse. Sigue anotado para la semana del 28.
- **`2026-09-21-direccion.md`**: las cinco decisiones de la sesión de dirección del lunes,
  todas ya aplicadas o en curso: (1) formato largo suspendido (C42) — sigo la regla hoy,
  `MDH-008` fuera de la parrilla y sin tocar; (2) estado en `05_calendario/estado/`, ya lo
  hago; (3) releer `origin/main` antes de decir que falta un vídeo — hecho, ver más abajo;
  (4) esta semana (21-25) no se toca voz, guion ni presentación — sigue en pie hoy, martes,
  dentro de esa ventana; (5) C47, lectura ligera diaria de métricas — código ya escrito
  ayer por esta misma tarea, sigue pendiente de que el codirector suba el workflow (ver
  `07_pruebas/metricas-diarias-21-09/`).
- **`2026-09-21-metricas_1.md`**: nota de la tarea de métricas sobre el derecho de tanteo
  (C46) y la mediana de C26 (11,0). Informativo para la planificación del jueves, no me
  toca actuar sobre ello hoy.
- `novedades.md` trae la misma pregunta del codirector sobre el vídeo de otro canal
  (labios sincronizados, posible cara ficticia) que ya estaba el 21/09. Sigue sin ser tarea
  de revisión de calidad ni de código: no la contesto ni la investigo — no es esta tarea
  programada, y no hay nadie delante para redirigirla a quien corresponda.

## Comprobación de entregas pendientes (primera acción)

`git log --oneline -8` sobre `origin/main`:

```
4536b98 Registro y expediente de calidad: MDS-022
fcdd071 revisión de métricas semanales
79695cf feat(metricas): lectura ligera diaria
0a4b7b2 Merge branch 'main' of github.com-mecanicadelhumor:mecanicadelhumor/mecanica-del-humor
7047804 cancelar largos y revisión dirección
71c5062 métricas 2026-09-21
7e6e4b7 direccion 21/09: suspension del formato largo (42), estado/ por dias (C45), el canal se lee a sí mismo (C46)
8aa8dc2 incidencia en la voz (episodio del domingo ausente del registro de publicaciones) y revisión del 21 al 26.
```

No es viernes ni sábado. El último commit es el propio bot de producción subiendo
`MDS-022` (el vídeo de hoy) a las 06:20 UTC: nada que indique un paquete de planificación a
medias. La planificación del 17 (semana 21-26) y su reescritura del 18 (C38) siguen
aplicadas; nada nuevo las toca. **Nada pendiente de aplicar.**

Re-`fetch`eado dos veces: a las 09:30 UTC (al empezar) y a las 09:42 UTC (justo antes de
escribir este fichero y el de `estado/`). Mismo `HEAD` (`4536b98`) las dos veces.

## Paso 1 — revisión editorial

Guiones españoles sin producir: `MDS-023` a `MDS-025` (23 al 25/09) y `MDH-008`
(suspendido, sin fecha). `MDS-022` (hoy) se revisa completo en el paso 3, contra el vídeo
ya producido.

- **Sin cambios de guion desde ayer.** El último commit que toca `MDS-023` a `MDS-025` sigue
  siendo `8262b7b` (18/09, reescritura C38) y el que toca `MDH-008` sigue siendo `4b275a5`
  (18/09, planificación de la semana). Las revisiones del 20 y 21/09 ya los revisaron
  enteros, escena por escena, con la regla de los dos canales, C19, expresión del
  personaje, resaltado, C17 y bibliografía. No los repito completos hoy; sí he vuelto a
  correr el validador sobre los cuatro:
  - `MDS-023` a `MDS-025`: **sin errores graves**, sin avisos.
  - `MDH-008`: los 21 avisos de siempre (planos fijos >10s, ya revisados y aceptados por
    `notas_humor`) y los tres avisos de C17 sobre A01/A05/L01, también ya revisados. Nada
    nuevo. No lo toco: está fuera de la parrilla y esta semana tampoco se tocan guiones.
- **Bibliografía:** `python3 04_agentes/validar_bibliografia.py` → «Los dos ficheros dicen
  lo mismo. Regenerar el .md es seguro.» Nada que corregir; no he abierto ningún DOI porque
  no he tocado ninguna ficha.

## Paso 2 — comprobar que la producción salió

Registro releído de `origin/main` a las **09:30 UTC** y otra vez a las **09:42 UTC**
(justo antes de escribir este fichero), sin cambios entre una lectura y otra.

- **`MDS-022` (hoy, serie «Esto no tiene gracia y esto sí»): correcto.** `estado: private`,
  `publicar_en: 2026-09-22T17:00:00Z`, `subido_utc: 2026-09-22T06:20:12Z`. Patrón automático
  esperado, no incidencia.
- **`MDS-021` (ayer): sigue `private` con `publicar_en: 2026-09-21T17:00:00Z`**, hora que ya
  pasó (son las 09:42 UTC de hoy). Esto **no es indicio de que el vídeo siga oculto**: es
  exactamente el patrón documentado («estado: el del momento de la subida», corregido solo
  por `metricas.py` los lunes o por `sincroniza_registro.yml`) y `publicar_en` está
  presente y correcto, que es la condición que separa esto del fallo real de `MDH-004`. Mi
  registro no dice «sigue sin publicarse»: dice que no lo sé desde aquí. **Pero sí hay algo
  digno de nota, y va en la sección de código de más abajo:** `sincroniza_registro.yml`
  (C31) debería haber corregido esto y no lo ha hecho.
- **`MDH-007`, `MDS-017` a `MDS-020`:** todos `public`. Sin novedad.

## Paso 3 — revisión del vídeo (MDS-022, hoy)

**`python3 04_agentes/validar_guion.py` sobre `MDS-022.es.json`: sin errores, sin avisos.**

**Revisión de los dos canales, escena por escena, contra `MDS-022.es.json`:**

1. `enunciado`, icono `i-bocadillos`: «Vístete para el *puesto*» — cuatro palabras, comillas
   angulares correctas (cita del jefe), cumple C19 (icono + ≤4 palabras, primera escena no es
   tarjeta de texto). Narración: «Mi jefe me dijo: vístete para el puesto que quieres.» —
   correlato exacto. Sin `personaje`, válido (14.3 no lo exige en todas las escenas).
2. `enunciado`: «Fui en *pijama*» — narración «Al día siguiente me presenté en pijama.»:
   correlato exacto. Sin `personaje`.
3. `comparacion`: et_a «mismo jefe, mismo *pijama*» / et_b «y ya no pasa nada» — narración
   «Y ahora vuelve a leerlo. Mismo jefe, mismo pijama, y ya no pasa nada.»: correlato
   literal. `personaje: rie` — correcto (comparación pura, ninguna de las dos caras
   tristes) y coherente con la reacción DESPUÉS del remate: el remate del chiste está en la
   escena 2 (`pausa_despues_s: 1.35`, dentro del rango 1,2-1,5 que pide la regla de la
   pausa antes del remate) y aquí, en la 3, es donde el personaje se ríe.
4. `diagrama`, fuente A06: pasos «Algo *no encaja*» / «el pijama no es ropa de oficina» y
   «Y de pronto encaja» / «sí lo es, si el puesto es quedarse en casa» — narración «Suls lo
   explicó en dos pasos: notas que algo no encaja, y luego encuentras el sentido.»
   `personaje: piensa`, correcto para una escena que expone un modelo. **Comprobado con
   cuidado porque a primera vista parece un caso del tipo MDS-009** (texto que introduce un
   dato que la narración de esa escena no dice: aquí «pijama» y «puesto» no están en la
   narración de ESTA escena). La diferencia con MDS-009 es que allí «dinero» no aparecía en
   NINGUNA escena del Short; aquí «pijama» y «puesto» los dice la narración de las escenas 1
   y 2 del mismo vídeo, y esta escena aplica el modelo abstracto de Suls al ejemplo ya
   establecido — es la reformulación concreta del propio chiste que el Short lleva contando
   desde el segundo 0, no un dato nuevo sin sostén. Quien mira sin oír ya sabe qué es «el
   pijama» y «el puesto» por las dos escenas anteriores; quien oye sin mirar entiende el
   modelo en abstracto sin necesitar el ejemplo aquí. No lo trato como hallazgo.
5. `enunciado`: «Falta el paso *uno*», icono `i-muelle` — narración «La segunda vez falta el
   paso uno, y el dos solo no da risa.»: correlato literal. `personaje: entiende`, correcto.
6. `cierre`: «Y falla aquí: es un *modelo*» + «El chiste en dos fases · incongruencia y
   resolución» — narración «Y aquí falla: lo predice un modelo de los setenta, no un
   experimento. Nadie ha medido el pijama.»: correlato exacto («es un modelo» / «un modelo
   de los setenta»). `personaje: no`, correcto: es la escena donde la narración dice que
   algo falla.

**Resaltado:** un ámbar por escena (puesto/pijama/pijama/no encaja/uno/modelo), sin cian
(no hace falta término de oficio aquí) y sin coral (sigue sin existir la sintaxis — pendiente
ya anotado, no nuevo). Nada de resaltado en `narracion`.

**Los seis `.jpg` de `05_calendario/qa/MDS-022.es/`, mirados uno a uno:** sin texto cortado,
sin elementos tapados, sin faltas de ortografía, nada en las zonas que tapa la interfaz de
YouTube. Un detalle que comprobé a fondo antes de descartarlo: en `f4_22.74s.jpg`, la
palabra «casa» al final del segundo pie del diagrama se ve mucho más tenue que el resto del
texto — a simple vista parece un corte o un fallo de contraste. Lo he recortado y ampliado
(`/tmp/crop_f4.png`, no forma parte de la entrega) y leído el código de `pintar()` en
`escena.html`: es el revelado palabra a palabra de C15.1, que hace entrar cada palabra con
su propio retardo (`paso`, entre 0,11 y 0,30 s) y una curva de suavizado; en el instante
exacto de esta captura, «casa» —la última palabra de esa línea— todavía está a mitad de su
entrada. Es el mismo caso que la palabra «uno» en `MDS-021` (revisión del 21/09), ya
verificado entonces leyendo el mismo código. No es un hallazgo.

**`ficha.json`:** `lufs -14.26` (OK), `pico_dbtp -1.38` (OK), `fragmento_antes_de_la_narracion:
false` (OK), `fragmentos.cuantos: 0` (OK — el defecto de audio de `MDS-021` no se repite
aquí), `quemados: false` (correcto), `lineas_ass: 0` (esperado con motor Gemini).
`sincronia_voz.desfase_s: 0.26` (< 0.5, OK). `voz_mezclada: false`. `modelo_voz:
gemini-3.1-flash-tts-preview`, sin `edge-tts` en ninguna de las seis escenas. `origen_voz`:
6 de 6 por llamada en vivo, 0 de caché.

**Sin incidencia hoy.** `MDS-022` está limpio en guion, vídeo y ficha técnica.

**Nota de seguimiento, no incidencia nueva:** la incidencia de ayer sobre `MDS-021`
(fragmento de audio de 0,41s en el 0:32,3) no tiene ninguna respuesta escrita en ningún
sitio —ni en `08_comunicacion/`, ni en ninguna bitácora, ni en el propio `07_pruebas/`— y el
vídeo ya pasó su hora de publicación (17:00 UTC de ayer) sin que nadie lo retirara ni lo
retrasara. No vuelvo a abrirlo como incidencia hoy (ya está dicho y ya pasó la ventana en la
que se podía actuar), pero lo dejo escrito para que quede constancia de que salió con el
defecto, por si en algún momento se pregunta por qué.

## Paso 4 — código y encargos

**Ningún cambio de código hoy.** Repasada la cola completa de `00_estrategia/tareas/revision-diaria.md`
contra la nota de dirección del 21/09 (que prohíbe tocar voz, guion o presentación hasta el
28), y todo lo que queda abierto cae en una de tres bandas:

- **Presentación, bloqueado esta semana:** P3+P4 (icono en escena 1 — YA hecho además, ver
  abajo), P6 (tres tamaños), P2/P7/P5 (semana del 21 de la cola vieja), el caso de
  `escena.html` con `lista`+personaje, y el pendiente del resaltado «coral». Ninguno se toca.
- **Voz, bloqueado esta semana:** C36/C44 (una sola toma de voz, ficha fija). No se tocan.
- **Ya hecho en sesiones anteriores, confirmado hoy sin cambios que hacer:** el campo
  `icono` y la escena 1 sin tarjeta de texto (C19+C16, visto hoy mismo en `MDS-022` f1);
  `mediana_48h_ultimos_shorts()` en `metricas.py` (C26); el glob recursivo del CSV de
  Studio; C31 (`sincronizar_registro()` extraída y con su workflow ya subido — ver más
  abajo); P10 (estructura de la serie en `validar_guion.py`, confirmado con `grep` que ya
  existe para «El experimento» y «Esto no tiene gracia y esto sí»); P9 (los tres sonidos
  acreditados en `03_produccion/sonidos/creditos.json`, indexados por sha256, solo los tres
  que existen — el cuarto de `attribution_texts.md` sigue sin fichero, como ya se dejó
  dicho). C7 (Gemini TTS en los Shorts) ya está en producción desde el 14/09, confirmado hoy
  en la ficha de `MDS-022` (`modelo_voz: gemini-3.1-flash-tts-preview`, sin `edge-tts`).
  Música (C18): sigue bloqueada por red, no reintentada.
- **Pendiente del codirector, no mío:** que suba `metricas_diarias.yml` (07_pruebas/metricas-diarias-21-09/)
  para que `metricas_diarias.json` empiece a existir; hasta entonces no puedo aplicar la
  regla de C47 (incidencia si un Short pasa 24h a 0 visualizaciones).

**Hallazgo de hoy, sin tocar código propio pero que sí toca escribir: `sincroniza_registro.yml`
(C31) parece no estar corrigiendo el registro desde hace dos días.** Es el workflow que
existe precisamente para que no se repita el caso `MDS-011` (seis días de incidencia falsa
por leer un registro caducado). Encontrado al comprobar `MDS-021` en el paso 2:

- El fichero está en `origin/main` desde el 15/09 (`b68a31b`), así que **no** es cierto que
  «falta que el codirector lo suba» — eso es lo que seguía diciendo `07_pruebas/LEEME.md` y
  lo he corregido ahí mismo (ver ficheros tocados).
- `sincronizar_registro()` en `metricas.py` **solo** escribe el fichero y actualiza
  `_estado_leido_utc` si `corregir_registro()` encuentra al menos una discrepancia
  (`04_agentes/metricas.py`, líneas 215-227 y 180-195: leído entero, la lógica es correcta
  — compara `privacidad` real contra `estado` guardado y corrige si no coinciden). El último
  commit de sincronización real es `421ba35`, **«registro: sincronizado con YouTube
  2026-09-20»**, a las 13:14 UTC del sábado.
- Desde entonces, `MDS-021` pasó de subido (06:26 UTC del 21/09) a público (17:00 UTC del
  21/09) — un cambio de estado real que el workflow debería haber detectado y corregido. El
  cron de `sincroniza_registro.yml` es `50 8 * * *` y `10 9 * * *` (dos pasadas, no las
  «tres» que dice su propia cabecera — la cabecera y el `on.schedule` no dicen lo mismo,
  aviso menor aparte). Eso da al menos dos oportunidades desde ayer a las 17:00 UTC (las de
  hoy, 08:50 y 09:10 UTC) y ninguna ha dejado commit.
- **No puedo saber desde aquí si el workflow corre y no encuentra nada que corregir (es
  decir, si `MDS-021` sigue de verdad `private` en YouTube por algún motivo, que sería grave
  y distinto) o si está fallando en silencio** (token, permisos, lo que sea): no tengo
  credenciales de YouTube ni acceso al historial de Actions desde este contenedor, y el
  intento de mirarlo por `WebFetch` pidió una aprobación que nadie iba a dar, así que no he
  insistido.
- **No lo pongo como `INCIDENCIA` en `estado/2026-09-22.md`**, porque el criterio que
  distingue esto de un fallo real (MDH-004) sigue siendo si `publicar_en` está presente y
  correcto — lo está — y porque esto no para el canal: los vídeos se siguen publicando bien,
  solo el fichero que los describe puede estar desactualizado. Pero sí lo dejo en
  `08_comunicacion/`, porque es justo el tipo de cosa que el codirector puede mirar en el
  historial de Actions en un minuto y yo no puedo mirar en absoluto.

## Y una nota sobre `07_pruebas/`

Revisado. Corregida la fila de `registro-diario-15-09/` (arriba). El resto sin novedad desde
el 21/09.

## Paso 6 — `05_calendario/estado/2026-09-22.md`

Escrito con `ESTADO: OK`: `MDS-022` (hoy) está limpio en guion, vídeo y ficha técnica, y no
hay ningún defecto de los que un espectador notaría en lo que ya está producido.
`Pendiente del codirector: nada` — lo de `sincroniza_registro.yml` no para el canal, así que
va en la bitácora y en `08_comunicacion/`, no en «pendiente».

---

**Ficheros tocados esta sesión** (los listo todos por nombre):

- `05_calendario/bitacora/2026-09-22-revision.md` (este, nuevo)
- `05_calendario/estado/2026-09-22.md` (nuevo)
- `07_pruebas/LEEME.md` (modificado: corregida la fila de `registro-diario-15-09/`)
- `08_comunicacion/2026-09-22-revision-diaria.md` (nuevo)

**No tocados, a propósito:** ningún guion, `parrilla.json`, `CALENDARIO.md`, `demanda.json`,
`registro_publicaciones.json`, `05_calendario/qa/`, ningún fichero de código en
`03_produccion/` ni `04_agentes/` (esta semana no toca presentación ni voz, y todo lo demás
de la cola ya estaba hecho), ni ningún fichero de `.github/workflows/`.
`08_comunicacion/novedades.md` leído, no escrito.
`00_estrategia/tareas/revision-diaria.md` leído como el prompt que es, no tocado.

**Resumen para quien lea solo esto:** no había entregas pendientes. `MDS-022` (hoy) revisado
completo — guion, dos canales, vídeo y ficha — sin ningún hallazgo: `ESTADO: OK`. Ningún
guion tocado (nada que lo pidiera; todos sin cambios desde su última revisión completa).
Ningún cambio de código: la cola está bloqueada por la ventana sin voz/guion/presentación
hasta el 28, y todo lo demás ya estaba hecho en sesiones anteriores. Un hallazgo de proceso,
no de contenido: `sincroniza_registro.yml` (C31) lleva desde el 20/09 sin corregir el
registro pese a que `MDS-021` cambió de estado real ese tiempo; no es incidencia (no para el
canal, y el patrón `private`+`publicar_en` sigue siendo el correcto) pero sí queda escrito en
`08_comunicacion/` para que el codirector pueda mirar el historial de Actions, que yo no
puedo ver desde aquí.
