# Revisión diaria — 7 de septiembre de 2026, 11:30 (lunes)

Sin acceso al ordenador del codirector en esta sesión (tarea programada en la
nube; confirmado al arrancar — no hay ninguna herramienta `mcp__remote-devices__*`
disponible). Trabajo sobre un clon del repositorio.

## Comprobación de entregas pendientes (primera acción)

Hoy es **lunes**: la regla de comprobar el paquete de la planificación del
jueves solo se aplica viernes/sábado. `git log --oneline -8` sobre
`origin/main` muestra la sesión de dirección de esta misma mañana
(`82a799f fix: problemas con voces y cara triste`, `fb56e8a Registro y
expediente de calidad: MDS-011`, `7f952e4 fix: producir.yml...`), ya
aplicada y en el árbol de trabajo (`escena.html` trae el `padding-bottom`
a 790px y `piensa` redibujada, `REGLAS.md` la regla 14.3 corregida a dos
caras). **No hay entrega pendiente que bloquee `05_calendario/`.** Se
procede con normalidad.

## Paso 1 — revisión editorial

Guiones pendientes de producir (cruzando `parrilla.json` con
`registro_publicaciones.json`, que ya llega hasta `MDS-011.es`): `MDS-012`
a `MDS-015` (8-11/09) y `MDH-006` (12/09). Leídos enteros, con la regla 14
(dos canales) escena por escena, `validar_guion.py` sobre los cinco (sin
errores, solo avisos ya conocidos de C17 — ver abajo) y contraste con las
bibliografía citadas.

### Regla 14.3 (la cara del personaje): nada nuevo que anotar

Repasé los candidatos a cara triste fuera de sitio (`duda`/`no` en escenas
que solo presentan) en los cinco guiones pendientes, incluidos los dos que
al principio me parecieron sospechosos:

- `MDS-013` escena 4 (`comparacion`, personaje `duda`) — ya evaluado hoy por
  la dirección (`05_calendario/bitacora/2026-09-07-direccion.md`, primera
  tanda) y marcado correcto: la narración sí dice que algo falla («Si no
  puede, no era broma»).
- `MDH-006` escena 12 (`dato`, personaje `duda`) — mismo caso, ya evaluado y
  marcado correcto hoy.
- `MDH-006` escena 28 usa `piensa`, no `duda`/`no`: con la cara redibujada
  esta mañana ya no es candidato a nada.
- El único «positivo real» que quedaba de la evaluación de la dirección
  (`MDS-015` esc.3, `piensa` en una escena `dato`) queda resuelto por el
  mismo redibujado de `piensa` — ya no es una de las dos caras prohibidas.
  No hace falta ninguna nota en `revisiones/`.

No he encontrado ningún caso nuevo. No re-abro lo que la dirección ya
litigó esta mañana.

### Rule 14.1 (texto vs. narración): un patrón que evalúo y descarto

`MDH-006` (y en menor medida `MDS-011` esc.3) usa varias veces una tarjeta
`comparacion`/`lista` que muestra las dos/tres opciones en pantalla de
golpe, pero narra solo la primera en esa escena y las demás en las
escenas siguientes (ejemplo: esc.3 «Los dos finales de esa frase» solo
narra «si sale bien»; «si sale mal» llega en la esc.4, inmediatamente
después). Lo comprobé contra el caso real de la regla (MDS-009, el
dinero que no se menciona en NINGUNA escena del vídeo) y es distinto: aquí
la información sí se dice, un compás después, con web pausa mínima entre
escenas — un oyente sin pantalla la recibe completa en cuestión de un
segundo, no se queda nunca sin ella. Lo trato como un recurso de ritmo
deliberado (mostrar la tarjeta completa, desvelar por partes), no como el
defecto que la regla 14 quiere cazar. Lo dejo escrito por si vuelve a
aparecer y conviene fijarlo como patrón aceptado.

### C17 (repetición de fuentes): sin hallazgos nuevos

`validar_guion.py` avisa de D01 (MDH-006, ya explicado en sus propias
`notas_humor` como fuente central legítima aquí, apoyo de pasada en
MDS-005) y D07 (MDH-006, apoyo de pasada en los dos vídeos con ángulos
distintos: detector de risa fingida en MDH-004, humor forzado en la
oficina aquí). Los dos, revisados, no son repetición real. Sin más avisos
de C17 en los otros cuatro guiones (fuentes todas nuevas).

### Bibliografía

`01_bibliografia/BIBLIOGRAFIA_CURADA.md`: comprobé que las catorce fuentes
citadas esta semana (D01, D02, G01, G02, L02, L03, J04, J05, B05, B06,
D06, E03, C04, C03) están todas en el corpus curado, sin duplicados. No he
verificado cada una contra la fuente original —no es una entrada nueva,
son códigos ya curados—, así que no añado ni corrijo nada. Sin cambios.

### `05_calendario/revisiones/`: una nota cerrada por obsoleta

`MDH-006.md` seguía describiendo el guion **sin adaptar** («no tiene
formato, ningún personaje, ningún escéptico», «todavía no está en
parrilla.json»). El propio guion dice en su campo `_adaptado` que se
reescribió el 3/09 y que esta nota «se retira al aplicarla» — pero el
fichero nunca se borró. Lo he marcado RESUELTO (siguiendo el mismo
formato que `MDS-013.md`/`MDS-015.md`: actualización arriba, hallazgo
original debajo como historial), con la confirmación de hoy (regla 14,
`validar_guion.py`, C17) de que el guion adaptado no tiene defectos. Las
demás notas resueltas (`MDH-004.md`, `MDH-005.md`, `MDS-006.md`,
`MDS-009.md`) ya estaban correctamente marcadas; `MDH-007.md`/`MDH-008.md`
siguen siendo backlog real (todavía no están en `parrilla.json`), sin
cambios.

**Ningún guion pendiente necesita una nota nueva en `revisiones/` hoy.**
No he editado ningún guion. No he tocado `parrilla.json`, `CALENDARIO.md`
ni `demanda.json`.

## Paso 2 — ¿salió la producción? **Aquí está la incidencia**

`MDS-011.es` (Short de hoy, `parrilla.json`: automático, 19:00) tiene en
`registro_publicaciones.json` `estado: "private"` con **`publicar_en:
null`**. Es exactamente el patrón de MDH-004 el 29/08: un vídeo `private`
sin `publicar_en` no sale nunca por sí solo.

Reconstruido con `git log`/`git show` sobre el fichero:

- Subida automática original (commit `b460a7d`, 01:32 UTC): `video_id
  I5IsDkxjaDI`, `estado: private`, **`publicar_en:
  "2026-09-07T17:00:00Z"`** — correcto, esa es la emisión de hoy.
- Rehecho a mano por el codirector esta mañana (commit `fb56e8a`, 07:52
  UTC, tras arreglar el audio — ver
  `05_calendario/bitacora/2026-09-07-direccion.md`): `video_id
  B7AaS2dXIOw` (nuevo), mismo `estado: private`, pero **`publicar_en` se
  ha quedado en `null`**. El registro no vuelve a tocarse en el commit de
  mediodía (`82a799f`).

**No lo arreglo yo**: `registro_publicaciones.json` es del bot de Actions,
no mío, y tocarlo a mano sin pasar por `registrar.py`/`publicar.py`
arriesga desincronizar el campo `video_id` real de YouTube. Lo dejo en
`ESTADO.md` como la incidencia del día — el codirector es quien puede,
desde YouTube Studio, poner la fecha de publicación a mano o publicar el
vídeo directamente.

## Paso 3 — revisión del vídeo (MDS-011, todos los días)

Con `ficha.json` y las seis capturas de `05_calendario/qa/MDS-011.es/`:

- **Audio arreglado**: `lufs -14.52` (objetivo ~-14±1), `pico_dbtp -1.33`
  (≤-1.0), `silencio_inicial` 0.629s (correcto), `fragmento_antes_de_la_
  narracion: false`. `subtitulos.lineas_ass: 108` (>0, el canario está
  bien), `quemados: false` (correcto). Sin el defecto de «guion bajo
  pensamiento divergente guion bajo» — la escena 5 del guion sigue
  llevando `_pensamiento divergente_` en la narración (no se ha tocado el
  guion, no hacía falta: `hablable()` lo limpia antes de sintetizar) y
  `validar_guion.py` avisa de ello correctamente, como aviso, no error.
- **Dos canales**: repasadas las seis escenas contra su narración — todas
  sostenidas, sin datos inventados en pantalla (el patrón de tarjeta
  compartida/narración diferida de la escena 3 es el mismo que describo
  arriba para MDH-006, y lo trato igual: no es un hallazgo).
- **Geometría pie/personaje — la parte que sí me preocupa.** Este vídeo
  concreto (`video_id B7AaS2dXIOw`) se renderizó a las 07:52 UTC, **antes**
  de que la dirección subiera el `padding-bottom` a 790px (commit
  `82a799f`, 11:22 UTC). Con el código de entonces (720px), la propia
  dirección midió esta escena exacta (MDS-011 esc.4) con `pintar(t)` en 21
  instantes: **7px de hueco en el peor momento** — el mismo caso que
  motivó la corrección de hoy. El fotograma estático de
  `qa/MDS-011.es/f4_26.89s.jpg` se ve limpio (la medida quieta engaña,
  como ya se documentó el 06/09), pero el propio informe de la dirección
  dice que con el vídeo en movimiento el hueco baja a esos 7px. **El vídeo
  que está en cola para publicar hoy no lleva el arreglo de esta mañana.**
  No he podido volver a renderizar y resubir (no toco YouTube ni tengo
  credenciales de subida desde este contenedor), así que lo dejo en
  `ESTADO.md` junto con lo del `publicar_en`: si se re-renderiza antes de
  las 19:00 con el `escena.html` de ahora, el mismo arreglo que sube el
  hueco a 43px en esta escena ya está aplicado.

## Paso 4 — código: P3+P4, el campo `icono` (C19+C16)

**El encargo de la semana**, con tope el jueves 10 a las 22:00. Implementado
completo hoy:

- **`04_agentes/esquema_guion.json`** — campo `icono` (enum de los ocho ids
  de `02_marca/iconos.svg`), admisible en cualquier escena.
- **`03_produccion/pipeline/escena.html`**:
  - Los ocho iconos, copiados como marcado real (no `<use>`: un `<use>`
    clona su contenido en un árbol de sombra que `querySelectorAll` no ve,
    así que no habría forma de animar cada trazo por separado — la misma
    razón por la que el personaje también vive duplicado en este
    documento en vez de enlazado).
  - `iconoHTML(id)` los inserta en las ocho plantillas (`titulo`, `dato`,
    `enunciado`, `lista`, `cita`, `comparacion`, `diagrama` vertical,
    `cierre`); si `d.icono` no viene, no añade nada (cero cambio para todo
    lo que ya existe).
  - `dibujarIcono()`, enganchado en `pintar(t)` reutilizando la misma `p`
    (curva de entrada, ~0,55s) que ya calcula cada unidad del escalonado:
    los trazos (`.ic`, `.ic-am`, `.ic-ci`, `.ic-co`, `.ic-te`) se revelan
    con `stroke-dasharray`/`stroke-dashoffset` medido una vez con
    `getTotalLength()` y cacheado; los rellenos (`.ic-f`: remaches,
    flechas) aparecen con un fundido corto en el último cuarto del trazo;
    las guías discontinuas (`.ic-guia`) solo cambian de opacidad, sin
    tocar su propio `stroke-dasharray` decorativo. Determinista, función
    de `t`, sin `Math.random()` (regla 11.5).
- **`04_agentes/validar_guion.py`** — dos avisos nuevos: `icono` con un id
  que no es de los ocho (para pillar erratas antes de `render.py`, que lo
  ignoraría en silencio) y C19 en la escena 1 de un Short sin `icono`, sin
  `personaje` y sin ser `comparacion` (las tres salidas válidas que fija el
  encargo). Aviso, no error: los guiones ya escritos antes de hoy no
  pueden tener `icono` y no hay que bloquearlos por eso.
- **`04_agentes/prompts/guionista_corto.md`** — una nota corta en la
  sección que ya diagnosticaba el problema («el Short no puede ser todo
  texto centrado»), documentando que el campo ya existe.

**No he tocado** `00_estrategia/tareas/planificacion-jueves.md` — la
dirección ya lo actualizó esta mañana con la instrucción de comprobar
`esquema_guion.json` antes de usar `icono`, así que no hacía falta nada
más por mi parte para que el jueves lo use.

### Verificación (regla 11.2: capturas del motor, no memoria)

1. **Barrido de regresión sobre los 480 escenas de los 27 guiones del
   repositorio** (ninguno usa `icono` todavía): `cargar()` + `pintar(dur)`
   + `comprobarDesbordes()` con el motor real → **cero problemas**, cero
   cambio respecto a antes de esta sesión. El campo no afecta a nada de lo
   que ya existe.
2. **Tres escenas de prueba** (`enunciado`+`i-bisagra`, `dato`+`i-balanza`,
   `lista`+`i-publico`, formato corto) renderizadas con Playwright:
   `comprobarDesbordes()` sin avisos en las tres; medidos los
   `strokeDashoffset` de los trazos en diez instantes entre t=0 y t=1.0s —
   bajan de largo total a 0 exactamente entre t≈0 y t≈0.55s (la `entrada`
   del escalonado) y se quedan en 0 el resto de la escena; capturas
   (`/tmp/icontest/*.png`, no forman parte del repositorio) confirman que
   se dibujan solos, sin solaparse con el texto ni el personaje, y que los
   colores heredan bien el tema (ámbar/cian/coral/tenue).
3. **Aviso nuevo probado** con un guion de prueba con `icono:
   "i-inventado"`: `validar_guion.py` avisa correctamente y no lo trata
   como error.
4. `python3 04_agentes/validar_guion.py 05_calendario/guiones/*.json`: sin
   errores nuevos (el único `ERRORES(1)` que aparece es el de siempre en
   `MDH-001.*` por duración, anterior a hoy). El aviso C19 nuevo aparece,
   como se esperaba, en los once Shorts ya escritos (ninguno usa `icono`
   todavía) — es exactamente el 72% que motiva el encargo.

### Lo que NO he tocado

`00_estrategia/` (salvo lo mío, `ESTADO.md`), ningún guion, `parrilla.json`,
`registro_publicaciones.json`, `05_calendario/qa/`, `montaje.py`, ningún
fichero de `.github/workflows/`.

## `07_pruebas/`

Revisado `LEEME.md` y el contenido de la carpeta: `prueba-de-voces/` ya
respondida el 04/09, `producir-yml-07-09/` ya aplicado (está en el
commit `82a799f` de esta mañana, línea `producir.yml | 4 +-`). Nada nuevo
dirigido a mí hoy. No dejo ninguna prueba nueva.

## Paso 5 — esta bitácora

Este fichero.

## Para el codirector

1. **INCIDENCIA — `MDS-011` no va a publicar solo esta tarde.** Ver
   `ESTADO.md` y el detalle en el paso 2 de arriba.
2. **El vídeo en cola tampoco lleva el último arreglo de geometría**
   (padding-bottom 790px, de las 11:22 de hoy) — ver paso 3. Si se
   re-renderiza para poner `publicar_en`, ya sale con el arreglo puesto.
3. **P3+P4 (el campo `icono`) entregado**, verificado, cero regresiones.
   Lo puede usar la planificación del jueves 10.
4. Resto de la cola sin cambios: P6, P10, C7 (Gemini TTS), `metricas.py`
   (la mediana), etc. — siguen en `PLAN_DE_CAMBIOS.md` versión 6.1.

## Paquete

Ficheros tocados en esta sesión:

- `04_agentes/esquema_guion.json` (editado — campo `icono`)
- `03_produccion/pipeline/escena.html` (editado — motor del icono)
- `04_agentes/validar_guion.py` (editado — avisos de `icono` y C19)
- `04_agentes/prompts/guionista_corto.md` (editado — nota sobre `icono`)
- `05_calendario/revisiones/MDH-006.md` (editado — nota cerrada por
  obsoleta, adaptación ya aplicada el 03/09)
- `05_calendario/bitacora/2026-09-07-revision.md` (nuevo, este fichero)
- `05_calendario/ESTADO.md` (reescrito — `INCIDENCIA`)

No lleva `registro_publicaciones.json` ni `05_calendario/qa/`. No lleva
`parrilla.json`, `CALENDARIO.md`, `demanda.json` ni ningún guion de
`05_calendario/guiones/`. No lleva ningún fichero de `.github/workflows/`
ni `montaje.py`. No lleva los ficheros de prueba temporales
(`/tmp/icontest/*`), que nunca formaron parte del árbol del repositorio.
