# Revisión diaria — 10 de septiembre de 2026, 11:30 (jueves)

Sin acceso al ordenador del codirector en esta sesión (tarea programada en la
nube; el propio sistema lo confirma al arrancar — sin `mcp__remote-devices__*`
disponibles esta vez ni siquiera para probar). Trabajo sobre un clon de
`origin/main`, entrego con `.tar.gz`.

**Hoy es jueves, no viernes/sábado**, así que la regla de comprobar el
paquete de la planificación del jueves (que hoy mismo escribe esa
planificación, a las 22:00) no se aplica todavía: se aplica mañana. `git log
--oneline -20` sobre `origin/main` muestra la producción automática de esta
madrugada (`757b3fe Registro y expediente de calidad: MDS-014`) encima de la
revisión de ayer (`561b88a`), ya aplicadas. **No hay entrega pendiente que
bloquee `05_calendario/` hoy.** Se procede con normalidad, con un ojo puesto
en el reloj: los encargos 1-3 (P3+P4, P6, P10) tienen tope hoy a las 22:00, y
los tres estaban ya entregados y aplicados antes de hoy (07/09 y 09/09) —
comprobado en el código (`esquema_guion.json`, `escena.html`,
`validar_guion.py`, y el propio `guionista_corto.md` ya documenta `icono`)
antes de gastar nada más en repetir ese trabajo.

## Paso 1 — revisión editorial

Guiones pendientes de producir (`parrilla.json` contra
`registro_publicaciones.json`, que ya llega hasta `MDS-014.es`): `MDS-015`
(11/09) y `MDH-006` (12/09). `python3 04_agentes/validar_guion.py` sobre los
dos: sin errores, solo avisos ya conocidos (C19 en MDS-015 — la planificación
de esta noche es la primera que puede usar `icono` en escena 1 — y dos
escenas largas + dos C17 en MDH-006).

### `MDS-015`: leído entero con la regla 14, sin hallazgos

Cinco escenas, serie «Ríete primero, te explico después». Los dos canales se
sostienen escena por escena: el chiste inicial (alarma de coche) no necesita
fuente porque es anécdota, no dato; la cifra «10-16 millones de años»
(escena 3, fuente E03) tiene su `pie` como reformulación evidente, no un
dato nuevo; «_umbral del dolor_» en cian (escena 4, fuente D06) es el
término exacto del estudio, no decorativo; y el cierre («no está medido»)
coincide con la nota de `notas_humor` sobre ser el «dónde falla» más
honesto de la semana. Caras: `rie` en la escena de la risa contagiada,
`piensa` en la escena `dato` (correcto: presenta, no falla), `no` en el
cierre (correcto: ahí sí falla algo). Coral no se usa —comprobado en
`escena.html`, `rico()` solo interpreta `*ámbar*` y `_cian_`; no existe
ninguna sintaxis de marcado para coral en el guion todavía, así que no hay
manera de que este guion lo use mal—. Sin hallazgos nuevos.

### `MDH-006`: **un hallazgo nuevo** (regla 14.3), y dos C17 evaluados y descartados

La lectura de fondo (regla 14.1/14.2, cifras y fuentes) ya se hizo el
07-08/09 y las tres notas siguen abiertas en `revisiones/MDH-006.md`. Lo que
faltaba por mirar con ojos frescos era la regla 14.3 completa (la cara)
escena por escena, porque el 07-08/09 se centraron en el resaltado y los
datos huérfanos. Encontrado:

- **Escena 12** (`tipo: dato`, fuente D01): `personaje: "duda"` en una
  escena que presenta «el giro» del segmento sobre humor y estatus en el
  trabajo (que arriesgarse sube la confianza percibida incluso fallando).
  Regla 14.3: `dato`/`comparacion`/`enunciado` que solo presentan van con
  `neutra`/`entiende`/`rie`/`piensa`, nunca `duda`/`no`. Es el mismo
  mecanismo que motivó la corrección de la regla el 07/09 (MDS-011 escena
  4), solo que con la otra cara prohibida. Corrección propuesta: cambiar
  a `piensa`, que además es justo la cara que se redibujó ese día para
  poder presentar sin parecer triste. Detalle completo, antes/después, en
  `revisiones/MDH-006.md` (nueva sección arriba; las tres notas del 07-08/09
  se quedan debajo intactas). **MDH-006 publica el sábado 12/09 a las
  12:00 — desde esta revisión faltan ~48h30, así que se queda justo FUERA
  de la excepción de las 48 horas**: nota a `revisiones/`, no toco el guion.

- **C17, D01** (broma apropiada/inapropiada y estatus): central en MDH-006
  (diez escenas, 6-9/12-15/36-37, el hilo entero del segmento de trabajo).
  Ya citada en MDS-005, pero ahí en una sola escena de cierre, como apoyo de
  pasada, citando un hallazgo distinto del mismo estudio (que el chiste
  inapropiado hunde, no que arriesgarse sube la confianza) para una tesis
  completamente distinta (la bisagra del chiste). Las dos frases no se
  parecen, las dos tesis no se solapan: un espectador de MDS-005 no tiene la
  sensación de «esto ya me lo han contado» viendo MDH-006. **Evaluado y
  descartado**, razonamiento completo en `revisiones/MDH-006.md` para que no
  se reabra sin releerlo.
- **C17, D07** (risa real/fingida en 24 sociedades): apoyo de pasada en
  MDH-006 (una sola escena, 34), aunque fue central en MDH-004. No cumple la
  condición de la regla («fuente central... no un apoyo de pasada») del
  lado de hoy. **Evaluado y descartado.**

### Bibliografía

Comprobadas contra `01_bibliografia/BIBLIOGRAFIA_CURADA.md` las fuentes de
MDH-006 que no se habían verificado en sesiones anteriores: G01 (Cooper,
Kong y Crossley — humor del líder, poder y asimetría) y G02 (Mesmer-Magnus
et al. — metaanálisis, tamaños de efecto moderados) casan exactamente con
lo que narran las escenas 24-27 y 31-32. D02 (guía práctica de Bitterly y
Brooks) ya se había comprobado el 07/09. Las ocho de MDS-014/MDS-015 se
verificaron el 08/09. Sin cambios en el corpus curado.

## Paso 2 — ¿salió la producción?

`registro_publicaciones.json` trae `MDS-014.es` (subido hoy 01:32 UTC,
`estado: private`, `publicar_en: "2026-09-10T17:00:00Z"` — correcto, es la
emisión de hoy).

**`MDS-011.es` sigue exactamente igual, ya van cuatro días de calendario
(07, 08, 09, 10/09) y unas 64,5 horas desde su hora de emisión (07/09,
19:00):** `estado: "private"`, `publicar_en: None`, sin ni un carácter de
cambio desde el 07/09 por la tarde (comprobado otra vez con `git log -p`
sobre la entrada: los tres commits de MDS-012/013/014 solo añaden sus
propias entradas al final). Es el mismo caso que llevo escalando desde el
08/09: se rehizo a mano ese día con `plan_suelto()`, que deja
`publicar_en=None` por diseño para que se revise antes de publicar, y nadie
lo ha revisado desde entonces. No lo toco (`registro_publicaciones.json` es
del bot de Actions). Sigue en `ESTADO.md`.

## Paso 3 — revisión del vídeo de hoy (MDS-014)

`ficha.json`: `lufs -14.39` (objetivo ±1), `pico_dbtp -1.41` (≤-1,0),
`fragmento_antes_de_la_narracion: false`, `subtitulos.quemados: false`
(correcto), `lineas_ass: 111` (canario bien, >0 — este vídeo se hizo con
motor edge, de antes de que existiera C7). Sin avisos.

Miradas las seis capturas de `05_calendario/qa/MDS-014.es/`: sin texto
cortado ni elementos tapados, buen contraste, nada cae en las zonas de
interfaz de YouTube, el personaje se lee bien a tamaño de móvil (`rie` en
la anécdota, `piensa` en la comparación rasgo/estado — correcto, presenta,
no falla —, `no` en el cierre — correcto, ahí sí falla algo).

Dos canales, escena por escena contra el guion, y **un hallazgo menor en la
escena 4**: la narración dice «Lo jovial que eres y lo jovial que estás
**hoy**», y en pantalla el lado «estado» de la comparación pone «Lo jovial
que estabas el **martes**». «Martes» no aparece en la narración de esta
escena ni de ninguna otra del guion (la única vez que aparece «martes» es en
el título subido a YouTube, que un espectador mudo no ve dentro del vídeo).
No es el caso MDS-009 —no introduce un dato que cambie de qué trata el
mecanismo, y «rasgo vs. estado» se entiende igual sin ese día concreto—,
pero técnicamente sí falla el criterio literal de la regla 14.1 (sustantivo
en pantalla sin correlato en la narración de esa escena, y «martes» no es
una reformulación evidente de «hoy»). Lo dejo escrito con toda la precisión
para que decida él: a mi juicio es defendible dejarlo pasar (es un matiz de
una palabra, no confunde el mecanismo), pero no me corresponde decidir eso
por él — la lección del 03/09 sigue siendo la contraria a callarlo.

## Paso 4 — código: C7, Gemini TTS en los Shorts (`voz.py`)

**El cambio de código de la sesión.** Con P3+P4, P6 y P10 (encargos 1-3)
confirmados como ya entregados antes de hoy, tocaba el siguiente de la cola
con presión real de fecha: C7 necesita escribirse **esta semana** (con
`--motor edge` por defecto, sin tocar la producción de hoy) para que el
lunes 14 solo haga falta cambiar un valor por defecto. No se había escrito
nada de esto todavía (comprobado: `voz.py` no tenía `--motor` ni caché antes
de esta sesión).

**Ficheros tocados, y por qué los tres son un solo encargo:**

- **`03_produccion/pipeline/voz.py`** — el cambio central:
  - `--motor {edge,gemini}` (por defecto `edge`; pasa a `gemini` el lunes
    14 cambiando una sola línea, comentada en el propio argparse).
  - Gemini solo entra si `--motor gemini` **y** `guion["formato"] ==
    "corto"` (los Shorts): un episodio largo (~40 escenas) no cabe en las
    10 peticiones/día del nivel gratuito, así que ni lo intenta.
  - Una llamada por escena, una sola voz por llamada (`Charon` para
    narrador, `Puck` para escéptico — la misma asignación que ya usaba
    `04_agentes/prueba_voz.py`, el escalón anterior de C7), reutilizando su
    mismo patrón de llamada a la API (`google-genai`, con las dos formas
    del SDK por si cambia de sitio, igual que allí).
  - **Caché por contenido**, `03_produccion/cache_voz/<sha256(narración+
    motor+voz)>.mp3`: antes de llamar a Gemini se mira si ya está. Solo
    para Gemini —edge-tts no tiene cuota que ahorrar y cachearlo perdería
    las marcas de palabra que sí necesita—.
  - **Respaldo automático e inmediato a edge-tts** ante cualquier fallo de
    Gemini en una escena (cuota, red, respuesta vacía), sin reintentos que
    arriesguen el RPM de 3/min. Si el fallo es de cuota **diaria**, el
    resto de la producción va directa a edge sin más intentos (probado:
    con un mock de "RESOURCE_EXHAUSTED... per day" en la primera escena de
    tres, solo se intenta Gemini una vez y las tres acaban en
    `edge (respaldo)`; con un fallo genérico de red, cada escena lo vuelve
    a intentar por su cuenta).
  - 25s entre llamadas **reales** a Gemini (ni caché ni respaldo cuentan).
  - Motor real usado por escena en `e["motor_voz"]` (`gemini`,
    `gemini (caché)`, `edge` o `edge (respaldo)`), y ritmo por escena en
    `e["ritmo_pal_s"]` con aviso si cae fuera de 1,6-3,2 palabras/segundo
    (no bloquea).
  - El recorte de sílaba suelta al principio de escena (`fragmento_inicial`)
    solo se intenta si la escena de verdad vino de edge-tts: el patrón se
    midió sobre ese motor y su red de seguridad depende de marcas de
    palabra que Gemini no da.
  - Sin marcas de palabra tras una producción con Gemini, el aviso de «el
    sintetizador puede haber dejado de mandar WordBoundary» ya NO se
    dispara — se sustituye por un mensaje que dice que es lo esperado, para
    no confundir una consecuencia conocida con un fallo nuevo.

- **`03_produccion/pipeline/qa.py`** — el canario sustituido, que el propio
  encargo pide explícitamente: sin marcas de palabra en las escenas Gemini,
  `lineas_ass` deja de servir de canario para esas producciones. Añadida
  `sincronia_voz()`: compara la duración calculada por `voz.py`
  (`duracion_total_s`, la que usa `render.py`) con la duración real de
  `voz.mp3` (ffprobe), con margen de 0,5s — se calcula siempre que exista
  `voz.mp3`, no solo con Gemini, porque es una comprobación más barata y
  directa que nunca sobra. También se añade al resumen de `guion` en la
  ficha qué motor pidió la producción y cuántas escenas salieron con cada
  motor real, y qué escenas quedaron con el ritmo fuera de rango.

- **`requirements.txt`** — añadido `google-genai` (import perezoso en
  `voz.py`, así que no afecta a las producciones que siguen en edge-tts).

**Lo que NO puedo cerrar yo, y hay que decirlo ahora para que no se
descubra el lunes 14:** `producir.yml` (protegido) sigue sin exponer el
secreto `GEMINI_API_KEY` al paso que llama a `voz.py` — hoy solo lo tiene
`voz_prueba.yml`, el workflow manual de la prueba de voces. Cambiar el
valor por defecto de `--motor` a `gemini` el lunes sin ese secreto en el
entorno no rompe nada (cae al respaldo edge en la primera escena y sigue),
pero tampoco prueba nada: hace falta añadir `GEMINI_API_KEY` al `env:` del
paso de `voz.py` en `producir.yml` antes del lunes 14 para que C7 se
encienda de verdad. Es exactamente el tipo de cambio de `.github/workflows/`
que solo puede hacer el codirector.

### Verificación (regla 11.2 y 11.5: el motor, no memoria; determinista)

Sin `GEMINI_API_KEY` ni red a Google desde este contenedor, no se puede
probar la llamada real a la API (como ya pasaba con `04_agentes/
prueba_voz.py` cuando se escribió). Lo que sí se puede y se ha hecho, en
`/tmp` (fuera del repositorio, con `edge-tts` instalado para poder importar
`voz.py`):

1. **Cinco escenarios de lógica**, mockeando `_gemini_pcm` y `sintetizar`
   (edge) pero dejando ffmpeg/ffprobe reales sobre audio sintético (donde
   vive el riesgo real: la conversión PCM→mp3 y la medición de duración):
   motor edge puro (no toca Gemini para nada); Gemini con éxito en las dos
   escenas de un Short; la MISMA producción repetida sirviendo las dos
   desde caché sin llamar a la API; fallo de red en dos escenas seguidas
   (cada una lo vuelve a intentar por su cuenta, ninguna se rinde de más);
   fallo de cuota **diaria** en la primera escena de tres (solo un intento
   a Gemini, las tres acaban en `edge (respaldo)`); y `--motor gemini`
   pedido sobre un guion `formato: "largo"` (se ignora del todo, cero
   llamadas a Gemini). Las cinco pasan.
2. **`sincronia_voz()` de `qa.py`**, cuatro casos: duración calculada y
   real que cuadran (desfase < 0,5s), que no cuadran (desfase > 0,5s), sin
   `voz.mp3` (devuelve `None`) y sin duración esperada (devuelve `None`).
   Los cuatro correctos.
3. `python3 -m py_compile` sobre los dos ficheros: sin errores de sintaxis.
4. Repositorio limpio después de la prueba: `03_produccion/cache_voz/` NO
   se creó dentro del repositorio (la prueba redirige la constante a un
   directorio de `/tmp`), y `git status` solo muestra los tres ficheros de
   verdad tocados.

**Lo que NO se ha podido probar, y hay que decirlo:** la llamada real a
`gemini-3.1-flash-tts-preview` (voces, calidad, si el SDK usa
`interactions.create` o `models.generate_content` en la versión que se
instale), porque este contenedor no tiene `GEMINI_API_KEY` ni salida a
Google. Eso solo se puede verificar en GitHub Actions, con el secreto
puesto. Recomendación: antes del lunes 14, o bien re-lanzar
`voz_prueba.yml` apuntando a este código (no lo hace todavía; prueba el
guion entero de una vez, no escena a escena), o bien lanzar `producir.yml`
una vez con `--motor gemini` forzado a mano sobre un Short ya pasado, en
privado, para ver un vídeo real antes del lunes.

## `07_pruebas/`

Revisado `LEEME.md`: `prueba-de-voces/` sigue respondida desde el 04/09,
`producir-yml-07-09/` ya aplicado. Nada nuevo dirigido a mí hoy. No dejo
ninguna prueba nueva: el encargo 6 (workflow `voz_adelantada.yml` propuesto
en `07_pruebas/`) es expresamente para la semana del 14, después de cerrar
1-5, y hoy toca 4 (C7).

## Paso 5 — esta bitácora

Este fichero.

## Para el codirector

1. **INCIDENCIA — dos cosas, la vieja y una nueva pequeña.** (a) `MDS-011`
   sigue sin fecha de publicación, ya cuatro días de calendario. (b)
   `MDS-014` (hoy) trae un matiz menor en la escena 4: pantalla dice «el
   martes», la voz dice «hoy» — no confunde el mecanismo, pero no lo
   dice la narración de esa escena. Detalle de las dos en `ESTADO.md` y
   arriba.
2. **C7 escrito esta semana, con `--motor edge` por defecto** (no toca la
   producción de hoy): caché, respaldo automático, canario nuevo en
   `qa.py`. **Falta un secreto en `producir.yml`** (`GEMINI_API_KEY`) antes
   del lunes 14 para que el cambio de motor sirva de algo — no puedo
   tocar ese fichero.
3. **Un hallazgo nuevo en MDH-006** (`revisiones/MDH-006.md`, regla 14.3,
   escena 12), para que la planificación de esta noche lo aplique antes del
   12/09. Dos C17 evaluados y descartados con el razonamiento por escrito.
4. **`MDS-015` sin hallazgos.**
5. Resto de la cola sin cambios: `metricas.py` (la mediana, sigue con
   prioridad alta, antes del 27/09), P1/P8 y la caché de voz por adelantado
   (semana del 14, ya con la caché de C7 puesta), P9 (sonidos, sigue
   esperando la autorización nueva en `PROMPT_DE_ARRANQUE.md`).

## Paquete

Ficheros tocados en esta sesión:

- `03_produccion/pipeline/voz.py` (editado — C7: `--motor`, Gemini por
  escena, caché, respaldo automático, ritmo, motor por escena)
- `03_produccion/pipeline/qa.py` (editado — canario `sincronia_voz()`,
  resumen de motor/ritmo en la ficha)
- `requirements.txt` (editado — añadido `google-genai`)
- `05_calendario/revisiones/MDH-006.md` (editado — cuarto hallazgo arriba,
  historia del 07-08/09 intacta debajo)
- `05_calendario/bitacora/2026-09-10-revision.md` (nuevo, este fichero)
- `05_calendario/ESTADO.md` (reescrito — INCIDENCIA)

No lleva `registro_publicaciones.json` ni `05_calendario/qa/`. No lleva
`parrilla.json`, `CALENDARIO.md`, `demanda.json` ni ningún guion de
`05_calendario/guiones/`. No lleva ningún fichero de `.github/workflows/`
ni `montaje.py`. No lleva la caché de prueba ni el script de verificación
de `/tmp`, que nunca formaron parte del árbol del repositorio.
