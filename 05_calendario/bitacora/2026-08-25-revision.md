# Revisión diaria — 25 de agosto de 2026 (martes)

## Comprobación de entregas pendientes

`git log --oneline -8` sobre `origin/main`: el último commit es
`640cbb1` (registro de calidad de `MDS-002`, de hoy). Hoy no es viernes ni
sábado, así que el bloqueo de `05_calendario/` de `PROPIEDAD_DE_FICHEROS.md`
no aplica. La próxima planificación semanal es el jueves 27/08; hasta
entonces no hay paquete de planificación que esperar. Sin entrega pendiente.

De paso, algo que no esperaba: la nota de `revisiones/MDS-005.md` de ayer
(el rótulo prohibido «La violación») **ya está aplicada** — Silvestre la
metió a mano el lunes dentro del commit `96e52cf` («cambios en la medición
de métricas»), junto con la corrección paralela en `notas_humor` («violación
benigna» → «ruptura benigna», que mi nota de ayer no pedía pero es
coherente). El fichero está movido a `revisiones/_aplicadas/`. Sin acción
por mi parte, solo lo dejo anotado.

## Paso 1 — revisión editorial

Guiones españoles sin producir (`parrilla.json` cruzado con
`registro_publicaciones.json`, que hoy suma `MDS-001` y `MDS-002.es`,
producido esta madrugada a las 03:38 UTC): `MDS-003` a `MDS-006`, `MDH-004`
a `MDH-008`. Comprobado por fichero que ninguno ha cambiado desde la
revisión de ayer (último commit de cada uno es `4c0ee2b`, del 21/08, o
anterior) — así que las cinco notas abiertas en `revisiones/` (`MDH-004` a
`MDH-008`) siguen vigentes tal cual, no hace falta reescribirlas.

`python3 04_agentes/validar_guion.py` sin errores en los nueve: mismos
avisos de ritmo/monotonía que ayer en los cuatro `MDH` sin adaptar (ya
recogidos en sus notas), nada nuevo.

Encima de eso, pasé comprobaciones automáticas específicas sobre los nueve
guiones (no las había hecho ayer con este detalle):

- **Palabra prohibida «violación»**: solo aparece una vez en todo el
  repositorio, en una nota interna de `MDH-001.es.json` (ya producido y
  publicado, no en pantalla, fuera de alcance). Ninguna en los guiones sin
  producir.
- **Comillas rectas** en `narracion`/`texto`: ninguna: todas usan « » donde
  corresponde.
- **Resaltado ámbar (`*así*`)**: como mucho uno por campo de texto en pantalla
  en los nueve guiones. Sin violaciones.
- **Escéptico en `MDH-004`** (el único de los cuatro `MDH` con esa voz):
  escenas 8 y 21, 10 palabras cada una — dentro del límite de 12.
- **Pausa antes del remate en los cuatro Shorts** (`MDS-003` a `MDS-006`):
  1,2 / 1,3 / 1,4 / 1,3 s en la escena 0, las cuatro dentro de 1,2-1,5.
- **Remate real en la última escena**: las cuatro cierran con 20-25 palabras
  de narración y dicen dónde falla, muy por encima del mínimo de seis.

Sin hallazgos nuevos. Las cinco notas abiertas de ayer (bibliografía de
`MDH-004`, adaptación pendiente de `MDH-005` a `MDH-008`) se quedan como
están: nadie las ha tocado y siguen esperando a la planificación del jueves.

## Paso 2 — ¿salió la producción?

`registro_publicaciones.json` tiene `MDS-002.es` (subido `2026-08-25T03:38:32Z`,
`estado: private`, en modo `revision` como marca `parrilla.json` para esta
semana), justo lo que tocaba hoy. Sin incidencia.

## Paso 3 y 4 — no tocan hoy

Hoy es martes: la revisión de vídeo y la mejora visual son de lunes y
jueves. No las hago.

Sí hice una comprobación barata y fuera de esos pasos: confirmar que el
arreglo de `qa.py` de ayer (el campo `quemados` que mentía `true` cuando no
se había quemado nada) llegó al repositorio y funciona con datos reales.
`05_calendario/qa/MDS-002.es/ficha.json` (generado hoy) trae
`"quemados": null` con `"quemados_origen": "sin manifiesto de montaje.py: no
se puede verificar"` — ya no miente `true`. De paso, sin ser revisión de
vídeo formal: `lineas_ass: 114` (canario sano), `lufs: -14.59`, `pico_dbtp:
-1.22` (dentro de rango) y `fragmento_antes_de_la_narracion: false`. Nada
que anotar.

## Lo que cambié

Nada en `03_produccion/` ni en `04_agentes/` hoy: el arreglo de `qa.py` de
ayer ya está confirmado funcionando y no encontré ningún defecto nuevo que
justifique tocar código. La propuesta de ayer para `montaje.py` (manifiesto
`montaje.json` con `subtitulos_quemados`, protegido, necesita permiso de
Silvestre) sigue sin aplicarse — no la repito aquí, ya está en
`bitacora/2026-08-24-revision.md`.

## Descartado

- Reescribir las notas de `revisiones/MDH-005.md` a `MDH-008.md`: siguen
  siendo exactas, ninguno de esos guiones ha cambiado desde ayer.
- Tocar algo en `03_produccion/`/`04_agentes/` solo por tocar: no apareció
  ningún defecto nuevo hoy, y la regla de un cambio por sesión es para
  cuando hay algo que cambiar, no una obligación de cambiar algo.

## Paquete

`mdh-revision-2026-08-25.tar.gz`, con exactamente este fichero:

- `05_calendario/bitacora/2026-08-25-revision.md` (nuevo, este fichero)

No lleva ninguna nota nueva en `revisiones/` (las de ayer siguen vigentes,
no hacía falta tocarlas) ni cambios en `03_produccion/`/`04_agentes/` (nada
que arreglar hoy). No lleva `registro_publicaciones.json` ni
`05_calendario/qa/`.

## Para Silvestre

1. Sin entregas pendientes ni defectos nuevos hoy.
2. Sigue pendiente de tu decisión: el diff propuesto ayer para
   `montaje.py` (manifiesto de subtítulos quemados) y si merece la pena
   C6.3 a preescalado ×4, ambos en `bitacora/2026-08-24-revision.md`.
3. Recordatorio de lo que ya sabías: `MDH-005` a `MDH-008` siguen sin
   adaptar al formato nuevo y `MDH-004` sigue con el aviso de bibliografía
   sin cerrar en la ficha `E02` — ambos son trabajo de la planificación del
   jueves 27/08, ya anotado en `revisiones/`.
