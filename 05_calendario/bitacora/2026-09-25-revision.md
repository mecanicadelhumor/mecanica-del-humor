# Revisión diaria — 25 de septiembre de 2026, 11:30 → sesión extendida (viernes)

Sesión en la nube, sin puente `mcp__remote-devices__*` (no existe en este modo). No se ha
pedido ninguna autorización, permiso ni confirmación en toda la sesión. Clono `origin/main`,
trabajo en el contenedor, entrego con `.tar.gz` por `SendUserFile`.

**Mensaje en directo del codirector durante la sesión (no por fichero):** avisa de que la
planificación semanal no se hizo anoche por un imprevisto administrativo, que no es grave, y
que la hará él mismo justo después de esta revisión; y que hoy no sabe si habrá sesión de
dirección porque está ocupado con otras cosas. Lo dejo escrito aquí porque explica exactamente
lo que se ve en el punto siguiente, y porque si alguna otra tarea programada lee esta bitácora
antes de que él aplique su paquete, que sepa que el hueco es transitorio y esperado.

## Antes de nada: `08_comunicacion/`

Leídos `novedades.md` (del codirector, sin tocar; sigue con la pregunta sobre el vídeo de otro
canal, que no es tarea de esta revisión) y los ficheros con fecha de los últimos siete días:
`2026-09-23-direccion.md` y `2026-09-23-direccion-c50.md`. Ambos ya incorporados a
`00_estrategia/tareas/revision-diaria.md` (versión 12/13 del plan); no traen nada que no esté
ya en mi prompt. No hay ficheros de planificación ni de métricas de los últimos días.

## Comprobación de entregas pendientes (primera acción)

`git log --oneline -8` sobre `origin/main`:

```
31d3260 Registro y expediente de calidad: MDS-025
dc2ea94 medición de demanda 2026-09-24
0932030 registro: sincronizado con YouTube 2026-09-24
90592a2 Registro y expediente de calidad: MDS-024
0c0b2ed registro: sincronizado con YouTube 2026-09-23
82c50d1 Registro y expediente de calidad: MDS-024
cf1208e Visuales (C50): manifiestos y hojas de contactos
373541a C50 en produccion: visuales.yml y el paso del material visual
```

**Es viernes y NO hay un commit con los guiones de la planificación de esta semana** (la
última «Planificación semana…» es la del 17/09, para el 21-26). `parrilla.json` no tiene
ninguna emisión más allá del 26/09 y no existe `MDS-026`. Es justo el caso que cubre la regla
1: **no toco absolutamente nada de `05_calendario/` salvo lo mío** (esta bitácora, el estado
del día y `revisiones/`) y me dedico a los pasos 3 y 4. El mensaje en directo de arriba confirma
que es un retraso conocido y que se aplicará después de esta entrega, no una entrega perdida.

Como no hay guiones nuevos que revisar, **la lectura en frío del paso 1 no tiene sobre qué
correr hoy**: el único Short pendiente de producir es la red de seguridad de `MDS-023` del
sábado 26, que ya se produjo bien el 23/09 (`video_id kRmwbAW6eSo`, `estado: public` — lo
confirma el registro) y que por tanto `cola.py` no va a repetir; y el domingo no hay emisión.
Nada de `05_calendario/visuales/` que revisar para mañana tampoco, por el mismo motivo (`MDS-023`
no lleva campo `visual`).

`git fetch` repetido antes de cerrar (14:11 UTC): trajo el commit de sincronización del
registro de hoy. Sin cambios en la conclusión anterior.

## Paso 2 — producción

`registro_publicaciones.json`, releído tras el último `fetch`:

- `MDS-023`: `public`, `kRmwbAW6eSo`, publicado 23/09 17:00 UTC. Correcto.
- `MDS-024`: `public` (ya sincronizado hoy a las 14:11 UTC), `N2qUpGcAy2Y`, publicado 24/09
  17:00 UTC. Correcto.
- `MDS-025`: `private` con `publicar_en: 2026-09-25T17:00:00Z`, `DVKoEJDPCqM`, subido hoy a las
  06:19:48 UTC. Correcto — son las 14:23 UTC, todavía no toca publicarlo.
- `diagnostico.json` (C50): Pexels y Pixabay `ok`; `cloudflare_token` `ok`; `cloudflare_imagen`
  `ok`; solo falla `cloudflare_token_de_cuenta` (401, token sin alcance de cuenta). Como Pexels
  no falla, no es incidencia — queda aquí para quien mantenga las claves de Cloudflare.

Sin incidencias de producción propiamente dichas.

## Paso 3 — revisión del vídeo, y el hallazgo del día

Reviso `MDS-024` (ayer, sin revisar antes: no hay bitácora ni estado de los días 23 ni 24 en
el repositorio) y `MDS-025` (hoy).

**`validar_guion.py` sobre los dos guiones: sin errores graves.** Un aviso en `MDS-024`
(duración 53s frente a los 40s de referencia de la serie — es aviso, no error, desde el 23/09;
no hay nada que cosa mal, la lectura en frío la haría la dirección si tocara este guion, que no
es el caso). `fuentes citadas`: `L04` (MDS-024) y `G04` (MDS-025), ninguna repetida como fuente
central en las últimas seis semanas (C17, comprobado contra `MDS-014` a `MDS-023`).

Regla de los dos canales sobre las seis escenas de cada guion: todo lo que hay en pantalla
(`texto`, `a`/`b`, `titulo`, `subtitulo`, `cifra`, `pie`) tiene correlato en la narración de su
propia escena; ninguna cara `duda`/`no` fuera del cierre; los dos cierres dicen dónde falla
(«Truco de actores, no de científicos» / «Eran diez por grupo») con dos redacciones distintas,
que es justo lo que pedía el punto 8 de la nota del 23/09. Comprobado también contra los
fotogramas de `qa/`: texto dentro de zona segura, sin cortes, contraste correcto, cara del
Engranaje coherente con el tono en los seis fotogramas mirados.

**`ficha.json` → `visual`: `null` en los dos.** Es el primer y segundo Short producidos desde
que C50 entró en marcha (24 y 25/09) y los dos han salido con el fondo de siempre, pese a que
los dos SÍ tenían manifiesto en `05_calendario/visuales/` (`MDS-024.json`, `MDS-025.json`).
Por la regla del paso 3.5, esto es `INCIDENCIA`. Busco la causa en el propio manifiesto en vez
de en el log de Actions (no hace falta: el manifiesto ya la trae escrita):

- `MDS-024.json` y `MDS-025.json` → `"errores_ia": ["module 'cv2' has no attribute
  'CascadeClassifier'"]`, y en `descartes` **todos y cada uno** de los candidatos de **todas**
  las escenas de los dos Shorts vienen rechazados con el motivo `"no se pudo mirar: module 'cv2'
  has no attribute 'CascadeClassifier'"`. Cero candidatos sobrevivieron nunca: por eso las 15-16
  planos de cada guion terminan en `"fuente": "sin_imagen", "motivo": "ni archivo ni imagen
  generada: sale como tarjeta de marca"`. `incompleto` sale `false` porque, formalmente, la
  resolución terminó sin excepción — el fallo está dentro, no en el proceso.
- **Reproducido.** `03_produccion/pipeline/visual.py` (función `caras()`) construye el detector
  de caras con `cv2.CascadeClassifier(...)`. `.github/workflows/visuales.yml` instala
  `opencv-python-headless` **sin fijar versión**. Probado en un venv limpio con Python 3.12
  (la misma versión del workflow): `pip install opencv-python-headless` de hoy trae la
  **5.0.0**, y en esa versión `hasattr(cv2, 'CascadeClassifier')` es `False` — OpenCV 5.0
  eliminó el detector Haar clásico de la build headless (lo sustituye por
  `cv2.FaceDetectorYN`, basado en un modelo ONNX que hay que descargar aparte). Con la 4.13.0
  (la que había hasta hace poco) el atributo existe. El código de `caras()` solo capturaba
  `ImportError`; el `AttributeError` de construir el detector subía sin que nada lo parara,
  `mirar()` lo convertía en «no se pudo mirar» y el candidato se descartaba — TODOS los
  candidatos, siempre, desde el primer Short con C50.
- **No es un fallo que un espectador note**: el propio diseño de C50 hace que, si el modo
  archivo falla, el Short salga como llevaba haciéndolo desde agosto («una imagen nunca cuesta
  un vídeo», `05_calendario/visuales/LEEME.md`). Pero la función que el codirector pidió con
  urgencia el 23/09 no se ha activado ni una sola vez en sus dos primeros días.

**Arreglo aplicado hoy** (el cambio de código de esta sesión; no consume ranura por ser
arreglo de defecto, regla 11.1): en `03_produccion/pipeline/visual.py`, `caras()` ahora
captura también el `AttributeError` al construir `_DETECTORES` y, si ocurre, se queda con una
lista vacía en vez de propagar la excepción — el candidato se sigue mirando y usando, solo que
sin información de dónde hay caras (`encuadre()` ya centra por defecto cuando no la tiene).
Verificado contra `opencv-python-headless==5.0.0` en el mismo venv de la reproducción: ya no
lanza excepción y devuelve `[]` limpiamente. **Efecto práctico:** no se nota hoy —
`MDS-024` y `MDS-025` ya tienen `video_id` en el registro y `resolver_guion()` no vuelve a
tocar episodios publicados — pero en cuanto exista `MDS-026` (la planificación de esta noche)
y el workflow «Visuales (C50)» corra sobre él, debería volver a encontrar vídeo e imagen de
verdad en vez de tarjeta de marca en blanco.

**Arreglo de raíz, que no es mío:** fijar la versión de `opencv-python-headless` en
`.github/workflows/visuales.yml` (línea 88), por ejemplo `opencv-python-headless<5`, para no
quedar a merced de la próxima subida mayor de la librería. No toco ese fichero (regla 4/5: no
se mete nada de `.github/workflows/` en el paquete ni se edita). Lo dejo dicho aquí y en
`revisiones/MDS-024.md` y `revisiones/MDS-025.md` para que quede constancia doble.

**Segundo hallazgo, solo en `MDS-024` (ya publicado):** `ficha.json` →
`arranque.fragmento_antes_de_la_narracion: true`, con `duracion_fragmento_s: 0.794` entre los
segundos ~0,88 y ~1,67 del audio de la escena 1 — una sílaba suelta que el sintetizador metió
antes de empezar a hablar, el mismo tipo de defecto que se vio en `MDS-021` el 21/09. No hay
corrección de guion que aplicar (es un artefacto de síntesis de voz, no un error de texto), y
el vídeo lleva público desde ayer a las 17:00 UTC sin que ninguna revisión lo haya visto antes
(no hay bitácora ni estado de los días 23-24 en el repositorio). Lo dejo en `revisiones/MDS-024.md`
y como parte de la incidencia de hoy para que el codirector decida si lo escucha y si merece
sustituir el audio a mano.

Por rememder la regla del 23/09 (dirección): **`MDS-023`, `MDS-024` y `MDS-025` son de la
dirección — no se editan los guiones, ni con la excepción de las 48 horas**, aunque los dos
hallazgos de arriba tienen que ver con el pipeline y no con el texto, así que la regla no
llegaba a aplicar de todos modos: no he tocado ningún guion.

## Paso 4 — código y encargos

**El cambio de código de la sesión es el de arriba** (arreglo de `caras()` en `visual.py`).
No entro en ningún encargo nuevo de la cola.

**Nota para quien mantenga `00_estrategia/tareas/revision-diaria.md`:** varios de los encargos
numerados de la cola del paso 4 (fechados en su mayoría del 7 al 14 de septiembre) **ya están
hechos en el código actual**, aunque el propio fichero de la tarea los sigue listando como
pendientes:

- Encargo 1 (P3+P4, el campo `icono` y la escena 1, C19+C16): hecho — `escena.html` tiene
  `dibujarIcono()`/`iconoHTML()` desde el 07/09, y `MDS-024` escena 1 ya usa `"icono":
  "i-bocadillos"` (visto en el fotograma, dos bocadillos de diálogo dibujándose).
- Encargo 2 (P6, tres tamaños): hecho, comentarios «P6 · tres tamaños» del 09/09 en `escena.html`.
- Encargo 5 (mediana de C26 en `metricas.py`): hecho — `mediana_48h_ultimos_shorts()` existe y
  `metricas.json.control_c26` ya trae `mediana_vistas_48h: 11.0` sobre 20 vídeos.
- Encargo 6, parte de P1 (profundidad): hecho, comentario «P1 · profundidad (16/09/2026)» en
  `escena.html`, con `#reticula` y `#vineta`.
- Encargo 11 (C31, que el estado de YouTube entre en el repositorio a diario): hecho y en
  marcha — `.github/workflows/sincroniza_registro.yml` existe y corre a diario (los commits
  «registro: sincronizado con YouTube» del 19 al 25/09 lo confirman).
- La caché de voz (encargo A de C27) y el `voz_adelantada.yml` (encargo B) también están:
  `origen_voz` en `ficha.json` ya distingue «caché» de «llamada», y
  `.github/workflows/voz_adelantada.yml` existe.

No he auditado el resto de la cola (P2, P5, P7, P8, P9 créditos, el caso abierto de `lista` +
personaje, el CSV de Studio) por tiempo — la prioridad de hoy era el hallazgo de C50. Pero
dado el patrón, sugiero que la próxima sesión (o la dirección, al escribir la próxima versión
del plan) compruebe el estado real del código antes de dar por pendiente algo de esta lista:
se está reseñando trabajo ya terminado.

No he tocado `01_bibliografia/` hoy: ninguna ficha nueva que verificar y el tiempo de la sesión
se fue al hallazgo de arriba.

## Paso 5 — resumen

- **Entrega pendiente detectada**: no hay commit de la planificación semanal de esta semana;
  confirmado en directo por el codirector (retraso administrativo, la hará después de esta
  entrega). No he tocado nada de `05_calendario/` salvo lo mío.
- **Hallazgo principal (INCIDENCIA)**: C50 (imagen/vídeo detrás del texto) ha fallado el 100%
  de las veces desde que entró en producción (`MDS-024` y `MDS-025`, las dos únicas), por un
  `AttributeError` de OpenCV 5.0 no capturado en `caras()`. Reproducido, diagnosticado y
  arreglado en `03_produccion/pipeline/visual.py`. Queda pendiente, y no es mío, fijar la
  versión de `opencv-python-headless` en `.github/workflows/visuales.yml`.
- **Hallazgo secundario**: `MDS-024`, ya publicado, lleva una sílaba suelta antes de la
  narración de la escena 1 (0,79s hacia el segundo 0,9-1,7). Sin corrección posible por mi
  parte (no es texto); documentado para que se decida si se sustituye el audio a mano.
- **Escrito en `revisiones/`**: `MDS-024.md`, `MDS-025.md`.
- **Ficheros tocados**: `03_produccion/pipeline/visual.py` (el arreglo),
  `05_calendario/bitacora/2026-09-25-revision.md` (este),
  `05_calendario/estado/2026-09-25.md`, `05_calendario/revisiones/MDS-024.md`,
  `05_calendario/revisiones/MDS-025.md`.
- **No tocado, aunque se descubrió algo**: `.github/workflows/visuales.yml` (no es mío, no se
  mete en el paquete), ningún guion de `05_calendario/guiones/` (MDS-023/024/025 son de la
  dirección), `parrilla.json`, `registro_publicaciones.json`, `05_calendario/qa/`.
