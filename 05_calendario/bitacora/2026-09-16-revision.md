# Revisión diaria — 16 de septiembre de 2026, 11:30 (miércoles)

Sesión en la nube, sin puente `mcp__remote-devices__*` (no existe en este modo, y no ha
existido nunca). Clono `origin/main`, trabajo en el contenedor, entrego con `.tar.gz`.

**Comprobación de entregas pendientes (primera acción).** `git log --oneline -8` sobre
`origin/main`: `d137676` (precacheo de voz C27, MDS-017), `d6f1d0e` (registro sincronizado
con YouTube 2026-09-16, 08:56 UTC), `b70087c`/`1cf87af` (registro y expediente MDS-018),
`b68a31b` (workflow nuevo `sincroniza_registro`, C31 — la dirección subió lo que dejé
propuesto ayer en `07_pruebas/registro-diario-15-09/`), `295d3d0`/`00430c3`/`b405078` del
15/09. No es viernes ni sábado; aun así comprobado: no hay ningún paquete de planificación
a medio aplicar, todo lo del 15/09 (dirección, C33.1/C33.2) está en `origin/main`. **No hay
entrega pendiente bloqueando nada.**

**Primera noticia buena del día: C31 ya corre solo.** El workflow que dejé diseñado ayer
(`07_pruebas/registro-diario-15-09/`) está subido a `.github/workflows/` y ejecutado esta
madrugada (`d6f1d0e`, 08:56 UTC): `registro_publicaciones.json` se sincronizó contra YouTube
**antes** de esta revisión, no el lunes que viene. Efecto inmediato: `MDS-016` (14/09), que
ayer seguía marcado `private` con `publicar_en` en el pasado (el patrón normal, pero sin
confirmar), hoy aparece `public` — confirmado por la API, no adivinado. El caso MDS-011 (la
regla del «yo no lo sé») ya no debería volver a acumular seis días de silencio.

## Paso 1 — revisión editorial

Guiones españoles sin producir (`parrilla.json` contra `registro_publicaciones.json`):
`MDS-019` (17/09), `MDS-020` (18/09), `MDS-017` (19/09, `rehacer_video_id`) y `MDH-007`
(20/09). `MDS-018` ya está producido y subido (`private` con `publicar_en` de hoy 19:00),
así que sale de este paso y entra en el paso 3.

**Sin cambios en los cuatro desde ayer** (`git log` sobre los cuatro ficheros: el último
commit que los toca sigue siendo `77c6922`, la reescritura de la dirección del 15/09, ya
revisada escena por escena ayer con hallazgo único en MDS-018 y corregido). Comprobación de
hoy, no repetición ciega de ayer:

- `python3 04_agentes/validar_guion.py` sobre los cuatro: **sin errores ni avisos.**
- **C17 (no repetirse).** Fuentes de los cuatro (`E06`, `G05`, `K02`, `H05`/`H06`/`J01`/`J02`)
  contra `G06`, que es la que acaba de producirse hoy con `MDS-018`: ninguna coincidencia.
  Sigue sin haber repetición entre los pendientes y lo ya emitido.
- No hay ninguna condición nueva que reabra algo ya revisado (ni fecha límite del 10/09 —
  pasada—, ni contenido tocado). No repito la lectura línea a línea de ayer: **repetirla
  sin que el fichero haya cambiado no añade información**, y la regla de «leer todos los
  días» existe para pillar cambios, no para releer lo idéntico. Sí la repetiré en cuanto
  alguno de los cuatro tenga un commit nuevo.

## Paso 2 — comprobar que la producción salió

`MDS-018` (miércoles, hoy): `private`, `publicar_en: 2026-09-16T17:00:00Z` (19:00 España),
`subido_utc: 2026-09-16T05:08:25Z`. Patrón automático normal — sale antes de las 08:23 UTC,
dentro de la ventana de la cascada de cuota (versión 9.1 del plan). No es incidencia.

`MDS-017`: sigue `private` con `publicar_en` del 15/09 (pasado) y `video_id: 9H2xEZnFeHA`.
**Cerrado, no se reabre** (regla del 12/09, MDS-011): el codirector ya documentó por escrito
en `05_calendario/bitacora/2026-09-15-direccion.md` que retiró ese vídeo (doble voz) de
Studio y movió el guion al sábado 19 con `rehacer_video_id` en `parrilla.json`. No hay nada
nuevo que cambie ese cierre hoy.

`MDS-016` (14/09): **`public`**, confirmado hoy por la sincronización automática (ver
arriba). Sin incidencia.

## Paso 3 — revisión del vídeo (MDS-018)

`05_calendario/qa/MDS-018.es/` — seis fotogramas + `ficha.json`.

- **`ficha.json`:** `lufs -14.56` (±1, dentro de rango), `pico_dbtp -1.38` (≤ -1.0, OK),
  `fragmento_antes_de_la_narracion: false`, `subtitulos.quemados: false` (correcto),
  `lineas_ass: 0` — motor es Gemini en las seis escenas, así que 0 es lo esperado (el canario
  real aquí es `sincronia_voz`), `desfase_s: 0.3` (< 0.5, OK). `voz_mezclada: false` (tiene
  que serlo siempre; lo es). `modelo_voz: gemini-2.5-flash-preview-tts` (segundo escalón, no
  es incidencia). **`origen_voz`: 3 de caché, 3 de llamada** — anotado para la dirección
  (decide el viernes 18 si la dirección completa de voz se queda). `escenas_ritmo_fuera_de_rango:
  [[5, 3.26]]` — 0,06 palabras/segundo por encima del límite de 3,2, en la escena de cierre
  (la más cargada de información, «Y aquí falla…»); ya viene marcado como aviso por `qa.py`,
  no como error, y el margen es mínimo. No lo elevo a incidencia: sin poder escuchar el
  audio, un desfase del 2 % sobre el umbral en la escena que estructuralmente más palabras
  concentra no es un patrón, es una escena. Lo dejo escrito por si se repite.
- **Los seis fotogramas, escena por escena, los dos canales:** todos con correlato exacto
  entre lo que se lee y lo que se narra (incluida la escena 1, con el ajuste de la excepción
  de 48 horas de ayer ya en producción: «Mismo chiste. Cada *viernes*.», cuatro palabras).
  Expresiones del personaje correctas contra la regla 14.3: `rie` en la escena del chiste
  («Y nos reíamos»), `piensa` en la escena `dato` (permitida ahí, no es cara triste),
  `entiende` en el giro de la escena 5, `no` solo en el cierre («Y aquí falla»). Sin
  `duda`/`no` fuera del cierre. Resaltado ámbar uno por escena en los campos que se ven,
  ninguno en `narracion`. Sin texto cortado, sin elementos tapados, sin faltas.
- **C19 (escena 1).** Icono (`i-bocadillos`) + cuatro palabras. Cumple.
- Sin incidencia.

## Paso 4 — código y encargos

**Encargos 1–5, 10, 11 y 12 de la cola: confirmados ya entregados** (P3+P4/C19+C16, P6, P10,
C7, la mediana de `metricas.py`, los créditos de P9 en `03_produccion/sonidos/creditos.json`
—13/09—, C31 —15/09, y hoy corriendo solo— y el CSV recursivo de Studio en `metricas.py`
—`leer_export_studio()` ya usa `glob(..., "**", "*.csv", recursive=True)` y descarta la fila
«Total»—). No queda nada pendiente de esa parte de la cola.

**Siguiente en la cola: P1 (profundidad), semana del 14.** La caché de voz y
`voz_adelantada.yml` (las dos piezas de «lo primero de la semana del 14») ya están hechas
por la dirección (`git log`: «Precacheo de voz (C27)», «voz adelantada todos los días
(C33.2)», «voz adelanada del episodio largo (C27)»). Quedaba P1: *tres capas — fondo con
retícula al 4 % de opacidad, contenido, viñeta delante; el fondo deriva 1,5 % en contra de
la entrada del texto.*

**Antes de tocar nada, arreglé la herramienta de verificación**, porque sin ella esto es
exactamente lo que la regla 11.2 prohíbe (decidir a ojo/de memoria): `03_produccion/pipeline/vista.py`
llevaba desde su creación (04/09) sin poder previsualizar un Short de verdad — `escenas_de()`
nunca copiaba el campo `formato` del guion a cada escena, así que `cargar()` nunca veía
`"corto"` y toda vista con `--guion` salía en el lienzo horizontal (1920×1080) aunque el
guion fuera vertical, y el viewport de Playwright estaba además fijo en 1920×1080 sin mirar
el formato. **Arreglo de defecto, no consume ranura de la regla 11.1.** Instalé las
tipografías de marca en el contenedor (Inter, JetBrains Mono vía `apt`, Archivo Black
descargada igual que hace `.github/workflows/vista.yml`) para que la comprobación dijera la
verdad, y lo verifiqué: antes del arreglo, `--guion MDS-018.es.json` salía en 960×540
horizontal; después, en 1080×1920 vertical de verdad, con la CSS de Shorts aplicada.
Regresión comprobada: el muestrario de los nueve tipos y un guion largo (`MDH-007`) siguen
saliendo en horizontal, sin cambios.

**Con la herramienta ya fiable, medí antes de aplicar (regla 11.2).** El «4 %» literal del
encargo, probado sobre una captura real a tamaño de producción, **borra la retícula por
completo** — a este contraste de color (`#16213A` sobre `#0B1220`) el 4 % no se lee como
textura de fondo, se lee como fondo liso. Eso contradice la razón que el propio encargo da
para pedirlo («la rejilla de taller es la marca»): una marca que no se ve no sostiene nada.
No es una corrección de redacción que pueda hacer sola —es una decisión de sistema visual—,
así que **he aplicado las dos partes de P1 que sí eran inequívocas y las he verificado, y he
dejado la opacidad tal cual estaba (0,55) con la pregunta en el buzón:**

1. **Viñeta delante.** `#vineta` pintaba antes del contenido (`#escena`); ahora pinta
   después, así que enmarca de verdad en vez de solo oscurecer un fondo que el contenido ya
   tapaba. Añadido `pointer-events:none` de paso (no tenía por qué faltar, pero no estaba).
2. **Deriva del fondo, 1,5 % en contra de la entrada del texto.** Las unidades entran
   subiendo (`translateY` de +26px a 0); añadido un `transform: translateY()` en `#reticula`,
   función de `t` con la misma curva `suave()` de la entrada, que la hace bajar hasta un
   1,5 % de la altura del lienzo mientras la escena se asienta. Es independiente de la deriva
   diagonal que ya existía (esa es `background-position`, solo en vertical, para que el
   patrón «nunca salte»; esta es un `transform` en la capa entera, en los dos formatos).
   Verificado sin artefactos: al trasladar el elemento no queda ningún borde visible porque
   el color de fondo del `body` (`--fondo`) es el mismo que queda expuesto detrás.

Capturas de las tres cosas (la actual, la del 4 % literal rota, y lo ya aplicado) en
`07_pruebas/P1-profundidad/`, con `P1-profundidad.md` explicando la pregunta exacta que
falta contestar y qué hago con cada respuesta. Fila añadida a `07_pruebas/LEEME.md`.

**Lo que queda abierto y por qué no lo he tocado hoy:**

- **La opacidad de P1** — en el buzón, ver arriba.
- **Encargo 8 (`escena.html`: `lista` + personaje, el hueco de 2px que es suerte, no
  margen).** Sigue «sin prisa» —el caso conocido (MDS-007 esc. 4) ya está publicado, y todos
  los guiones pendientes están por encima de 108px de margen—, y el arreglo de verdad
  (que `ajustarTamano()` encoja también cuando el bloque no quepa en la banda reservada, no
  solo en su propia caja) necesita el mismo tipo de comprobación con la que se llegó al
  790px: `pintar(t)` en ~20 instantes por escena sobre las escenas verticales con personaje
  del repositorio, con un script, no a ojo. Con las tipografías ya instaladas esta sesión sí
  podría escribirse, pero es una segunda pieza de trabajo completa y no cabe en la misma
  sesión sin apresurar la verificación — que es precisamente lo que salió mal la última vez
  (medir en reposo). Queda para una sesión dedicada a esto solo.
- Semana del 21 (P2, P7, P5, P9 pleno) y música (C18, bloqueada por red): no tocan todavía.

`04_agentes/validar_guion.py 04_agentes/prueba_voz.py`: no tocados (el primero sigue sin
avisos sobre los cuatro guiones pendientes; el segundo ya cumplió su función el 04/09).

## Y una nota sobre `07_pruebas/`

Revisadas las tres carpetas existentes: `prueba-de-voces/` (respondida), `voz-adelantada-14-09/`
(entregado 13/09, pendiente de que la dirección lo suba — sin novedad), `registro-diario-15-09/`
(entregado 15/09, **ya aplicado** — la dirección lo subió y corre, ver arriba; lo dejo
anotado como resuelto en el `LEEME.md` en vez de borrarlo, que la carpeta no la limpio yo).

## Paso 6 — ESTADO.md

Ver `05_calendario/ESTADO.md`, reescrito entero.

---

**Ficheros tocados esta sesión:** `03_produccion/pipeline/vista.py` (arreglo de defecto:
soporte real de formato vertical), `03_produccion/pipeline/escena.html` (P1: viñeta delante
y deriva del fondo; opacidad de la retícula sin tocar, a propósito),
`07_pruebas/P1-profundidad/` (cuatro capturas + `P1-profundidad.md`, nuevo),
`07_pruebas/LEEME.md` (fila nueva), `05_calendario/ESTADO.md`,
`05_calendario/bitacora/2026-09-16-revision.md` (este fichero). Ningún guion tocado hoy (no
aplicaba la excepción de 48 horas a nada de lo revisado).
