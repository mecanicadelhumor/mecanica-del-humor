# Revisión diaria — 24 de septiembre de 2026, 09:30 UTC (jueves)

Sesión en la nube, sin puente `mcp__remote-devices__*` (no existe en este modo). No
se ha pedido ninguna autorización, permiso ni confirmación en toda la sesión. Clono
`origin/main`, trabajo en el contenedor, entrego con `.tar.gz` por `SendUserFile`.

## Antes de nada: `08_comunicacion/`

Leído `novedades.md` (del codirector, sin tocar): dos entradas, ninguna es tarea de
revisión de calidad — la pregunta sobre @Maestro_Seductor (labios sincronizados, cara
ficticia) es para quien corresponda, no para esta tarea, y el aviso de «cambios
profundos visuales en marcha» ya se ha materializado hoy en C50 (ver más abajo). No
la contesto ni la investigo, como los días anteriores.

Leídos los ficheros de los últimos siete días: `2026-09-19-revision-diaria.md` (mía,
pendiente antiguo del resaltado coral, sigue sin sintaxis, no toco hoy — es cambio de
presentación y sigue bloqueado hasta el 28 salvo lo explícitamente autorizado de
C50), `2026-09-21-direccion.md`, `2026-09-21-metricas_1.md`, `2026-09-22-revision-diaria.md`
(mía, sobre `sincroniza_registro.yml` — el codirector contestó que va bien por
Actions, cerrado), `2026-09-23-direccion.md` y `2026-09-23-direccion-c50.md` (las dos
sesiones extraordinarias de ayer: C48 reescribe los tres guiones de esta semana y la
lectura en frío entra como paso 1; C50 entra en producción esa misma tarde).

**Nada del codirector en `novedades.md` señala un defecto de guion sin publicar**, así
que no hay primer hallazgo por esa vía hoy (regla del 23/09).

## Comprobación de entregas pendientes (primera acción)

`git log --oneline -8` sobre `origin/main`:

```
90592a2 Registro y expediente de calidad: MDS-024
0c0b2ed registro: sincronizado con YouTube 2026-09-23
82c50d1 Registro y expediente de calidad: MDS-024
cf1208e Visuales (C50): manifiestos y hojas de contactos
373541a C50 en produccion: visuales.yml y el paso del material visual
b290932 Registro y expediente de calidad: MDS-023
1a9f216 pruebas visuales y feedback
29642ba Prueba visual (C50): hojas de contactos
```

Hoy es jueves: no aplica la comprobación de viernes/sábado sobre la planificación
(esa es esta noche, a las 22:00, con C48 ya escrito). El último commit es el propio
bot subiendo el expediente de calidad de `MDS-024` (el vídeo de hoy). `MDS-023` y
`MDS-024` de la dirección (C48) ya están en `origin/main` desde ayer, y el código y
los prompts de C48/C50 también. **Nada pendiente de aplicar.** Re-`fetch`eado otra
vez a las 09:35 UTC, justo antes de escribir este fichero: mismo `HEAD` (`90592a2`).

## Paso 1 — revisión editorial

### Lo primero: la lectura en frío del Short de mañana (C48)

Mañana (viernes 25) se produce `MDS-025» («El experimento», humor y memoria). Copiadas
solo las seis `narracion` + texto de pantalla, en orden y numeradas:

```
1. «A diez personas de unos setenta años les ponen vídeos graciosos. A otras diez las
   sientan veinte minutos sin hacer nada.» [diez personas: Vídeos graciosos / otras
   diez: Sin hacer nada]
2. «Sin móvil, sin leer y sin dormirse.» [Sin móvil, sin leer]
3. «Yo no aguanto ni cinco.» [Ni cinco minutos]
4. «Después, en un test de memoria, las que vieron los vídeos mejoraron un cuarenta y
   tres coma seis por ciento.» [43,6% — las que vieron los vídeos]
5. «Los autores apuntan al estrés: en ellas bajó el cortisol, la hormona del estrés.»
   [Les bajó el cortisol]
6. «Lo malo es que eran diez por grupo. Y las que solo estuvieron sentadas, sin
   móvil, mejoraron casi la mitad.» [Eran diez por grupo / Humor y memoria en
   mayores · Bains y otros]
```

Encargo literal de «La lectura en frío» (`guionista_corto.md`) a un subagente sin
contexto (herramienta Agent, tipo general-purpose). Respuesta completa:

1. **¿De qué va el vídeo?** «Un experimento con veinte personas mayores muestra que
   ver vídeos graciosos veinte minutos mejora más la memoria que quedarse sentado sin
   hacer nada.»
2. **¿Qué pregunta responde, y qué responde?** «¿Ayuda reírse a la memoria en
   personas mayores? Responde que sí: el grupo que vio vídeos graciosos mejoró un
   43,6% en un test de memoria, más que el grupo que solo estuvo sentado.»
3. **Frases que no entendió:** «"Y las que solo estuvieron sentadas, sin móvil,
   mejoraron casi la mitad." — no sé si "la mitad" se refiere a la mitad del 43,6% o
   a otra cifra que nunca se da.»
4. **Qué frase quitaría:** «Yo no aguanto ni cinco.»
5. **Dónde se habría ido:** «En la escena 6, porque al decir "eran diez por grupo" de
   repente me hace desconfiar de todo lo anterior [...] y encima remata con un dato a
   medias ("casi la mitad") que no puedo comprobar.»

**Es `INCIDENCIA`** (primera línea de `estado/2026-09-24.md`): el lector copió una
frase de la que no supo el referente (punto 3). `de_que_va` y `pregunta_y_respuesta`
sí coinciden con la `historia` del guion (mismo estudio, mismo resultado, misma
salvedad del tamaño de muestra), así que el criterio (b) pasa; el que no pasa es (a).

**No lo arreglo yo.** `MDS-025` es uno de los tres guiones que reescribió la dirección
el 23/09 (C48): la nota de esa sesión dice explícitamente no tocarlos, ni con la
excepción de las 48 horas, aunque encuentre algo — se escribe en `revisiones/` y en
`estado/`. Detalle y una propuesta de corrección (antes/después) en
`05_calendario/revisiones/MDS-025.md`. La pregunta 4 (frase sobrante) no la trato
como hallazgo: es el chiste de apertura, del que depende el `puente` de `historia`, y
ya está justificado en `notas_humor` por qué se queda.

`MDH-008` (suspendido, sin fecha, formato largo) sigue fuera de la parrilla; no lo
reviso hoy, sin cambios desde el 18/09.

### La regla de los dos canales, resaltado y expresión — `MDS-024` (hoy, ya producido) y `MDS-025` (mañana, sin producir)

**`MDS-025`:** sin errores del validador. Regla de los dos canales escena por escena:
el texto de cada escena (`et_a`/`et_b`, `texto`, `cifra`+`pie`, `titulo`) tiene
correlato en la `narracion` de esa misma escena (comprobado sustantivo a sustantivo,
sin ningún dato nuevo introducido por el texto que la voz no diga). Personaje: `rie`
en escena 3 tras el chiste de apertura (correcto, reacciona después del remate,
`pausa_despues_s: 0.6` no es la pausa del remate principal pero la escena 3 es el
propio chiste); `piensa` en la escena 4 (dato, exposición pura — correcto, no es
tarjeta triste); `entiende` en la escena 5 (exposición del mecanismo — correcto); `no`
en el cierre (la narración dice «lo malo es» — correcto). Resaltado: un ámbar por
escena (graciosos/móvil/cinco/43,6%/cortisol/diez), sin cian (no hace falta término
de oficio) ni coral (sigue sin sintaxis, pendiente de siempre). Nada en `narracion`.
C17: fuente G04, y `notas_humor` confirma que no se ha usado como fuente central en
las últimas seis semanas (G06 sostuvo `MDS-018`, con otra pregunta). Sin hallazgos de
guion propios (el hallazgo de hoy sobre `MDS-025` es solo el de la lectura en frío,
arriba).

**`MDS-024`** (hoy, ya producido y revisado también en el paso 3): repasado igual,
escena por escena — sin violaciones de la regla de los dos canales, expresión del
personaje correcta en las cuatro escenas que lo llevan. Único apunte, no bloqueante:
la escena 1 (icono `i-bocadillos`) lleva cinco palabras de texto («—Vengo de Lisboa.
—Qué bien.»), por encima de las «cuatro palabras o menos» de C19 para la escena 1.
Anotado en `05_calendario/revisiones/MDS-024.md` como nota, no como hallazgo — no lo
edito (es de la dirección) y no es el tipo de cosa que note un espectador.

`python3 04_agentes/validar_guion.py 05_calendario/guiones/MDS-024.es.json
05_calendario/guiones/MDS-025.es.json`: sin errores graves en ninguno de los dos (el
único aviso, en `MDS-024`, es sobre la duración frente a la referencia de la serie —
53 s contra 40 s de referencia—, ya es AVISO desde C48, no ERROR, y ya está resuelto:
la lectura en frío de ayer, hecha por la dirección, no pidió quitar nada).

### La imagen: la hoja de contactos (C50) — y el hallazgo grande del día

Mirada la hoja de `MDS-025.jpg` (mañana): **10 de 11 planos «SIN IMAGEN → marca»**,
solo el plano 4.2 (marcado `marca: true` en el propio guion) es una tarjeta de marca
a propósito. El de pasado mañana (sábado 26) es la segunda emisión de `MDS-023`, que
no lleva `visual` (sale como siempre, según la nota de dirección del 23/09) — no hay
hoja que mirar ahí.

Con todos los planos vacíos, las cinco preguntas de la hoja de contactos no se pueden
aplicar (no hay imagen que juzgue si cuenta la frase, si contradice el texto, etc.).
Antes de escribir «no hay nada que ajustar», comprobé por qué: abrí el manifiesto
(`05_calendario/visuales/MDS-025.json`) y sus `descartes` — 127 candidatos de Pexels y
Pixabay, **todos** descartados con el mismo motivo: `no se pudo mirar: module 'cv2'
has no attribute 'CascadeClassifier'`. El respaldo de imagen generada también falla,
con el mismo error (`errores_ia`). Comprobado lo mismo en `MDS-024.json` (143
descartes, idéntico motivo) — y `MDS-024` es HOY, ya producido: su `ficha.json` trae
`"visual": null` con manifiesto presente en `05_calendario/visuales/`, que es
exactamente la señal de que el modo archivo falló entero (paso 3, punto 5).

Diagnóstico completo, arreglo de código y lo que hace falta para que `MDS-025` no
repita lo mismo esta madrugada: en `08_comunicacion/2026-09-24-revision-diaria.md` y
en la sección de código, abajo. `diagnostico.json` (Pexels ok, Pixabay ok, Cloudflare
con el token de cuenta fallando en 401 — no es la causa de esto, es un problema
aparte, ya visto ayer en la sesión de dirección) no apuntaba a esto; hizo falta leer
`render.py` y los `descartes` del manifiesto para encontrarlo.

No he escrito nada en `05_calendario/visuales/ajustes.json` hoy: no es un problema de
qué busca cada plano (ninguna búsqueda llegó a evaluarse de verdad), es un fallo de
código que ya he corregido — corregirlo ahí habría sido parchear el síntoma.

### La bibliografía

`python3 04_agentes/validar_bibliografia.py`: «77 fichas en el .md · 78 en
semillas.json. Los dos ficheros dicen lo mismo. Regenerar el .md es seguro.» Nada que
corregir; no he abierto ningún DOI porque no he tocado ninguna ficha hoy.

## Paso 2 — comprobar que la producción salió

Registro releído de `origin/main` a las 09:30 UTC y otra vez a las 09:35 UTC (justo
antes de escribir este fichero): mismo `HEAD`, sin cambios entre una lectura y otra.

- **`MDS-024` (hoy):** `estado: private`, `publicar_en: 2026-09-24T17:00:00Z`
  (19:00 España), `subido_utc: 2026-09-24T06:15:45Z`. Patrón automático esperado, no
  incidencia.
- **`MDS-023` (ayer, rehecho por la dirección):** sigue `private` con
  `publicar_en: 2026-09-23T17:00:00Z`, hora ya pasada. Patrón conocido: el registro
  dice el estado del momento de la subida y se corrige por la tarde con
  `sincroniza_registro.yml` (el commit de ayer, `0c0b2ed` a las 13:xx UTC, confirma
  que sigue corriendo). No lo pongo como incidencia — no es la sexta vez seguida
  (es la primera desde el rehecho de ayer) y `publicar_en` está presente y correcto,
  que es lo que distingue esto del fallo real de `MDH-004`.
- **El resto** (`MDH-007`, `MDS-001` a `MDS-022`): todos `public`, sin novedad.
- No hay ningún vídeo esperado por la parrilla que falte del registro.

## Paso 3 — revisión del vídeo (`MDS-024`, hoy)

**`ficha.json`:** `lufs -14.26` (OK), `pico_dbtp -1.37` (OK), `rango_lu 3.9`,
`fragmento_antes_de_la_narracion: true` con `duracion_fragmento_s: 0.794` — **mirado
con atención**: la nota de la propia ficha dice que es el sintetizador metiendo una
sílaba suelta antes de la primera frase; `fragmentos.cuantos: 0` (vacío, como debe
estar) y `sincronia_voz.desfase_s: 0.3` (< 0.5, OK), así que no se oye como un corte
de audio real, solo como el colchón de entrada un poco largo. No lo trato como
hallazgo — no hay fragmento suelto en medio del audio, que es lo que sí sería un
defecto que se oye. `subtitulos.quemados: false` (correcto), `lineas_ass: 0` (esperado
con motor Gemini). `voz_mezclada: false`. `modelo_voz: gemini-3.1-flash-tts-preview`,
sin `edge-tts` en ninguna escena. `origen_voz`: 5 de 6 por caché, 1 de 6 por llamada
en vivo — la caché de voz (C27-A) está funcionando. **`visual: null`** — ver el
hallazgo de C50, arriba: el Short debía llevar vídeo de archivo y no lo lleva; causa
diagnosticada y corregida, no es un fallo nuevo de esta ficha en concreto.

**Los doce `.jpg` de `05_calendario/qa/MDS-024.es/`, mirados uno a uno:** sin texto
cortado, sin elementos tapados, sin faltas de ortografía, nada en las zonas que tapa
la interfaz de YouTube. Fondo: el de siempre (rejilla azul oscuro), no el de archivo
— coherente con `visual: null`. El personaje `piensa` (escena 4) se ve con la cara
redibujada del 07/09 (boca recta, cejas levantadas) y el `no` del cierre con la boca
plana — correcto para «truco de actores, no de científicos».

**Regla de los dos canales sobre el vídeo ya producido** (no solo sobre el guion):
comprobada en los mismos doce fotogramas contra `MDS-024.es.json` — sin
discrepancias entre lo que se lee y lo que se oye en cada escena.

**Sin incidencia de vídeo/guion en `MDS-024`.** La única incidencia de hoy sobre este
Short es la de C50 (ver arriba y comunicación).

## Paso 4 — código y encargos

**Un solo cambio de código hoy, y es un arreglo de defecto — no consume ranura de la
regla 11.1.** `03_produccion/pipeline/visual.py`:

1. `caras()` ahora envuelve la creación de los detectores Haar y la detección en un
   único `try/except Exception` (antes solo cubría `ImportError`), y guarda en
   `_DETECTORES_ROTOS` que el entorno no sirve para no reintentarlo en cada plano de
   la pasada. Cualquier fallo devuelve `[]` (sin caras) en vez de propagar la
   excepción — que es lo que hoy tumbaba `mirar()` para todos los candidatos y el
   respaldo de IA, y dejaba el Short entero en `sin_imagen`.
2. `VERSION` de `1` a `2`, con el comentario que ya estaba en el código («subirla
   obliga a volver a resolver todo lo pendiente»): para que `MDS-024` y `MDS-025`,
   resueltos ayer con el código roto, se vuelvan a resolver enteros en cuanto corra
   «Visuales (C50)», no solo si cambia el guion.

**Probado antes de entregar** (regla 11.2, con el muestrario y no de memoria):
simulé un `cv2` que falla exactamente como en el runner (tiene `cv2.data` pero no
`CascadeClassifier`, así que `cv2.CascadeClassifier(...)` da el mismo
`AttributeError` textual que aparece en los `descartes` de ambos manifiestos) —
`caras()` ya no lanza, devuelve `[]` en la primera llamada y en las siguientes sin
volver a tocar el detector roto. Con `cv2` real (el de este contenedor, que sí
funciona) comprobado que el camino normal no cambia. `python3 -m py_compile` sobre
el fichero, sin errores. No he podido ejecutar `resolver_guion()` de verdad contra
Pexels/Pixabay (sin red ni claves desde aquí), así que lo que queda por confirmar es
que mañana `MDS-025` sí traiga vídeo de archivo — pedido en la comunicación de hoy.

**Nada más de la cola de encargos hoy.** Con C50 recién estrenado y su propio fallo
crítico de hoy, no toco nada de la cola de P6/P2/P7/P5/`escena.html` (`lista`+
personaje) — sigue bloqueada por la ventana sin presentación hasta el 28, y hoy además
no era el día para abrir una segunda vía de cambios de código estando ya la de C50 en
curso. `metricas.py` (la mediana, C26), el glob recursivo del CSV, C31, P10, P9,
música (C18): sin cambios desde la confirmación del 22/09, nada nuevo que revisar hoy.

## Y `07_pruebas/`

Revisado. Nada dirigido a la revisión diaria hoy. Sin novedad desde el 23/09.

## Paso 6 — `05_calendario/estado/2026-09-24.md`

Escrito con `ESTADO: INCIDENCIA` — dos hallazgos, ninguno de los cuales para el
canal: C50 falló su primer día de producción por un `AttributeError` de código,
diagnosticado y corregido (falta el `push` para que se aplique antes de esta noche);
y `MDS-025` (mañana) no pasa del todo la lectura en frío nueva (una frase sin
referente claro en el cierre). `Pendiente del codirector: nada` — la regla dice que
ese campo lleva algo solo si el canal se para sin ello, y ninguno de los dos lo para
(el Short sale igual, con el fondo de siempre si C50 no llega a tiempo); la urgencia
de aplicar el paquete hoy queda dicha en la comunicación y aquí, no en ese campo.

---

**Ficheros tocados esta sesión** (los listo todos por nombre):

- `03_produccion/pipeline/visual.py` (modificado: `caras()` resiliente a un `cv2`
  roto, `VERSION` de 1 a 2)
- `05_calendario/revisiones/MDS-025.md` (nuevo)
- `05_calendario/revisiones/MDS-024.md` (nuevo)
- `05_calendario/bitacora/2026-09-24-revision.md` (este, nuevo)
- `05_calendario/estado/2026-09-24.md` (nuevo)
- `08_comunicacion/2026-09-24-revision-diaria.md` (nuevo)

**No tocados, a propósito:** ningún guion (`MDS-023`, `MDS-024` y `MDS-025` son de la
dirección), `parrilla.json`, `CALENDARIO.md`, `demanda.json`,
`registro_publicaciones.json`, `05_calendario/qa/`, `05_calendario/visuales/ajustes.json`
(no había nada editorial que ajustar: el problema era de código), los manifiestos y
hojas de `05_calendario/visuales/*.json` y `*.jpg` (los escribe el bot), ningún
fichero de `.github/workflows/`. `08_comunicacion/novedades.md` leído, no escrito.
`00_estrategia/tareas/revision-diaria.md` leído como el prompt que es, no tocado.

**Resumen para quien lea solo esto:** no había entregas pendientes. Hallazgo grande
del día: **C50 (la imagen detrás del texto) falló su primer día de producción
entero** —hoy, `MDS-024`, y mañana le iba a pasar lo mismo a `MDS-025`— por un
`AttributeError` en la detección de caras (`module 'cv2' has no attribute
'CascadeClassifier'`, no algo de las claves de Pexels/Pixabay) que tumbaba cada
candidato de imagen uno a uno; corregido en `visual.py` (código propio, arreglo de
defecto, no consume la ranura de la regla 11.1) y probado contra un `cv2` simulado
que falla igual. **Urge aplicar el paquete hoy**: en cuanto se suba, el propio `push`
relanza «Visuales (C50)» y `MDS-025` debería salir esta madrugada con vídeo de
archivo de verdad. Segundo hallazgo: `MDS-025` no pasa del todo la lectura en frío —
una frase del cierre sin referente claro («casi la mitad» ¿de qué?) —, documentado
con una propuesta de corrección en `revisiones/MDS-025.md`, sin tocarlo (es de la
dirección). `MDS-024` (hoy), revisado completo en guion, dos canales y vídeo: sin
hallazgos de contenido, solo una nota menor de recuento de palabras en la escena 1.
