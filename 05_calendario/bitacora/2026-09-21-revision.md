# Revisión diaria — 21 de septiembre de 2026, 11:30 (lunes)

Sesión en la nube, sin puente `mcp__remote-devices__*` (no existe en este modo, y así lo
dicen todas las bitácoras anteriores). No se ha pedido ninguna autorización, permiso ni
confirmación en toda la sesión. Clono `origin/main`, trabajo en el contenedor, entrego con
`.tar.gz` por `SendUserFile`.

## Antes de nada: `08_comunicacion/`

Leídos `08_comunicacion/novedades.md` (del codirector, sin tocar) y los dos ficheros con fecha
de los últimos siete días:

- **`2026-09-19-revision-diaria.md`**: nota de la revisión del 19/09 sobre la sincronización de
  bibliografía (C39) y un punto sin resolver — `escena.html` no tiene sintaxis para el
  resaltado «coral» que describe esta misma tarea. **Sigue sin resolver hoy** (no lo toco: es
  un cambio de presentación y esta semana no se tocan — ver más abajo). Lo dejo anotado para
  quien retome la semana del 28.
- **`2026-09-21-direccion.md`**: sesión de dirección de esta mañana, con el codirector delante.
  Cinco decisiones que me afectan directamente y que sigo hoy: (1) formato largo **suspendido**
  (C42) — nada de sábado ni domingo, `MDH-008` fuera de `parrilla.json` y sin tocar; (2) el
  estado del canal se escribe en `05_calendario/estado/`, ya lo hacía desde ayer; (3) releer
  `origin/main` antes de decir que falta un vídeo — hecho, ver paso 2; (4) **esta semana no se
  toca voz, guion ni presentación** (regla 11.1, primera medida limpia de C38, 21-25/09) — por
  eso hoy no toco `escena.html` aunque tuviera algo que arreglar ahí, y por eso P2/P7/P5 de la
  cola antigua **no los empiezo**, aunque el fichero de tareas más viejo dijera que tocaban esta
  semana: manda la nota de hoy, no la cola de la versión 6; (5) encargo nuevo, C47: lectura
  ligera diaria de visualizaciones. Ver paso 4.
  - `novedades.md` trae hoy una pregunta del codirector sobre un vídeo de otro canal (labios
    sincronizados, posible cara ficticia). No es una tarea de revisión de calidad ni de código:
    la dejo sin tocar y sin intentar responderla — no es mía, y **no la contesto ni la investigo
    yo**, para no gastar la sesión en algo que no es esta tarea programada.

## Comprobación de entregas pendientes (primera acción)

`git log --oneline -8` sobre `origin/main`:

```
0a4b7b2 Merge branch 'main' of github.com-mecanicadelhumor:mecanicadelhumor/mecanica-del-humor
7047804 cancelar largos y revisión dirección
71c5062 métricas 2026-09-21
7e6e4b7 direccion 21/09: suspension del formato largo (42), estado/ por dias (C45), el canal se lee a sí mismo (C46)
8aa8dc2 incidencia en la voz (episodio del domingo ausente del registro de publicaciones) y revisión del 21 al 26.
48d2260 Registro y expediente de calidad: MDS-021
9255666 métricas 2026-09-21
cadaee1 Precacheo de voz (C27): MDH-008
```

No es viernes ni sábado, así que la regla estricta del «paquete del jueves a medias» no aplica
hoy. Comprobado igual: la planificación de la semana 21-26 (commit `4b275a5`, del jueves 17) y
su reescritura C38 (`8262b7b`, tarde del 18/09) siguen aplicadas; el commit `7047804` de esta
mañana es del propio codirector, después de la sesión de dirección (toca `novedades.md` —
suyo— y responde P1 en `07_pruebas/`, ninguno de los dos mío). **Nada pendiente de aplicar.**
Re-`fetch`eado a las 09:35 UTC inmediatamente antes de escribir el fichero del día: mismo
`HEAD` que al principio de la sesión.

## Paso 1 — revisión editorial

Guiones españoles sin producir, cruzando `parrilla.json` con `registro_publicaciones.json`:
`MDS-022` a `MDS-025` (22 al 25/09) y `MDH-008` (suspendido, sin fecha).

- **Sin cambios de guion desde ayer.** `git log` sobre los cinco confirma que el último commit
  que los toca sigue siendo `8262b7b` (la reescritura C38 del 18/09 por la tarde). La revisión
  de ayer (`2026-09-20-revision.md`) ya los revisó enteros contra ESA versión, escena por
  escena, con la regla de los dos canales, C19, expresión del personaje, resaltado y
  bibliografía. No los repito hoy completos; sí he vuelto a correr el validador sobre los seis
  (los cinco Shorts más `MDH-008`):
  - `python3 04_agentes/validar_guion.py` sobre `MDS-021` a `MDS-025`: **sin errores graves** en
    los cinco.
  - `MDH-008`: los 21 avisos de siempre (planos fijos >10s en un episodio largo, ya conocidos
    desde el 18/09, y los tres avisos de C17 sobre A01/A05/L01, revisados ese mismo día y
    descartados por el mismo razonamiento: `notas_humor` justifica el ángulo distinto en cada
    sitio). Nada nuevo. No lo toco: no está dentro de la excepción de 48 horas —está **fuera de
    la parrilla**— y además esta semana no se tocan guiones.
- **`MDS-021` (hoy):** revisado editorialmente ayer junto con el resto; hoy, además, cruzado
  contra el vídeo ya producido (paso 3) y contra `ficha.json`. Sin cambios de guion desde
  `4b275a5`.
- **Bibliografía:** `python3 04_agentes/validar_bibliografia.py` → «Los dos ficheros dicen lo
  mismo. Regenerar el .md es seguro.» Nada que corregir hoy. No he abierto ningún DOI porque no
  he tocado ninguna ficha.

## Paso 2 — comprobar que la producción salió

Registro releído de `origin/main` a las **09:30 UTC** (antes) y otra vez a las **09:35 UTC**
(justo antes de escribir este fichero y el de `estado/`), sin cambios entre una lectura y otra.

- **`MDS-021` (hoy, serie «El experimento»): correcto.** `estado: private`,
  `publicar_en: 2026-09-21T17:00:00Z`, `subido_utc: 2026-09-21T06:26:54Z`. Es el patrón
  automático esperado, no incidencia.
- **`MDH-007` (domingo):** `public`, sin novedad. La corrección de la dirección al pie de
  `estado/2026-09-20.md` (la incidencia de ayer era falsa, dieciséis minutos) sigue en su sitio;
  no hay nada que reabrir — regla del «asunto cerrado» del paso 2 de la tarea.
- **`MDS-017` a `MDS-020`:** todos `public`. Sin novedad.
- **Nada más que cruzar hoy**: no hay ningún vídeo del día anterior a MDS-021 pendiente de
  confirmar.

## Paso 3 — revisión del vídeo (MDS-021, hoy)

**`python3 04_agentes/validar_guion.py` sobre `MDS-021.es.json`: sin errores.**

**Los seis fotogramas de `05_calendario/qa/MDS-021.es/`, mirados uno a uno, cruzando cada uno
con su escena:**

- Escena 1 (f1, 2.53s): icono (personas, una resaltada) + «El gracioso de casa» — cuatro
  palabras, cumple C19. Sin personaje explícito (campo `personaje` ausente en el guion), que es
  válido: la regla 14.3 no obliga a que el personaje aparezca en todas las escenas.
- Escena 2 (f2, 7.03s): «Material: lo que se me da *fatal*» — un ámbar, nada en narración. Sin
  personaje, correcto (enunciado, sin exigencia de expresión si no aparece).
- Escena 3 (f3, 11.69s): personaje `rie`, coherente con «Veinte años. No se me ha acabado.»
- Escena 4 (f4, 18.41s, `dato`): «31 cómicos contra 400 *estudiantes*» — la narración dice
  «treinta y un cómicos profesionales y a cuatrocientos estudiantes»: correlato exacto, cifra
  con fuente (C05). Personaje `piensa` — correcto para una escena que expone un dato.
- Escena 5 (f5, 27.13s, `comparacion`): «Más humor de todo tipo» vs «Y menos éxito con uno» —
  correlato con «Los cómicos ganaron en los cuatro estilos (...) Y entre ellos, ese va con menos
  éxito». La palabra «uno» se ve más tenue que el resto del texto en este fotograma: **no es un
  fallo**, es el revelado palabra a palabra de C15.1 (`escena.html`, función `pintar()`) a mitad
  de su animación en el instante exacto de la captura — comprobado leyendo el código, no
  supuesto. Personaje `entiende`, correcto.
- Escena 6 (f6, 36.55s, `cierre`): «Y falla aquí: lo rellenan ellos» + «Cómicos profesionales ·
  cuestionarios de humor», personaje `no` — coherente con que aquí es donde la narración dice
  que algo falla («Y aquí falla: (...) el test lo rellena cada uno sobre sí mismo»). Sin texto
  cortado, sin elementos tapados en las zonas que oculta la interfaz de YouTube, sin faltas.

**`ficha.json`:** `lufs -14.87` (OK, dentro de ±1), `pico_dbtp -1.38` (OK, ≤ -1.0),
`fragmento_antes_de_la_narracion: false` (OK), `subtitulos.quemados: false` (correcto),
`lineas_ass: 0` (esperado con motor Gemini, no es el canario aquí). `sincronia_voz.desfase_s:
0.26` (< 0.5, OK). `voz_mezclada: false` (no incidencia). `modelo_voz:
gemini-3.1-flash-tts-preview`, sin `edge-tts` en ninguna escena (no incidencia). `origen_voz`:
**6 de 6 escenas por llamada en vivo, 0 de caché** — lo anoto porque la dirección pidió llevar
la cuenta; no hay ninguna decisión pendiente sobre ello hoy (la del viernes 18 ya pasó, y hoy no
toca tocar voz).

**INCIDENCIA encontrada — audio.** `fragmentos.cuantos: 1`, con una entrada en `0:32.3`, `0.411s`
de duración: «trozo de audio de menos de 0.45s rodeado de silencio», que la propia nota del
campo describe como «casi seguro una sílaba suelta que el sintetizador ha metido» y que
**«debe estar vacío»**. Comprobado contra los cinco Shorts anteriores (`MDS-016` a `MDS-020`):
los cinco tienen `fragmentos.cuantos: 0`. Es la primera vez que aparece en lo que llevamos
revisado. El instante (0:32.3) cae dentro de la escena 6 (cierre), que es la frase más larga del
guion («Y aquí falla: treinta y un cómicos, y el test lo rellena cada uno sobre sí mismo. Yo
saldría graciosísimo.») — compatible con una síntesis que mete una sílaba suelta a mitad de una
frase larga.

No puedo escuchar el audio desde este contenedor (sin credenciales, sin red hacia YouTube ni
hacia el mp4): juzgo por el canario que el propio pipeline dejó escrito para esto, tal como dice
su nota («para que la revisión diaria pueda juzgar el vídeo leyendo el repositorio, sin
credenciales y sin descargar el mp4»). Es exactamente el tipo de defecto que un espectador
notaría (un chasquido o sílaba suelta a mitad de frase) y el vídeo **todavía está en privado**
(`publicar_en` a las 17:00 UTC, quedan más de siete horas): lo dejo como `INCIDENCIA` en
`estado/2026-09-21.md` con el instante exacto, para que el codirector pueda escucharlo y decidir
—dejarlo pasar, esperar a que se rehaga con la caché de voz, o correrlo un día— antes de que
salga público. No toco `parrilla.json` ni reintento ninguna síntesis: no tengo credenciales de
Gemini ni me corresponde forzar nada fuera del workflow.

## Paso 4 — código y encargos

**Un solo cambio de código hoy**, y es el que sigue.

**Lo primero: la regla de esta semana.** La nota de dirección de hoy prohíbe explícitamente
tocar voz, guion o presentación hasta el lunes 28. Eso descarta, sin mirarlos siquiera, los tres
encargos que la cola antigua (`revision-diaria.md`, versión de antes de hoy) tenía como
«lo primero de la semana del 21»: **P2, P7 y P5** son presentación pura (continuidad visual,
composición por serie, cifra que se construye) y **no los empiezo**. Tampoco toco `escena.html`
por el pendiente de resaltado «coral» de la nota del 19/09 (arriba): también sería presentación.

**Encargo elegido: C47, la lectura ligera diaria de métricas** (nuevo hoy, versión 11 del plan,
no es voz/guion/presentación). `metricas.yml` solo corre el lunes, así que un Short puede pasar
seis días a 0 visualizaciones sin que nadie se entere — es justo lo que le ha pasado a
`MDS-017`. Escrito hoy en `04_agentes/metricas.py`:

- `escribir_metricas_diarias(ya, registro, dias=10)`: para los vídeos publicados en los últimos
  diez días, pide solo `views` (por `fila()`, ya existente) y `vistas_48h` (por
  `vistas_primeras_48h()`, ya existente) y escribe `05_calendario/metricas_diarias.json` con
  **solo** `id`, `publicado`, `visualizaciones`, `vistas_48h` — nada de retención, nada de
  tráfico, nada del CSV de Studio, tal como pide la nota de hoy («sin curvas de retención»,
  «megas de churn en git»). El fichero se **sobrescribe entero** cada vez (ventana rodante, no
  serie histórica como `metricas.json`).
- Flag nuevo `--diario` en `main()`, que comprueba el ámbito de analítica (si falta, avisa y
  sale sin tocar el fichero, igual que hace ya el resto del script) y llama a la función
  anterior.
- **Probado con un cliente de YouTube simulado** (tres vídeos de prueba: uno reciente con
  `video_id`, uno de hace 40 días, uno sin `video_id`): confirma que filtra correctamente el
  vídeo viejo y el que no tiene `video_id`, y que `visualizaciones`/`vistas_48h` salen del `dict`
  simulado sin errores. El fichero de prueba que generó ese test (`metricas_diarias.json`, con
  datos falsos) **lo he borrado antes de terminar**: no es mío escribir ese fichero con datos de
  mentira, lo escribe Actions con datos reales.
- `python3 -m py_compile 04_agentes/metricas.py` y `--help`: sin errores, la bandera nueva
  aparece con su ayuda.

**No he podido ejecutarlo de verdad** (sin `YT_REFRESH_TOKEN` ni red hacia la API en este
contenedor), como ya pasa con el resto de `metricas.py`. Igual que con C31 y con
`voz_adelantada.yml`, dejo el diseño del workflow que lo dispara en
`07_pruebas/metricas-diarias-21-09/metricas_diarias.yml` (cron a las 09:15 UTC, antes de la
revisión diaria) con su `LEEME.md`, porque `.github/workflows/` no se escribe en remoto.
Actualizado también `07_pruebas/LEEME.md` con la fila nueva, y cerrada la fila de
`P1-profundidad/`: el codirector respondió hoy en el propio `.md` («Déjala como está y márcalo
como resuelto») — no hace falta ningún cambio de código, la opacidad ya estaba en 0,55.

**Mientras `metricas_diarias.json` no exista** (falta que el codirector suba el workflow), no
puedo aplicar la regla nueva de C47 («INCIDENCIA si un Short pasa de 24h con 0
visualizaciones»): lo dejo dicho para que no se dé por hecho que ya se está comprobando.

**El resto de la cola, revisado y sin tocar:**

- Encargos 11 (C31, estado real de YouTube) y 12 (CSV de Studio recursivo): confirmados hechos
  en sesiones anteriores, sin cambios que hacer.
- Encargo 5 (mediana de C26 en `metricas.py`): **ya estaba hecho** —
  `mediana_48h_ultimos_shorts()` existe y `main()` la llama (línea 583 antes de mis cambios,
  ahora más abajo en el fichero). La cifra de hoy que cita la bitácora de dirección (11,0) sale
  de ahí. No hacía falta tocar nada.
- Encargo 8 (`ajustarTamano()` por banda reservada), P1 (ya cerrado arriba), C34 (banco de
  imágenes): **son presentación** — no los toco esta semana aunque C34 ya esté desbloqueado
  (`banco.json` del codirector, hoy). Quedan para el lunes 28.
- C36 y C44 (una sola toma de voz, ficha de voz fija): **son voz** — la propia sesión de
  dirección de hoy dice que entran el lunes 28, después de C38. No los empiezo.
- Encargo 9 (música, C18): sigue bloqueado por red, no reintentado.

## Y una nota sobre `07_pruebas/`

Revisado. `P1-profundidad/` respondida hoy por el codirector (arriba). El resto, sin novedad
desde el 18/09. Dejo mi propia entrada nueva, `metricas-diarias-21-09/`.

## Paso 6 — `05_calendario/estado/2026-09-21.md`

Escrito con `ESTADO: INCIDENCIA` por el fragmento de audio de `MDS-021` (arriba, paso 3).
`Pendiente del codirector: nada` — el workflow de C47 no es urgente (el canal no se para sin
él) y sigue el mismo patrón que `voz_adelantada.yml` y `sincroniza_registro.yml`: se avisa en
`07_pruebas/`, no en «pendiente».

---

**Ficheros tocados esta sesión** (los listo todos por nombre, como pide la regla):

- `05_calendario/bitacora/2026-09-21-revision.md` (este, nuevo)
- `05_calendario/estado/2026-09-21.md` (nuevo)
- `04_agentes/metricas.py` (modificado: `SALIDA_DIARIA`, `DIAS_VENTANA_DIARIA`,
  `escribir_metricas_diarias()`, flag `--diario`)
- `07_pruebas/metricas-diarias-21-09/metricas_diarias.yml` (nuevo, diseño de workflow)
- `07_pruebas/metricas-diarias-21-09/LEEME.md` (nuevo)
- `07_pruebas/LEEME.md` (modificado: fila nueva de `metricas-diarias-21-09/`, cerrada la de
  `P1-profundidad/`)

**No tocados, a propósito:** ningún guion, `parrilla.json`, `CALENDARIO.md`, `demanda.json`,
`registro_publicaciones.json`, `05_calendario/qa/`, `escena.html`, ni ningún fichero de
`.github/workflows/`. `08_comunicacion/novedades.md` leído, no escrito — es del codirector.
`00_estrategia/tareas/revision-diaria.md` leído como el prompt que es, no tocado — es de la
dirección.

**Resumen para quien lea solo esto:** no había entregas pendientes. Un hallazgo nuevo, de
producción, no de guion: `MDS-021` (hoy) tiene un fragmento de audio de 0,41s hacia el 0:32,
marcado `INCIDENCIA` mientras el vídeo sigue en privado. Ningún guion tocado (nada que lo
pidiera). Un cambio de código: lectura ligera diaria de métricas (C47), con su workflow
diseñado en `07_pruebas/` a falta de que el codirector lo suba.
