# Revisión diaria — 15 de septiembre de 2026, 11:30 (martes)

Sesión en la nube, sin puente `mcp__remote-devices__*` (no existe en este modo, y no ha
existido nunca). Clono `origin/main`, trabajo en el contenedor, entrego con `.tar.gz`.

**Comprobación de entregas pendientes (primera acción).** `git log --oneline -8` sobre
`origin/main`: `295d3d0` (voz adelantada todos los días, C33.2), `00430c3` (instrucciones del
actor), `b405078` (registro y expediente MDS-017), `7a00e7d` (producir guarda la caché de voz),
`77c6922` (dirección 15/09: C33.1, los cuatro guiones reescritos), y tres commits del 14/09. La
sesión de dirección de hoy (`05_calendario/bitacora/2026-09-15-direccion.md`) está aplicada
entera en `origin/main` — los commits llegan hasta las 11:09 UTC de hoy. **No hay entrega
pendiente bloqueando nada.**

## Paso 1 — revisión editorial

Guiones españoles sin producir (`parrilla.json` contra `registro_publicaciones.json`):
`MDS-017` a `MDS-020` y `MDH-007` — los cinco reescritos hoy por la dirección contra las tres
pruebas de cosido. `python3 04_agentes/validar_guion.py` sobre los cinco: sin errores ni avisos.

Releídos escena por escena, con los campos que se ven cruzados contra su propia `narracion`:

- **Los dos canales.** Sin hallazgos en los cinco. Comprobados en concreto: MDS-017 esc. 2
  (`cifra: "50 kHz"` ↔ «cincuenta kilohercios»), MDH-007 esc. 5/6/10/17/18 (los `cifra`
  cualitativos —«Con escala», «Independientes», «La interpretación»— tienen correlato exacto en
  su `narracion`).
- **La cara (regla 14.3).** `duda`/`no` solo donde la narración dice que algo falla o no cuadra.
  En los cuatro Shorts, `no` solo en el cierre («Y aquí falla…», los cuatro lo dicen con esas
  palabras). En `MDH-007`, `duda` en la escena 15 («la profecía se cumple sola», cierre del
  bucle que falla) y en la 36 («el cuestionario lo rellenas tú», el sesgo de juez y parte) — las
  dos narran un fallo, no una presentación. Sin hallazgos.
- **C19 (escena 1 del Short).** Las cuatro llevan icono + cuatro palabras o menos, salvo una:
  **MDS-018 escena 1 tenía `texto: "El mismo chiste. Cada *viernes*."`, cinco palabras** — los
  Shorts ya publicados (`MDS-012` a `MDS-016`) se mueven entre dos y tres. **Corregido**, ver
  abajo.
- **C29 (el SVG de `diagrama`).** `MDH-007` escena 9 trae el paso `*Usarla*` — exactamente el
  caso que describe el comentario de `ricoSVG()` en `escena.html`. Comprobado en el código que
  `diagrama()` ya llama a `ricoSVG()` sobre `p.titulo` y `p.pie` (línea 734-736): el arreglo de
  C29 (12/09) cubre este caso real, no solo el hipotético. No se ha podido ver el fotograma
  (el guion no está producido), pero el código está verificado.
- **C17 (no repetirse).** Fuentes de los cinco pendientes (`E06`, `G06`, `G05`, `K02`, `H05`,
  `H06`, `J01`, `J02`) contra las de los guiones ya producidos: ninguna coincidencia. Sin
  hallazgos.
- **Resaltado.** Ámbar uno por escena en los campos que se ven, ninguno en `narracion`. Sin
  cian en los `pie` de escena `dato` salvo donde la palabra es el nombre técnico de la cosa (no
  se ha dado el caso en los cinco). Sin hallazgos.
- **Cifras sin fuente.** Los `cifra` de `MDH-007` sin `fuente` (escenas 18) son etiquetas
  cualitativas («La interpretación»), no estadísticas — no es el caso que cubre el veto. El
  resto de escenas `dato` con número o dato concreto llevan `fuente`. Sin veto.
- **La pausa antes del remate.** Antes de escribir esto entendí mal el campo: la pausa de
  1,2–1,5 s va **entre el planteamiento y el remate**, que en estos cinco Shorts es la
  transición de la escena 0 a la 1 (1,35 s en los cuatro), no la escena antes del cierre. Los
  cuatro la cumplen. Sin hallazgos — y queda anotado para no repetir la confusión mañana.
- Sin «violación» ni comillas rectas por angulares, sin marcado en la `narracion`, sin chistes
  que no rematan.

**Corrección aplicada (excepción de las 48 horas).** `MDS-018` produce esta noche/mañana (el
cron de las 03:13 del 16/09 falla por cuota, sale a las 10:23 — lo dice
`00_estrategia/PLAN_DE_CAMBIOS.md` versión 9.1, «La cascada de madrugada» — y publica a las
19:00 del 16/09): dentro de las 48 horas. **HE EDITADO `05_calendario/guiones/MDS-018.es.json`,
Y NINGÚN OTRO FICHERO DEL CALENDARIO.** Cambio: escena 1, `texto` de `"El mismo chiste. Cada
*viernes*."` a `"Mismo chiste. Cada *viernes*."` — cuatro palabras, mismo resaltado ámbar, mismo
sentido, coherente con la `narracion` de la escena («Mi profesor de latín contaba un chiste
cada viernes»). `validar_guion.py` sigue sin avisos tras el cambio.

## Paso 2 — comprobar que la producción salió

`MDS-017` no se produce hoy: lo decidió el codirector ayer noche/hoy, documentado entero en
`05_calendario/bitacora/2026-09-15-direccion.md` (la subida de las dos voces, `9H2xEZnFeHA`, se
borró en Studio; el guion se movió al sábado 19 con `rehacer_video_id` en `parrilla.json`).
**Cerrado, no se reabre** (regla del 12/09: el codirector ya lo ha dicho por escrito).

`parrilla.json` todavía trae una entrada para `MDS-017` el 15/09 a las 19:00, sin nota de que se
canceló — parece un cabo suelto de la dirección de hoy. Comprobado en `cola.py`
(`subido_de_verdad()`, línea 198): como el registro tiene `video_id: "9H2xEZnFeHA"` para
`MDS-017.es` y esa entrada del 15/09 **no** lleva `rehacer_video_id`, `cola.py` la da por
«ya subida» y no vuelve a intentar nada esta noche. **No hay riesgo de doble producción.** No
toco `parrilla.json` (no es mío); lo dejo anotado por si a la dirección le sirve limpiarlo el
viernes.

`MDS-016` (14/09) sigue `private` con `publicar_en` en el pasado en el registro — es el patrón
normal del modo automático (Paso 2 del prompt: «lo normal es `private` con `publicar_en`»), no
el caso MDS-011 (que era `private` **sin** `publicar_en`). No es incidencia, no hace falta la
fórmula del «no lo sé»: aquí sí sé lo que dice el registro y es lo esperado. Se corrige solo el
lunes con `metricas.py` (o antes, si sube el workflow del paso 4).

## Paso 3 — revisión del vídeo

**No hay vídeo del día que revisar.** El único intento de hoy (`MDS-017`, doble voz) se retiró
antes de publicarse — nunca llegó a `public` ni a `private` con `publicar_en` válido para hoy, y
no hay una emisión real esta noche hasta `MDS-018` mañana. `05_calendario/qa/MDS-017.es/` tiene
capturas del render abortado, pero revisarlas no aporta nada: el vídeo no existe ya en YouTube y
el motivo de la retirada (la voz, no la imagen) está diagnosticado y resuelto por la dirección.
Mañana sí habrá vídeo real que mirar (`MDS-018`, si la cascada de cuota no lo retrasa más allá
de las 19:00 — el plan dice que no debería).

## Paso 4 — código y encargos

**El cambio de código de la sesión (regla 11.1): C31, el registro deja de mentir seis días.**

`04_agentes/metricas.py`:
- Extraída `corregir_registro(reg, fichas)` (función pura: corrige `estado` contra lo que diga
  YouTube, devuelve cuántas entradas cambió) y `sincronizar_registro(yt, registro)` (la envuelve,
  pide `ficha_youtube()` y escribe el fichero solo si hubo cambios) a partir del bloque que ya
  vivía dentro de `main()`.
- `main()` usa ahora `sincronizar_registro()` en el mismo sitio de siempre — mismo
  comportamiento, sin tocar la lógica.
- Flag nuevo `--solo-registro`: solo corrige `registro_publicaciones.json` contra la Data API
  (no pide `yt-analytics.readonly`, no toca `metricas.json`). Es la pieza que permite un
  workflow diario y ligero sin acoplarlo al ámbito que a este token ya le ha faltado dos veces.
- **Verificado con un cliente de YouTube simulado** (sin red, como siempre desde aquí): tres
  casos con `corregir_registro()` (corrige, ya coincide, sin `video_id` no se toca) y dos con
  `sincronizar_registro()` de punta a punta contra un fichero temporal (escribe y corrige;
  segunda pasada sin cambios no reescribe `_estado_leido_utc`). `python3 -m py_compile` y
  `pyflakes` limpios.

`07_pruebas/registro-diario-15-09/`: diseño de `sincroniza_registro.yml` — dos pasadas de
madrugada (08:50 y 09:10 UTC) que llaman `metricas.py --solo-registro` y hacen commit solo si
algo cambió. Con su `LEEME.md` (qué hace, qué pregunta contesta, qué se ha dejado fuera a
propósito). Añadida la fila a `07_pruebas/LEEME.md`. **No entra en `.github/workflows/` desde
aquí** (regla 11.6): queda para que el codirector lo copie cuando quiera.

No se ha tocado nada de C33 (voz): la dirección lo dejó hoy con doce y luego veintiuna pruebas
en verde, y no hay nada mío que verificar ahí sin gastar cuota real.

## Y una nota para mañana

Hoy se han agotado las dos cuotas diarias del modelo de Gemini (versión 9.1 del plan, «La
cascada de madrugada»): **`MDS-018` va a fallar esta noche a las 03:13 y a las 06:47 UTC sin
subir nada, y salir a las 10:23**. No es avería — está anticipado por la dirección y la
publicación de las 19:00 no corre riesgo. Si mañana a las 11:30 `MDS-018` sigue sin estar en el
registro, **eso sí** sería incidencia (después de las 08:23 ya no hay más intentos).

## Paso 6 — ESTADO.md

Ver `05_calendario/ESTADO.md`, reescrito entero.

---

**Ficheros tocados esta sesión:** `04_agentes/metricas.py`,
`05_calendario/guiones/MDS-018.es.json` (excepción de 48 h, único guion tocado),
`07_pruebas/LEEME.md`, `07_pruebas/registro-diario-15-09/sincroniza_registro.yml`,
`07_pruebas/registro-diario-15-09/LEEME.md`, `05_calendario/ESTADO.md`,
`05_calendario/bitacora/2026-09-15-revision.md` (este fichero).
