# Revisión diaria — 13 de septiembre de 2026, 11:30 (domingo)

Sesión en la nube, sin acceso al ordenador del codirector (no hay puente
`mcp__remote-devices__*` en este modo y no lo ha habido nunca). Clono
`origin/main`, trabajo en el contenedor, entrego con `.tar.gz`.

**Comprobación de entregas pendientes (primera acción).** `git log --oneline
-8` sobre `origin/main`: los dos últimos commits son de la dirección del
sábado 12/09 (`033965b` «corrección de guiones y revisiones semanal y
modificación de tareas», `16cac70` «fix (producir): añadir secreto Gemini
para cambio voces»). Hoy es domingo, no viernes ni sábado, así que la
comprobación estricta de la regla no aplica — pero además no hace falta:
`3814c55` (planificación del jueves 10/09, aplicada el viernes 11) sigue
siendo el último commit que toca `05_calendario/guiones/`, y nada de lo del
sábado lo modifica. **No hay entrega pendiente de planificación bloqueando
nada.** Se procede con normalidad.

**Aviso de la propia dirección leído antes de tocar nada.** El commit del
sábado reescribe el prompt de esta tarea (`revision-diaria.md`, versión 7 de
`PLAN_DE_CAMBIOS.md`) y toca código del que soy dueña: `escena.html` (C29:
`ricoSVG()`, `ajustarTextoSVG()`, la rama SVG de `comprobarDesbordes()`),
`validar_guion.py` (C28 + aviso C29) y los dos prompts de guionista,
reescritos enteros. Parto de `origin/main` para todo esto, no de lo que
pueda recordar de sesiones anteriores (que no tengo: esta sesión no tiene
memoria de las anteriores, cada firma es un clon nuevo).

## Paso 1 — revisión editorial

Guiones españoles sin producir (`parrilla.json` contra
`registro_publicaciones.json`, que llega hasta `MDH-006.es`): `MDS-016` a
`MDS-020` (14–18/09) y `MDH-007` (19/09) — los mismos seis de ayer y
anteayer, sin cambios (`git log` desde `3814c55`: cero commits sobre
`05_calendario/guiones/`). `python3 04_agentes/validar_guion.py` sobre los
seis: sin errores ni avisos, incluidas las dos comprobaciones nuevas del
sábado (C28 día/mes en pantalla, C29 pasos de diagrama largos) — no hay
ningún día/mes huérfano en pantalla en los seis, y los dos `diagrama` de
`MDH-007` no superan el tope de palabras por paso.

Con el guion sin cambios desde la lectura completa de dos días, no repito de
memoria: releo yo misma, escena por escena, con ojos frescos (esta sesión no
tiene memoria de las anteriores — es fresca por definición, no por mérito):

- **Los dos canales.** En los cinco Shorts, cada sustantivo con carga de
  `texto`/`cifra`/`pie` tiene correlato literal o reformulación evidente en
  la `narracion` de su propia escena. Comprobado campo a campo: MDS-017
  escena 4 (`cifra: "50 kHz"` ↔ «cincuenta kilohercios»), MDS-018
  escena 1 (`«viernes»` en pantalla ↔ «cada viernes» en la narración, el
  caso exacto que vigila C28), MDS-019 escena 3 (`«Atención sí» / «el
  mensaje, no siempre»` ↔ narración literal de Eisend), MDS-020 escena 3
  (`«20 cómicos»` ↔ «veinte cómicos profesionales» de Mirowski). Sin
  hallazgos. En `MDH-007` (41 escenas) los patrones de «tarjeta de sección +
  narración diferida un compás» (escenas de tipo `titulo`) se resuelven
  siempre en la escena siguiente; sin datos introducidos solo en pantalla.
- **La cara (regla 14.3).** `duda`/`no` solo donde la narración dice
  explícitamente que algo falla, duele o no cuadra: MDS-016 a MDS-020 llevan
  `no` únicamente en sus cierres («y aquí falla…», las cinco); `MDH-007`
  lleva `duda` en la escena de «la profecía se cumple sola… si no fuera
  porque duele» y en la de «aquí es donde falla todo lo anterior» — las dos
  con «falla»/«duele» explícito. Ninguna escena que solo presenta un dato o
  una comparación lleva cara triste. `piensa`/`entiende`/`rie` van siempre
  en escenas que presentan. Sin hallazgos.
- **C19 (escena 1 de los Shorts).** Los cinco arrancan con icono
  (`i-bocadillos`, `i-muelle`, `i-publico` ×2, `i-bocadillos`) y 2-4
  palabras; ninguno es tarjeta de texto.
- **La pausa antes del remate.** 1.35 s en los cinco Shorts (entre
  planteamiento y remate), dentro de 1.2–1.5. En `MDH-007`, primera risa
  hacia el segundo 11–12 (por debajo del tope de 15) y segunda risa en el
  bloque de improvisación — cumple regla 13.
- **El escéptico (`MDH-007`).** Dos intervenciones, «¿Timidez con nombre
  caro?» (8 palabras) y «¿Y si sí se ríen de mí?» (9 palabras); las dos por
  debajo de 12 y las dos resueltas en la escena siguiente, nunca antes.
- **C17 (no repetirse).** Fuentes centrales de los seis pendientes: `I05`
  (MDS-016), `E06` (017), `G06` (018), `G05` (019), `K02`/`K03` (020),
  `H05`/`H06`/`J01`/`J02` (MDH-007). Cruzado contra el resto de guiones del
  repositorio (`grep` por fuente en `05_calendario/guiones/*.json`): ninguna
  se repite como fuente central en otro guion, producido o pendiente. Sin
  conflicto.
- **Resaltado.** Comprobado por script en los seis guiones: cero apariciones
  de `*`/`_` dentro de ningún campo `narracion` (el defecto de MDS-011 no se
  repite), y nunca más de un ámbar por escena en los campos que se ven.
  Coral: sigue sin existir sintaxis de marcado para él en el guion (ya
  anotado el 10/09 en `MEJORA_VISUAL.md` y en la bitácora de ese día); no es
  hallazgo nuevo, es una limitación conocida y aceptada del renderizado
  actual — `rico()` en `escena.html` solo distingue `amb`/`cia`.
- **Estructura de serie (encargo P10, «El experimento»).** `MDS-018` y
  `MDS-020` son «El experimento» y las dos tienen una escena `dato` con
  `fuente`, cumpliendo el mínimo que pide el encargo. No implemento hoy el
  aviso en `validar_guion.py` (ver «Paso 4», la ranura de esta sesión se ha
  ido a C27-B).

**`MDH-007`:** releída la nota abierta de `revisiones/MDH-007.md` (regla 2,
escenas 19 y 31 con fuente floja o ausente respectivamente) — sigue vigente,
sin cambios en el guion desde que se escribió, y sigue bien razonada con sus
dos salidas para quien tenga el artículo delante. Publica el 19/09, la
planificación del jueves 17 tiene margen de sobra. No añado hallazgos
nuevos.

**Sobre el «Límite» de `MDH-007` (verificación propia, no solo heredada).**
Comprobé además que la estructura de `guionista.md` separa `Límite`
(«cuándo la técnica falla o hace daño») de `Cierre` (tesis + tarea + una
pregunta de comentarios) como dos tramos DISTINTOS en el episodio largo —
a diferencia de los Shorts, donde «el cierre dice dónde falla» es una sola
cosa. El «falla» de `MDH-007` vive en su bloque de Límite («el cuestionario
lo rellenas tú» y el sesgo de quién se apunta a un taller), y el cierre
final es una frase de cierre emocional sin remate, tal y como dicen sus
propias `notas_humor` («el cierre no lleva chiste»). Encaja con la
estructura documentada; no es un defecto.

## Paso 2 — ¿salió la producción?

`registro_publicaciones.json` sin novedad desde ayer: `MDH-006.es` sigue
siendo la última entrada (subida 12/09 01:33:42 UTC, `publicar_en`
2026-09-12T10:00:00Z, ya pasado). Hoy domingo **no hay emisión en
`parrilla.json`** (el canal publica L–V Shorts + sábado episodio largo; el
domingo no tiene hueco), así que no hay nada que comprobar hoy en este
paso — ni ausencia que investigar, ni entrada nueva que verificar.

**`MDS-011` — cerrado, no se vuelve a abrir (regla del 12/09).** Comprobado
`00_estrategia/PROMPT_DIRECCIÓN.md`, entrada del 9/9: *«La revisión diaria
sigue pensando que MDS-011 no se publicó, hasta el punto de ponerlo como
incidencia, cuando lo cierto es que lo publiqué yo a mano.»* El codirector
lo ha dicho explícitamente en uno de los tres sitios que cuentan
(`PROMPT_DIRECCIÓN.md`). El registro sigue con `estado: "private"`,
`publicar_en: null` — no lo toco, no es mío, y **no vuelve a aparecer en
`ESTADO.md`** a partir de hoy: el asunto está cerrado, tal como dice la
regla nueva. No lo cuento tampoco como «sigue sin confirmar»: esa fórmula es
para cuando el codirector NO ha dicho nada; aquí sí lo ha dicho, así que es
un peldaño más abajo que eso — simplemente no se menciona.

## Paso 3 — revisión del vídeo del día

No aplica: sin emisión programada para hoy domingo. El último vídeo
(`MDH-006`, sábado) ya se revisó a fondo ayer (capturas, los dos canales
sobre el vídeo producido, `ficha.json`) y quedó en la bitácora del 12/09 con
dos avisos ya trasladados (`pico_dbtp` −0,59 y el texto de la escena 22
saliéndose de su caja a las 2:34). Sobre este segundo: **confirmado hoy que
ya está arreglado**, ver Paso 4.

## Paso 4 — código y encargos

**El texto de MDH-006 (2:34) — confirmado arreglado, no es una incidencia
que siga abierta.** El codirector lo reportó en `PROMPT_DIRECCIÓN.md`
(12/09): «en el segundo 2:34 la frase "Espera a que lo abra el otro" no
entra en el rectángulo». Es exactamente la escena 22 de `MDH-006`, y el
propio comentario de C29 en `validar_guion.py` (escrito el sábado) lo cita
por su nombre: *"MDH-006 escena 22 salió publicada con «Espera a que lo abra
el otro», 604px en 533."* La dirección ya lo arregló el mismo sábado en
`escena.html` (`ajustarTextoSVG()` encoge el texto de los pasos de
`diagrama` hasta que quepa) y añadió el aviso C29 en el validador para que
no se repita sin verse. No hay nada más que yo tenga que hacer aquí — ya
está en `origin/main`, verificado con el propio comentario del código y con
que `validar_guion.py` no avisa sobre ningún guion pendiente hoy.

**GEMINI_API_KEY en el paso correcto de `producir.yml` — confirmado.** Los
tres avisos consecutivos de los días 10, 11 y 12 quedan resueltos: `grep` en
`.github/workflows/producir.yml` confirma que `GEMINI_API_KEY` está SOLO en
el paso «Sintetizar narración» (línea 197) y ya NO está en «Subir a
YouTube». El codirector hizo el `git add/commit/push` que pedía
`tareas_codirector_2026-09-12.md` (tareas 1 y 2), confirmado por los dos
commits de ayer tarde (`0700426`, `16cac70`). No hace falta seguir avisando
de esto.

**El cambio de código de la sesión: diseño de C27-B (la caché de voz del
episodio largo, por adelantado).**

`LO PRIMERO DE LA SEMANA DEL 14` (nuevo del 12/09) pide dos cosas: (A) la
caché de `voz.py` — **ya escrita**, comprobado en el código
(`CACHE_VOZ_DIR`, `_cache_voz_ruta()`, ya en `origin/main` desde el encargo
4/C7 del 10/09) —, y (B) el diseño de `voz_adelantada.yml` para que lo cree
el codirector a mano. Con (A) hecho, hoy entrego (B), y para que funcione de
verdad hacía falta una pieza de código que no existía: algo que rellene la
caché de un guion `largo` sin producir vídeo. Es el único cambio de código
de la sesión (regla 11.1):

- **`03_produccion/pipeline/voz_precache.py` (fichero nuevo).** Reutiliza
  como librería las piezas de `voz.py` que ya existen (`hablable()`,
  `_cache_voz_ruta()`, `_gemini_cliente()`, `_gemini_pcm()`, `_pcm_a_mp3()`,
  `CACHE_VOZ_DIR`, las voces `Charon`/`Puck`, la espera de 25s) — **no
  modifica ni una línea de `voz.py`**, así que el riesgo sobre la
  producción diaria de hoy es cero. Para cada escena de un guion `formato:
  "largo"` sin cachear todavía, sintetiza con `gemini-2.5-flash-preview-tts`
  (no el `gemini-3.1-flash-tts-preview` de los Shorts: son cuotas diarias
  independientes) hasta agotar un presupuesto de llamadas (9 por defecto,
  con margen bajo el límite real de 10/día).
  - **El detalle que hacía falta resolver, y no estaba en el encargo
    original:** la clave de caché de los Shorts es
    `_cache_voz_ruta(texto, "gemini", voz)` — el literal `"gemini"` a
    secas, porque hasta hoy solo había un modelo de Gemini en juego. Con
    dos modelos (3.1 para Shorts, 2.5 para el largo) ese literal
    colisionaría: un guion largo y un Short con la misma narración y la
    misma voz compartirían entrada de caché aunque el audio fuera de
    modelos distintos. `voz_precache.py` cachea con el **modelo exacto**
    (`gemini-2.5-flash-preview-tts`) en la clave, no con el literal, así
    que las dos cachés nunca chocan. Queda escrito en la cabecera del
    fichero como aviso para el día en que `voz.py` aprenda a construir el
    largo con Gemini de verdad: tiene que pedir la caché con este mismo
    modelo, o el precacheo de toda la semana no sirve de nada.
  - Presupuesto agotado o cuota diaria agotada (`RESOURCE_EXHAUSTED`/429
    «per day») paran limpio, sin excepción sin capturar, dejando dicho
    cuántas escenas quedan para el día siguiente. Un fallo transitorio de
    una escena suelta no cuenta contra el presupuesto ni marca nada como
    agotado: se reintenta al día siguiente.
  - Escritura atómica (`.tmp.mp3` + `replace()`): no deja cachés a medio
    escribir si el proceso muere a mitad de la conversión con ffmpeg.
- **`07_pruebas/voz-adelantada-14-09/voz_adelantada.yml` + `LEEME.md`
  (nuevos, en el buzón, no en `.github/workflows/`).** El diseño completo
  del workflow: de **martes a viernes** (corregido desde «viernes a
  viernes» del encargo original — el guion del sábado que viene no existe
  en el repo hasta que la planificación del jueves le hace `push`, que pasa
  normalmente el viernes por la mañana; el rango correcto son los cuatro
  días en que ese guion ya está escrito), coge el guion del próximo sábado
  con `cola.py --fecha ... --github-output` (reutiliza la única fuente de
  verdad sobre qué toca cada día, en vez de repetir la lectura de
  `parrilla.json`), llama a `voz_precache.py`, y comitea solo
  `03_produccion/cache_voz/` con el mismo patrón de `git pull --rebase
  --autostash` que ya usa `producir.yml` en su paso de registro. Grupo de
  concurrencia propio (`voz-adelantada`), para no competir por runner ni
  por commit con `produccion`. El `LEEME.md` explica todo esto, qué NO hace
  a propósito (no decide qué motor usa el episodio final — esa decisión es
  aparte, el viernes por la noche, y sigue sin existir el paso de
  producción que la aplique) y cómo probarlo con `workflow_dispatch` y
  `presupuesto: 1` sin gastar cuota de verdad.

**Verificación (regla 11.2/11.5: el motor, no memoria; determinista), en
`/tmp`, fuera del repositorio.** Con `edge-tts` y `google-genai` instalados
en el contenedor (hoy sí había red hacia PyPI) pero **mockeando la llamada
real a Gemini** (sin credenciales aquí):

1. `escenas_pendientes()` ignora narraciones vacías y limpia el marcado de
   resaltado con `hablable()` antes de calcular la clave.
2. La ruta de caché del largo es demostrablemente distinta de la que
   generaría el camino de Shorts para el mismo texto y la misma voz (hash
   exacto comparado, no solo «son objetos distintos»).
3. `narrador`/`escéptico` se asignan a `Charon`/`Puck` según el campo `voz`
   de la escena.
4. `formato: "corto"` no sintetiza nada (guarda temprano, ni una llamada).
5. El presupuesto se respeta exactamente: con 3 pendientes y presupuesto 2,
   cachea 2 y deja 1; una segunda pasada sin presupuesto extra completa
   justo la que faltaba, sin repetir las dos ya hechas ni gastar más
   llamadas de las necesarias.
6. Cuota diaria agotada: se detecta, no lanza excepción, no cachea nada a
   medias, dice cuántas quedan.
7. Sin ficheros `.tmp.mp3` huérfanos tras la ejecución.
8. `python3 -m py_compile` sobre `voz_precache.py`: sin errores. `git
   status`: solo el fichero nuevo tocado; nada de `/tmp/test_precache/`
   quedó en el repositorio.

**Lo que no se ha podido probar:** la llamada real a la API de Gemini con
`gemini-2.5-flash-preview-tts` (necesita `GEMINI_API_KEY` y red hacia
Google, que este contenedor no tiene). Queda dicho en el `LEEME.md` cómo
probarlo con `presupuesto: 1` en cuanto el codirector suba el `.yml`.

### Datos, no código: créditos de P9 dejados listos por adelantado

El encargo 10 de la cola pedía dejar hecha la entrada de los tres sonidos de
`03_produccion/sonidos/` en un `creditos.json` indexado por sha256, «igual
que la música», sin esperar a que `montaje.py`/`publicar.py` aprendan a
comprobarlo (eso entra en la semana del 21 según C25). Esto es escritura de
datos, no un cambio de comportamiento de ningún script, así que no consume
la ranura de código de hoy (regla 11.1 es sobre producción; esto no cambia
nada de lo que se produce hasta que algo lo lea).

**Decisión: fichero nuevo, `03_produccion/sonidos/creditos.json`, NO
añadido a `03_produccion/assets/musica/creditos.json`.** El de música vive
bajo `assets/musica/` y lo usa `publicar.py` específicamente para pistas de
cama; mezclar ahí acentos cortos de montaje habría forzado un `_nota` de
"esto no es música" en un fichero cuyo propio nombre de carpeta dice lo
contrario. El esquema es idéntico (`sha256` → `archivo`/`titulo`/`autor`/
`licencia`/`atribucion`) para que el día en que se escriba la comprobación
de verdad sea un `if` más, no un formato nuevo que aprender. Dejado escrito
en la `_nota` del propio fichero para quien lo use después.

Acreditados los tres que existen (`sha256sum` calculado hoy sobre los tres
`.wav` de la carpeta, no copiado de ningún sitio): Coin Pickup Sound V 0.2
(Davidsraba), Wooden Hit (jackyyang09), 20251126_Click - Case close
(Sadiquecat). **Sigue faltando el cuarto** (`wood board hit.wav`, de
alec_mackay, freesound.org/s/463651/) porque el fichero no está en la
carpeta — mismo hueco que ya anotó `PLAN_DE_CAMBIOS.md` el 07/09, sin
cambios hoy. No lo invento ni lo acredito sin el fichero.

### Resto de la cola, sin tocar hoy

- **P6 (tres tamaños), P10 (aviso de estructura de serie en
  `validar_guion.py`), C31 (estado real de YouTube a diario), encargo 8
  (`escena.html`: `lista` + personaje) y encargo 12 (CSV de Studio,
  prioridad baja):** sin cambios. La ranura de código de hoy se ha ido a
  C27-B, que tenía fecha de presión real («lo primero de la semana del
  14»). Nada de esto bloquea el canal mientras tanto.
- **C18 (música):** bloqueado por red, no reintentado.
- **`04_agentes/prueba_voz.py`:** no tocado, ya cumplió su función.

## `07_pruebas/`

Revisado `LEEME.md`: `prueba-de-voces/` sigue respondida desde el 04/09,
`producir-yml-07-09/` ya aplicado (confirmado con el `grep` de
`GEMINI_API_KEY` de más arriba — es el mismo commit el que aplicó las dos
cosas). Nada nuevo dirigido a mí hoy antes de escribir esta bitácora. Dejo
una entrada nueva (`voz-adelantada-14-09/`, ver Paso 4) y actualizo la tabla
del `LEEME.md` de la carpeta para que se encuentre.

## Paso 5 — esta bitácora

Este fichero.

## Para el codirector

1. **Nada nuevo pendiente de ti.** `GEMINI_API_KEY` ya está donde debía —no
   hace falta que hagas nada más ahí. El texto de MDH-006 a las 2:34 ya está
   arreglado en el código. `MDS-011` queda cerrado definitivamente: no
   vuelve a aparecer en `ESTADO.md`.
2. **Diseño listo para cuando quieras encenderlo:**
   `07_pruebas/voz-adelantada-14-09/voz_adelantada.yml` — cópialo a
   `.github/workflows/` cuando quieras que el episodio largo empiece a
   precachear voz de Gemini de martes a viernes. El `LEEME.md` de al lado
   explica todo y cómo probarlo con una sola llamada antes de dejarlo en
   cron. No es urgente: mientras no exista, el largo sigue en `edge-tts`
   exactamente como hasta ahora.
3. Sin hallazgos nuevos en los seis guiones pendientes, releídos hoy con
   ojos frescos. `MDH-007` sigue con los dos avisos de regla 2 de
   `revisiones/MDH-007.md` (fuente floja/ausente en dos escenas), esperando
   a la planificación del 17/09; hay margen de sobra.
4. Sin emisión hoy domingo (el canal no publica los domingos); nada que
   revisar en el vídeo del día.

## Paquete

Ficheros nuevos o tocados en esta sesión:

- `03_produccion/pipeline/voz_precache.py` (nuevo — C27-B, precacheo de voz
  de Gemini para el episodio largo, sin tocar `voz.py`)
- `03_produccion/sonidos/creditos.json` (nuevo — créditos de los tres
  sonidos de P9 que existen, adelantado del encargo 10)
- `07_pruebas/voz-adelantada-14-09/voz_adelantada.yml` (nuevo — diseño del
  workflow, para que lo cree el codirector a mano)
- `07_pruebas/voz-adelantada-14-09/LEEME.md` (nuevo — explicación completa)
- `07_pruebas/LEEME.md` (editado — fila nueva en la tabla)
- `05_calendario/bitacora/2026-09-13-revision.md` (nuevo, este fichero)
- `05_calendario/ESTADO.md` (reescrito — OK)

No lleva `registro_publicaciones.json` ni `05_calendario/qa/`. No lleva
`parrilla.json`, `CALENDARIO.md`, `demanda.json`, `demanda_bruta.json`,
`metricas.json` ni ningún fichero de `.github/workflows/`. No lleva ningún
guion de `05_calendario/guiones/` ni de `publicaciones/` — no hubo excepción
de 48 horas hoy, ningún guion pendiente se produce antes del jueves 17. No
toca `montaje.py` ni `escena.html` ni `validar_guion.py` ni `voz.py` — cero
cambios sobre código de producción existente esta sesión, solo un fichero
nuevo aislado. No lleva nada de `/tmp/test_precache/`, que nunca formó parte
del repositorio.
