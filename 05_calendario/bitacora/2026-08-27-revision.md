# Revisión diaria — 27 de agosto de 2026 (jueves)

## Comprobación de entregas pendientes

`git log --oneline -8` sobre `origin/main`: el último commit es `664f57f`
(«revisión del día 26/08/26, retoques en dos guiones», de ayer). Hoy es
jueves, así que el bloqueo de `05_calendario/` de
`PROPIEDAD_DE_FICHEROS.md` (que solo aplica viernes o sábado, para no
pisar la planificación del jueves) **no entra en juego todavía**: la
planificación de esta semana corre esta noche a las 22:00, después de esta
revisión, no antes. Sin entrega pendiente sin aplicar.

## Paso 1 — revisión editorial

Guiones españoles sin producir (`parrilla.json` cruzado con
`registro_publicaciones.json`, que sigue sin más entradas desde `MDS-003.es`
del 26/08 — ver Paso 2, es el hallazgo de hoy): `MDS-004` a `MDS-006` y
`MDH-004` a `MDH-008`. Leídos los ocho de principio a fin, no solo por
encima.

**Sin hallazgos nuevos.** Comprobado campo por campo que ninguno ha
cambiado desde ayer:

- `MDS-004.es.json` y `MDS-005.es.json` siguen con el arreglo de ayer
  (`personaje` en 2, 4, 6 y en 2, 3, 6 respectivamente; ninguno en la
  escena del chiste). Repasados otra vez pausas (1,2-1,5 s ✓), remate en la
  última escena ✓, cierre que dice dónde falla ✓, comillas angulares ✓,
  cifras con fuente ✓, un resaltado ámbar por pantalla ✓.
- `MDS-006.es.json`: sigue con `personaje` en 1, 2, 4, 5 y 6, exactamente
  como describe `revisiones/MDS-006.md`. La nota sigue vigente tal cual;
  no la reescribo.
- `MDH-004.es.json` (adaptado, publica el sábado 29/08): repasado escena a
  escena — gancho con risa sobre el segundo 11 (personaje `rie` en la
  escena 1), callback en la escena 29 al mismo «nos vemos luego», dos
  intervenciones del escéptico (escenas 9 y 22, diez palabras cada una,
  resueltas en la escena siguiente), duración ≈4m 56s, las tres cifras
  heredadas siguen con fuente y el aviso de bibliografía de `E02` sigue
  abierto — nada que añadir a `revisiones/MDH-004.md`, sigue vigente.
- `MDH-005` a `MDH-008`: mismo perfil sin adaptar de siempre (sin
  `formato`, cero `personaje`, cero escéptico). Las cuatro notas de
  `revisiones/` siguen describiendo exactamente lo que hay; no las
  reescribo porque ninguno de los guiones ha cambiado.

Búsqueda expresa de lo que más pesa en las reglas — «humor y atracción»,
supuestos de género, la palabra «violación», comillas rectas en diálogo —
sobre los ocho guiones: sin coincidencias.

`python3 04_agentes/validar_guion.py` sobre los ocho: **sin errores**
(exit 0). Avisos, más que ayer porque hoy bajé el umbral de aviso por
duración de escena — ver Paso 4 — pero ninguno nuevo de fondo.

## Paso 2 — ¿salió la producción?

**No.** `parrilla.json` esperaba `MDS-004.es` hoy (cron de `producir.yml`
a la 01:00 UTC). A esta hora (revisión iniciada a las 05:07 UTC, más de
cuatro horas después del cron) `registro_publicaciones.json` sigue
terminando en `MDS-003.es` del 26/08 y no existe
`05_calendario/qa/MDS-004.es/`. Para referencia, las tres últimas
producciones se registraron entre las 03:38 y las 04:05 UTC (unas 2,5-3 h
después del cron), así que esto no es "todavía no ha terminado": ya
debería estar.

Descartado que sea el guion: `python3 04_agentes/validar_guion.py
05_calendario/guiones/MDS-004.es.json` pasa limpio y en 54,5 s, dentro del
límite. No he podido mirar el propio run de GitHub Actions —el acceso a
la API de GitHub y a la página de Actions está bloqueado para esta
sesión (403 de `api.github.com`; `WebFetch` sobre la página pública pide
una aprobación que nadie puede dar en una tarea desatendida)—, así que no
sé si el workflow falló, se quedó colgado o simplemente no ha corrido.
No he intentado producir nada yo mismo ni tocar el workflow: no me
corresponde y sería reproducir a ciegas lo que hace el bot.

**Esto es el hallazgo principal de hoy.** Se lo aviso a Silvestre aparte.

## Paso 3 — vídeo (toca hoy, jueves)

Como no hay `MDS-004` que mirar, reviso `MDS-003.es` (el último producido,
26/08), que aún no había tenido su pase de vídeo formal.

Seis fotogramas (`05_calendario/qa/MDS-003.es/*.jpg`, 640×1138): texto sin
cortes ni desbordes en ninguno, contraste alto, el Engranaje siempre entre
~y=685 y ~y=820 px, por encima del límite inferior de ~901 px que taparía
la interfaz de Shorts. El rótulo de marca y el código de fuente (`A01`,
`A03`) caen en la esquina superior, dentro de la franja que tapa la
interfaz — pero es rótulo y cita, no información necesaria para entender
el vídeo, y es el mismo sitio de siempre. Nada que corregir.

Tres de los seis fotogramas (`f1`, `f2`, `f4`) confirman a ojo lo que dice
el Paso 4: el tramo central de la escena es un bloque de texto y mucho
fondo vacío, sin nada más moviéndose.

`ficha.json`: `audio.lufs -14.41` (dentro de ±1), `audio.pico_dbtp -1.3`
(≤ -1,0 ✓), `arranque.fragmento_antes_de_la_narracion: false` ✓,
`subtitulos.quemados: null` con `quemados_origen` explicado (sin
manifiesto de `montaje.py`, no es un fallo — así quedó documentado el
24/08), `subtitulos.lineas_ass: 108` (canario sano). Sin incidencia.

## Paso 4 — mejora visual (toca hoy, jueves)

Prioridad C6 otra vez. C6.2 (capa viva en FFmpeg) y C6.3 (zoom con
preescalado) siguen tocando `montaje.py`, que está protegido, y la
decisión de Silvestre sobre si merece la pena seguía sin caer a fecha de
ayer — no las toco.

**C6.4 (escenas más cortas y numerosas) sí tiene una parte que es mía: la
prompt y el portero, no el guion.** No puedo reescribir `MDH-005` a
`MDH-008` —son de la planificación—, pero sí puedo hacer que la
planificación del jueves que viene escriba escenas más cortas por
defecto, y que el portero lo delate antes de llegar a producción.

**Medido antes de tocar nada**, con `render.py` sobre un par de prueba
fuera del repositorio (no toca ningún guion real): una escena de 18,6 s de
narración captura 30 fotogramas; la misma narración partida en dos
escenas de ~10 s captura 60 — el doble de movimiento por el mismo
contenido, a coste proporcional al número de escenas, no al metraje.
Confirma lo que dice `00_estrategia/PLAN_DE_CAMBIOS.md` sobre C6.4.

**Cambiado:**

1. `04_agentes/validar_guion.py`: `MAX_IDEAL` de 14,0 s a 10,0 s (comentario
   con la medición de arriba y el porqué). Sigue siendo aviso, no error:
   no bloquea nada, solo adelanta al guion lo que antes solo se veía en el
   vídeo terminado.
2. `04_agentes/prompts/guionista.md`: nueva sección «Movimiento en
   pantalla: escenas cortas y numerosas» con la regla (apuntar a ≤10 s por
   escena, partir en dos en el punto natural de respiro en vez de a medias
   frase) y la misma medición, para que la planificación del jueves que
   viene la tenga delante al escribir.

**Verificado que no rompe nada:** `validar_guion.py` sigue devolviendo
exit 0 y «Sin errores graves» en los ocho guiones sin producir tras el
cambio — solo suben los avisos, que es la intención:

| Guion | Avisos antes (umbral 14 s) | Avisos después (umbral 10 s) |
|---|---|---|
| MDS-004 | 1 | 1 (el Short no toca esta regla) |
| MDS-005 | 0 | 0 |
| MDS-006 | 0 | 0 |
| MDH-004 (adaptado) | 0 | 19 |
| MDH-005 | 14 | 24 |
| MDH-006 | 15 | 25 |
| MDH-007 | 15 | 22 |
| MDH-008 | 15 | 24 |

`MDH-004` —el propio episodio de referencia, ya adaptado el 20/08— pasa de
cero avisos a diecinueve. Es intencionado: la adaptación resolvía
`formato`/`personaje`/escéptico, no la duración de escena, y con el
umbral nuevo casi todas sus escenas de 10-12,7 s quedan señaladas. No
lo interpreto como que la adaptación esté mal — sigue siendo el modelo a
seguir en estructura — solo como que la vara de medir el ritmo visual
acaba de subir para todos, adaptados incluidos.

**Descartado hoy:**

- Tocar `montaje.py` (C6.2, C6.3): protegido, decisión de Silvestre
  pendiente desde el 24/08, sin novedad.
- Reescribir `MDH-005` a `MDH-008` aplicando ya la regla de escenas
  cortas: es la reescritura completa que ya estaba anotada en
  `revisiones/`, y sigue sin ser mía — que la haga la planificación con
  la prompt ya actualizada delante.

## Lo que cambié

- `04_agentes/validar_guion.py` — `MAX_IDEAL` 14,0 → 10,0 s, con el porqué
  en comentario.
- `04_agentes/prompts/guionista.md` — nueva sección sobre movimiento en
  pantalla y escenas cortas (C6.4).

Nada en `05_calendario/` salvo esta bitácora — no hubo defecto editorial
nuevo que anotar en `revisiones/` hoy.

## Paquete

`mdh-revision-2026-08-27.tar.gz`, con exactamente estos ficheros:

- `04_agentes/validar_guion.py` (editado)
- `04_agentes/prompts/guionista.md` (editado)
- `05_calendario/bitacora/2026-08-27-revision.md` (nuevo, este fichero)

No lleva `registro_publicaciones.json` ni `05_calendario/qa/`. No lleva
nada de `05_calendario/guiones/` ni `revisiones/`: no hubo edición de
guion (ningún guion de hoy caía en la excepción de 48 horas con un
defecto nuevo) ni nota nueva que abrir.

## Para Silvestre

1. **`MDS-004.es` no se produjo esta madrugada.** Esperado por
   `parrilla.json` con el cron de `producir.yml` de la 01:00 UTC; a las
   05:07 UTC, más de cuatro horas después —y muy por encima de las 2,5-3 h
   que tardaron `MDS-001` a `MDS-003`— sigue sin entrada en
   `registro_publicaciones.json` ni carpeta en `05_calendario/qa/`. El
   guion pasa el validador limpio, así que no parece un problema de
   contenido. Esta sesión no tiene acceso a la API ni a la página de
   Actions de GitHub para ver el run y decir si falló, se quedó colgado o
   no llegó a lanzarse — hace falta que lo mires tú directamente en
   GitHub. Publica hoy a las 19:00; si el vídeo no aparece a tiempo, la
   emisión de hoy se pierde.
2. Sigue pendiente tu decisión, heredada del 24/08 sin novedad: permiso
   para tocar `montaje.py` con C6.2 (capa viva) o C6.3 (zoom con
   preescalado ×4), y el diff pendiente del manifiesto de subtítulos
   quemados para que `qa.py` no dependa de adivinar.
3. Nuevo hoy: bajé el aviso de duración de escena de `validar_guion.py` de
   14 a 10 segundos y añadí la regla correspondiente a
   `04_agentes/prompts/guionista.md` (detalle y medición arriba). Es solo
   aviso, no bloquea nada — pero si te parece demasiado agresivo o
   prematuro, dímelo y lo revierto la semana que viene.
4. Heredado del 26/08, sin acción mía: si quieres, aclara la regla del
   resaltado en cian («solo para datos») frente a la práctica real de
   varios guiones ya emitidos que lo usan para conceptos.
