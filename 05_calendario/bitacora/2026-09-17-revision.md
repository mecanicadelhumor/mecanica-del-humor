# Revisión diaria — 17 de septiembre de 2026, 11:30 (jueves)

Sesión en la nube, sin puente `mcp__remote-devices__*` (no existe en este modo, y no ha
existido nunca). No se ha pedido ningún permiso ni confirmación en toda la sesión. Clono
`origin/main`, trabajo en el contenedor, entrego con `.tar.gz`.

**Comprobación de entregas pendientes (primera acción).** `git log --oneline -8` sobre
`origin/main`: `f757bb1` (precacheo de voz C27, MDS-017), `195441f` (registro sincronizado
con YouTube 2026-09-17, 08:55 UTC), `6a039a8`/`3f7a48f`/`b929cab` (registro y expediente
MDS-019, vista previa del diseño, P1 aplicado ayer), `d137676` (precacheo de voz C27,
MDS-017 otra vez), `d6f1d0e` (registro sincronizado 2026-09-16), `b70087c` (registro y
expediente MDS-018). No es viernes ni sábado, así que la regla de «paquete de planificación
del jueves a medias» no aplica hoy; aun así comprobado: los cuatro cambios del 15/09
(C33.1/C33.2, MDS-017 a MDS-020 reescritos) y los cuatro del 12/09 (`escena.html`,
`validar_guion.py`, los dos prompts de guionista) siguen en `origin/main`, con los mismos
commits que ayer. **No hay entrega pendiente bloqueando nada.**

## Paso 1 — revisión editorial

Guiones españoles sin producir (`parrilla.json` contra `registro_publicaciones.json`):
`MDS-020` (18/09, **dentro de la ventana de 48 horas desde ahora**), `MDS-017` (19/09,
`rehacer_video_id`) y `MDH-007` (20/09). `MDS-019` se produjo esta madrugada y sale de este
paso al paso 3.

- `python3 04_agentes/validar_guion.py` sobre los tres: **sin errores ni avisos.**
- **Ninguno de los tres tiene un commit nuevo desde ayer** (siguen en `77c6922` los dos
  Shorts y en `3814c55` el largo, ya revisados escena por escena el 15 y el 16). No repito la
  lectura íntegra de los tres solo por rutina, pero **sí releí `MDS-020` entero** por estar
  hoy dentro de la ventana de 48 horas (aunque sin cambios, la releo igual: la ventana pesa
  más que la regla de «no repetir lo idéntico»): regla de los dos canales escena por escena
  (icono `i-bocadillos` + 4 palabras en la escena 1, correlato narración/pantalla en las seis
  escenas, `piensa` en la escena `dato` — no es cara triste—, `no` solo en el cierre),
  resaltado ámbar uno por escena en campos visibles, cita con fuente K02 y autor declarado,
  nada en `narracion`. **Sin hallazgo.** No hizo falta `05_calendario/revisiones/MDS-020.md`
  ni tocar el guion.
- **C17 (no repetirse).** Fuentes de los tres pendientes (`K02`, `E06`, `H05`/`H06`/`J01`/`J02`)
  contra `G05` (MDS-019, producido esta madrugada) y `G06` (MDS-018, producido ayer): ninguna
  coincidencia.

## Paso 2 — comprobar que la producción salió

`registro_publicaciones.json` sincronizado hoy a las 08:55 UTC (`_estado_leido_utc`), antes de
esta revisión — C31 sigue corriendo solo.

- `MDS-019` (jueves, hoy): `private`, `publicar_en: 2026-09-17T17:00:00Z` (19:00 España),
  `subido_utc: 2026-09-17T01:33:58Z`. Patrón automático normal, dentro de la cascada de cuota.
  **No es incidencia.**
- `MDS-018` (16/09): **`public`**, confirmado. Sin incidencia.
- `MDS-017`: sigue `private` con `publicar_en` del 15/09 (pasado). **Cerrado, no se reabre**
  (regla del 12/09): el codirector documentó por escrito en
  `05_calendario/bitacora/2026-09-15-direccion.md` que retiró ese vídeo de Studio y movió el
  guion al sábado 19 con `rehacer_video_id` en `parrilla.json`. Nada nuevo hoy que cambie ese
  cierre.

## Paso 3 — revisión del vídeo (MDS-019)

`05_calendario/qa/MDS-019.es/` — seis fotogramas + `ficha.json`.

- **`ficha.json`:** `lufs -14.65` (±1, OK), `pico_dbtp -1.36` (≤ -1.0, OK),
  `fragmento_antes_de_la_narracion: false`, `quemados: false` (correcto), `lineas_ass: 0`
  (motor Gemini 3.1 en las seis escenas: esperado), `sincronia_voz.desfase_s: 0.28` (< 0.5,
  OK). `voz_mezclada: false` (tiene que serlo siempre; lo es). `modelo_voz:
  gemini-3.1-flash-tts-preview` (motor principal, no incidencia). **`origen_voz`: las seis
  escenas «llamada» (dirección completa) — 0 de caché, 0 de dirección mínima.** Anotado para
  la dirección: junto con lo de `MDS-018` (3 llamada / 3 caché, ayer), sigue sin haber ningún
  rechazo que fuerce la dirección mínima en estos dos vídeos. `escenas_ritmo_fuera_de_rango: []`
  — sin avisos de ritmo, a diferencia de `MDS-018` ayer.
- **Los seis fotogramas, los dos canales:** correlato exacto entre lo que se lee y lo que se
  narra en las seis escenas (incluida la comparación de la escena 4, «el doble» / «menos», que
  no introduce ningún dato que la narración no diga). Expresiones correctas contra la regla
  14.3: `rie` en el chiste de la escena 2, `piensa` en la escena `dato` (permitida, no es cara
  triste), `entiende` en la síntesis de la escena 5, `no` solo en el cierre. Sin `duda` fuera de
  sitio. Resaltado ámbar uno por escena en campos visibles, ninguno en `narracion`. Sin texto
  cortado, sin elementos tapados, sin faltas. Personaje e icono fuera de las zonas que tapa la
  interfaz de YouTube en los seis fotogramas.
- **C19 (escena 1).** Icono (`i-publico`) + 4 palabras («Me lo sé entero.»). Cumple.
- **Sin incidencia.**

### Una observación de código, no una incidencia: el resaltado coral no se puede escribir

Revisando el resaltado del cierre de `MDS-019` («la investigación viene algo *sesgada*»)
comprobé la regla de «coral = lo que falla, reservado al cierre» contra el motor. **No hay
forma de producir coral hoy.** `escena.html` define la clase `.cor{color:var(--coral)}` pero
ningún sitio del fichero se la asigna a nada — ni JS ni plantilla la aplican nunca (comprobado
con `grep`). Y el analizador de resaltado (`rico()`, línea ~513) solo traduce `*así*` → ámbar y
`_así_` → cian; no hay un tercer símbolo. Confirmado también que
`04_agentes/prompts/guionista_corto.md`, reescrito el 12/09, **ya no menciona el coral en
absoluto** — solo documenta ámbar y cian. Así que `MDS-019` no tiene ningún defecto: usa ámbar
en el cierre porque es la única opción que el sistema ofrece de verdad, igual que todos los
guiones anteriores. Lo que hay es una clase CSS muerta y una regla de la tarea programada
(la mía) que describe una capacidad que el sistema dejó de tener en algún momento entre el
28/08 (cuando se documentó `ámbar`/`cian`, con coral ya mencionado por el codirector en algún
sitio) y el 12/09 (cuando se reescribió el prompt). No lo he tocado: no es mío decidir si el
cierre necesita un tercer color o si la regla simplemente ya no aplica — es una pregunta de
sistema visual, como la opacidad de P1. Lo dejo escrito aquí en vez de en `07_pruebas/` porque
no es una decisión de gusto (no hace falta ver nada), es una comprobación de hecho: el código no
tiene ese camino. Si la dirección quiere el coral, hace falta un tercer símbolo de marcado y
tocar `rico()`, `guionista.md` y `guionista_corto.md` a la vez.

## Paso 4 — código y encargos

**Ningún cambio de código hoy.** Repaso de la cola:

- **Encargos 1–5, 10, 11 y 12: confirmados entregados** (sin cambios desde ayer).
- **P1 (profundidad):** las dos piezas inequívocas (viñeta delante, deriva del fondo) siguen
  aplicadas desde ayer. La pieza que falta —la opacidad de la retícula— sigue en el buzón
  (`07_pruebas/P1-profundidad/P1-profundidad.md`) **sin respuesta todavía**. No hay nada nuevo
  que decidir sola hoy.
- **Encargo 8 (`escena.html`: `lista` + personaje, el hueco de 2px).** Sigue sin prisa —el caso
  conocido (MDS-007 esc. 4) ya está publicado y los tres guiones pendientes de hoy están muy por
  encima del margen (confirmado en el paso 1: ninguno usa `lista` con personaje ajustado al
  límite)—. El arreglo de verdad necesita construir el script de muestreo de `pintar(t)` en
  ~20 instantes por escena que todavía no existe (el 790px se calibró a mano el 07/09, no con
  una herramienta reutilizable), y luego rediseñar `ajustarTamano()` para que encoja también
  cuando el bloque no quepa en la banda reservada. Es dos piezas de trabajo completas y
  rigurosas, y la lección escrita en este mismo fichero de tareas es precisamente no
  apresurar la verificación de esta geometría (así salió MDS-011). No lo empiezo a medias hoy.
  Queda para una sesión dedicada, como dijo la revisión de ayer.
- **P8 (el personaje actúa).** Su bloqueo formal —la caché de voz— ya está resuelto (C27 y
  C33.2 entregados). Pero es la siguiente pieza completa de trabajo detrás de P1/encargo 8, no
  algo que quepa añadir de más en una sesión que ya ha revisado un vídeo, tres guiones y el
  código en profundidad. Queda en la cola, siguiente tras P1 y el encargo 8.
- **C36 (una llamada por vídeo).** Anotado en la versión 9.1 del plan como «gana urgencia»,
  pero **no tiene diseño escrito todavía** — no es un encargo con forma, es una idea. No es mío
  inventar el diseño sin que la dirección lo concrete en `PLAN_DE_CAMBIOS.md`.
- **Dato para el viernes (adelantado, no es tarea de hoy):** conté en `cache_voz/` cuántas
  escenas de `MDH-007` están ya cacheadas, usando `voz_precache.escenas_pendientes()` en modo
  lectura (sin llamar a ninguna API): **18 de 41 escenas cacheadas, 23 pendientes (las escenas
  19 a 41, la segunda mitad del episodio)**. `voz_adelantada.yml` ya corre todos los días
  (`cron: "0 9 * * *"`, comprobado leyendo el fichero, no editado — no es mío), no solo de
  martes a viernes, así que el cálculo de 27+10+10 peticiones del plan sigue vigente. No es
  incidencia: va según lo esperado, y lo dejo anotado para que mañana (con las fichas de
  `MDS-018` a `MDS-020` ya completas) se pueda cruzar con el conteo de `origen_voz`.

## Y una nota sobre `07_pruebas/`

Revisadas las siete carpetas. `P1-profundidad/` sigue sin respuesta (ver arriba).
`producir-yml-07-09/`, `imagen-14-09/` y `voz-15-09/` son diseños antiguos ya aplicados o
subsumidos por commits posteriores (ninguna trae una pregunta abierta sin contestar en su
`.md`); no las toco, la carpeta no la limpio yo.

## Paso 6 — ESTADO.md

Ver `05_calendario/ESTADO.md`, reescrito entero.

---

**Ficheros tocados esta sesión:** `05_calendario/ESTADO.md`,
`05_calendario/bitacora/2026-09-17-revision.md` (este fichero). Ningún guion tocado (no hizo
falta la excepción de 48 horas: sin hallazgos en `MDS-020`). Ningún fichero de `03_produccion/`
ni `04_agentes/` tocado hoy — la observación sobre el resaltado coral queda escrita, no
aplicada.
