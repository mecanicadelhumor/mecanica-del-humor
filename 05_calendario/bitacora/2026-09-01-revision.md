# Revisión diaria — 1 de septiembre de 2026, 11:30 (martes)

Sin acceso al ordenador de Silvestre en esta sesión (tarea programada en la
nube, sin puente al dispositivo): trabajo sobre un clon del repositorio y
entrego un `.tar.gz` al final, con los ficheros listados por nombre.

## Comprobación de entregas pendientes (primera acción)

`git log --oneline -8` sobre `origin/main`:

```
7a624df Registro y expediente de calidad: MDS-007
bf7b9a0 Registro y expediente de calidad: MDS-007
65f920e Registro y expediente de calidad: MDS-007
45a2dc3 revisión diaria 31/08
110e0b5 métricas 2026-08-31
0fdf9b6 cambios en la estrategia a 31/08: tareas programadas y cosas por hacer
7a34ad0 metricas semanales del 31/08
c3cc9e3 revisiones diarias del 29 y 30/08/26
```

Hoy es martes, no viernes ni sábado: la comprobación del paquete del jueves
no toca. **No hay entrega de planificación pendiente.** Los tres commits de
hoy son del propio workflow de producción (ver Paso 2, es el hallazgo
principal del día). Se procede con normalidad sobre `05_calendario/`.

## Paso 1 — revisión editorial

Guiones españoles pendientes, cruzando `parrilla.json` con
`registro_publicaciones.json`: `MDS-007` (01/09, hoy) a `MDS-010` (04/09) y
`MDH-005` (05/09). `MDH-006/007/008` siguen en backlog sin adaptar, no se
revisan hoy (a propósito, como ayer).

Releídos enteros los cinco, y revalidados con `validar_guion.py`: sin
errores en ninguno (un solo aviso ya conocido, `MDH-005` escena 39, ritmo
visual, no editorial). Nada ha cambiado en los guiones desde ayer, así que
confirmo lo ya registrado en vez de repetir el detalle:

- **MDS-007 y MDS-010** — sin hallazgos, igual que ayer.
- **MDS-008** — ya corregido ayer por la excepción de 48 h (hueco de audio y
  repetición de fuente); nada nuevo hoy.
- **MDS-009** — nota abierta desde el 30/08 sigue vigente sin aplicar (cian
  mal puesto en la escena 3), correcto que siga así hasta el jueves.
- **MDH-005** — las dos notas de contenido (cifra «4 estilos», cian
  decorativo) y la de C17 (B01/B02 repiten de MDS-004) siguen abiertas,
  correcto.

**C17 — cruce de `fuente` contra el corpus producido (11 entradas en
`registro_publicaciones.json`, sin cambios desde ayer porque hoy no se ha
confirmado ninguna publicación nueva — ver Paso 2):** mismo resultado que
ayer, sin repeticiones nuevas más allá de las dos ya anotadas en
`revisiones/MDH-005.md`.

`01_bibliografia/BIBLIOGRAFIA_CURADA.md`: sin cambios hoy, no he intentado
verificar fichas nuevas.

## Paso 2 — ¿salió la producción? **INCIDENCIA**

`MDS-007.es` (Short de hoy, `parrilla.json`: martes 01/09, 19:00, modo
`automatico`) **no está en `registro_publicaciones.json`.** Son las 09:30
UTC: las tres ventanas del cron (01:13, 04:47, 08:23 UTC) ya han pasado.
Investigado a fondo, sin acceso a los logs de Actions (la API de GitHub
sigue devolviendo 403 desde este contenedor, igual que ayer — el clon por
HTTPS sí funciona), pero el propio repositorio deja rastro suficiente:

**Lo que sí se puede confirmar, leyendo el árbol de commits de hoy:**

- Hay tres commits de `producir.yml` hoy, uno por cada intento de cron
  (01:30:56, 05:00:41 y 08:43:20 UTC — cuadran con 01:13+17min,
  04:47+13min, 08:23+20min de margen de ejecución).
- Los tres traen `05_calendario/qa/MDS-007.es/ficha.json` y sus seis
  fotogramas: **el vídeo se ha renderizado y montado correctamente las tres
  veces** (pequeñas diferencias de LUFS/silencio entre los tres commits,
  −1.44/−1.41/−1.43 dBTP, consistentes con volver a sintetizar la misma voz
  tres veces, no con un fallo de render).
- Pero el primer commit **reformatea `registro_publicaciones.json` sin
  añadir ninguna entrada `MDS-007.es`** — comprobado con
  `git show 65f920e -- registro_publicaciones.json | grep MDS-007`: solo
  aparece en el mensaje del commit, no en el JSON.
- `registrar.py` explica por qué: solo añade una entrada si existe
  `build/<ID>/publicado.json`, y si no existe, **la omite en silencio**
  («sin publicado.json (no llegó a subirse). Se omite.») — sin marcar error,
  sin parar el workflow. `publicado.json` lo escribe `publicar.py` al subir
  a YouTube, en el paso «Subir a YouTube», que en `producir.yml` va ANTES
  del paso «Expediente de calidad» (que sí corre siempre, con
  `if: ${{ !cancelled() }}`, incluso si el paso de subida falló).

**Conclusión, con la lógica del propio código delante, no a ciegas:** las
tres veces se ha renderizado el vídeo pero **ha fallado la subida a
YouTube**, silenciosamente desde el punto de vista del registro. Y como
`ya_subidos()` decide qué falta produciendo por lo que hay en
`registro_publicaciones.json` con `video_id`, al no haber entrada ahí cada
cron volvió a intentarlo desde cero — exactamente el diseño pensado para
reintentar subidas fallidas, pero aquí los tres reintentos se han topado con
lo mismo. **No hay indicio de que se haya subido tres veces el mismo Short**
(si alguna hubiera tenido éxito, `registrar.py` habría dejado la entrada, y
las siguientes ya no habrían vuelto a renderizar por `ya_subidos()`), así
que no hay vídeos duplicados en YouTube — el problema es que no hay ninguno.

**Lo que NO he podido averiguar sin los logs:** la causa exacta del fallo en
`publicar.py` (token de actualización caducado, cuota de la API, permiso de
verificación de YouTube, u otra cosa). No lo adivino ni lo fuerzo.

**Efecto:** el Short de hoy (01/09, 19:00) no se va a publicar solo. Las
tres ventanas de hoy están agotadas; mañana el cron busca la emisión del
02/09 (`MDS-008`), no reintenta `MDS-007` — así que sin que alguien lo
retome a mano (ejecución manual de `producir.yml` vía `workflow_dispatch`
con `guion: MDS-007.es` u otra fecha), este Short se queda huérfano.

## Paso 3 — revisión del vídeo (todos los días esta semana: motor C15)

Sin `.mp4` accesible (nunca se sube como artefacto de Actions ni al
repositorio), reviso lo único que hay: los seis fotogramas de
`05_calendario/qa/MDS-007.es/` y `ficha.json`.

**Técnico, todo en regla:** LUFS −14,58 (dentro de ±1), pico −1,43 dBTP
(≤−1,0), `fragmento_antes_de_la_narracion: false`, subtítulos no quemados
(correcto), 117 líneas `.ass` (>0, canario sano).

**Hallazgo visual — nº 1, con número: escena 4 («lista»), personaje tapando
el tercer punto.** Fotograma `f4_29.68s.jpg`: la escena `lista` («Cómo se
mide el gusto de verdad», tres puntos, `personaje: "piensa"`) muestra el
tercer punto — «Y el humor sexual, por su cuenta» — con la palabra «cuenta»
completamente oculta detrás del icono del personaje, y «su» a medio tapar.
El texto no se corta por el guion (el campo `puntos[2]` trae la frase
entera); se corta en el render, porque en `escena.html` el personaje ocupa
una zona fija (`bottom:430px; left:64px; width:250px;height:250px` en
formato vertical) que **no varía según cuánto ocupe el contenido de la
escena**, y una lista de tres puntos con texto largo puede crecer por
debajo de esa zona.

**Intento de reproducir y arreglar, y por qué no lo he tocado:**
`03_produccion/pipeline/vista.py` existe justo para esto, y lo he usado
(`vista.py --guion MDS-007.es.json --escenas 4`). Con las tipografías reales
del canal (Archivo Black / Inter / JetBrains Mono) el texto no cabría en una
sola línea por punto y desborda; en este contenedor solo está `DejaVu Sans`
(no hay `fc-list` con las de marca, ni están empaquetadas en el
repositorio — se instalan en el runner de Actions). Con `DejaVu Sans` los
tres puntos caben en una sola línea cada uno y **el defecto no se reproduce
aquí** — ver `/tmp/vista_antes/MDS-007_04_lista.png` de esta sesión, no
incluido en el paquete porque no demuestra nada sobre producción. El propio
`vista.py` avisa de esto en su cabecera: sin las tipografías de marca, «los
desbordamientos que veas pueden ser falsos» — y aquí es al revés, la
ausencia de desbordamiento aquí no dice nada sobre si lo hay en producción,
que es donde sí lo he visto en el fotograma real.

**No he tocado `escena.html` a ciegas.** Cambiar el CSS sin poder confirmar
con las tipografías reales habría sido arreglar adivinando, que es
justo lo que esta tarea tiene prohibido. Lo dejo anotado con precisión para
quien tenga acceso a `vista.yml` (el workflow de Actions con las
tipografías instaladas) o a una sesión con ellas disponibles:

- El caso concreto está en `05_calendario/guiones/MDS-007.es.json`, escena 4
  (índice 3), tipo `lista`, con `personaje: "piensa"`.
- Dos vías de arreglo, ninguna aplicada: (a) editorial — los tres puntos de
  esta lista suman ~22 palabras sin contar el título; acortarlos reduce el
  riesgo pero no lo garantiza sin verlo con la tipografía real; (b) código —
  `ul.lista li` en formato vertical tiene tamaño fijo (72 px) sin importar
  cuánto texto traiga la escena, a diferencia de `.enunciado`, que sí escala
  por `txt-xs`/`txt-s` según el recuento de palabras (líneas 557-561 de
  `escena.html`). Extender esa misma lógica de escalado por palabras a
  `ul.lista` sería la vía más robusta, pero exige verificarla con las
  tipografías de marca antes de tocar producción — no se puede hacer bien
  en este contenedor.

**Resto de las cuatro preguntas de la prioridad 1** (ritmo de revelado
palabra a palabra, deriva de la retícula, cruce del remate de marca con el
cierre): no se pueden juzgar desde seis fotogramas estáticos — hacen falta
el vídeo en movimiento, que esta sesión no tiene. El fotograma de cierre
(`f6_46.02s`) no muestra al personaje («no») invadiendo el subtítulo ni la
zona de 400 px inferior; el remate de marca (último 1,25 s) queda fuera de
los seis fotogramas capturados, igual que ayer.

**Capturas y minutos de render:** sin acceso a los logs de Actions (403 de
la API), no se pueden contar hoy tampoco. Mismo motivo que ayer.

## Paso 4 — encargo de código (uno por sesión)

**No he tocado el hallazgo del Paso 3** (necesita tipografías reales para
verificarse, ver arriba), así que la prioridad 1 de esta semana no exigía
gastar aquí el único cambio de hoy. He seguido con la cola de encargos:

**Encargo 2: `03_produccion/pipeline/cola.py`, el defecto por defecto.**

`modo = emision.get("modo", "revision")` significaba que una emisión de la
parrilla a la que se le olvidara poner `"modo"` se subía en privado y sin
`publicar_en` — nada la publicaría nunca. Es exactamente lo que le pasó a
`MDH-004` el 29/08 (dirección lo dejó escrito en
`bitacora/2026-08-31-direccion.md`, encargado a esta tarea). Cambiado el
valor por defecto a `"automatico"`: un olvido ahora falla hacia publicar,
no hacia el silencio. Un solo `.get()`, comentado en el propio código con
el caso de MDH-004 al lado. No toca el otro `"modo": "revision"` de la
función (la rama de `episodio` fuera de la parrilla, para producciones
manuales sueltas vía `workflow_dispatch`): ese es un valor fijo pensado
para pruebas manuales, no el defecto accidental que pedía el encargo.

Verificado: sintaxis válida (`ast.parse`), y releída `trabajo()` para
confirmar que con `modo == "automatico"` calcula `publicar_en` como se
espera — no hace falta más para un cambio de un valor por defecto.

Ningún otro encargo de la lista (aviso C17 en `validar_guion.py`, música
C18) tocado hoy — uno por sesión.

## Ficheros tocados hoy

- `03_produccion/pipeline/cola.py` — encargo 2: el defecto de `modo` pasa a
  `"automatico"`.
- `05_calendario/bitacora/2026-09-01-revision.md` — este fichero.
- `05_calendario/ESTADO.md` — reescrito entero, `INCIDENCIA` (MDS-007 sin
  publicar).

No toqué ningún guion (ni con la excepción de 48 h: el hallazgo del Paso 3
no tiene una corrección de guion verificable desde esta sesión). No toqué
`parrilla.json`, `registro_publicaciones.json`, `05_calendario/qa/`,
`CALENDARIO.md`, `demanda.json`, `validar_guion.py`, `montaje.py`, `voz.py`
ni ningún workflow.

## Para Silvestre

1. **MDS-007 (Short de hoy, 19:00) no se ha publicado.** Se ha renderizado
   bien tres veces (los tres intentos del cron de hoy) pero ha fallado la
   subida a YouTube las tres, sin dejar rastro del error accesible desde
   esta sesión (sin acceso a los logs de Actions). Conviene mirar el paso
   «Subir a YouTube» del run de `producir.yml` de hoy (el primero, ~01:30
   UTC, o cualquiera de los tres) para ver el error real: lo más probable es
   un token de actualización caducado, un límite de cuota de la API de
   YouTube, o un permiso de verificación — no lo puedo distinguir sin el
   log. Si se quiere publicar hoy, hace falta relanzar `producir.yml` a mano
   (`workflow_dispatch`, `guion: MDS-007.es`) una vez resuelta la causa: el
   cron automático ya no lo va a reintentar (agotadas las tres ventanas de
   hoy, y mañana busca la emisión del 02/09).
2. **Un defecto visual nuevo, sin arreglar aún:** en el mismo `MDS-007`, la
   escena 4 (lista de tres puntos) tapa parte del tercer punto con el
   personaje. No lo he tocado — necesita verse con las tipografías reales
   del canal, que este contenedor no tiene, para no arreglarlo a ciegas.
   Detalle completo y las dos vías de arreglo posibles arriba, en el Paso 3.
3. `cola.py` ya sube en modo `automatico` por defecto si una emisión de la
   parrilla se queda sin `"modo"` — no vuelve a pasar lo de MDH-004 por esa
   vía.
4. Nada nuevo en la revisión editorial: las notas abiertas de `MDS-009` y
   `MDH-005` siguen esperando al jueves, tal como deben.
5. Bibliografía: sin cambios hoy.

## Paquete

`mdh-revision-2026-09-01.tar.gz`, con exactamente estos ficheros:

- `03_produccion/pipeline/cola.py` (editado)
- `05_calendario/bitacora/2026-09-01-revision.md` (nuevo, este fichero)
- `05_calendario/ESTADO.md` (reescrito — `INCIDENCIA`)

No lleva `registro_publicaciones.json` ni `05_calendario/qa/`. No lleva
`parrilla.json`, `CALENDARIO.md`, `demanda.json` ni ningún guion.
