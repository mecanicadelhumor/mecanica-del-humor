# Revisión diaria — 3 de septiembre de 2026, 11:30 (jueves)

Sin acceso al ordenador de Silvestre en esta sesión (tarea programada en la
nube, sin puente al dispositivo — confirmado al arrancar: los tres
`mcp__remote-devices__*` no responden). Trabajo sobre un clon del
repositorio y entrego un `.tar.gz` al final, con los ficheros listados por
nombre.

## Comprobación de entregas pendientes (primera acción)

`git log --oneline -8` sobre `origin/main`:

```
273aeb0 Registro y expediente de calidad: MDS-009
6799cd3 revisión 2/9 segunda parte
6159c01 revisión diaria 2 de septiembre
5d75ffa Registro y expediente de calidad: MDS-008
97bfde5 Registro y expediente de calidad: MDS-007
6d2fa7a revisión del día 1/09/26
7a624df Registro y expediente de calidad: MDS-007
bf7b9a0 Registro y expediente de calidad: MDS-007
```

Hoy es **jueves**: la planificación de la noche corre a las 22:00 de hoy y
no llega a `origin/main` hasta que Silvestre la aplique mañana por la
mañana. La comprobación de la nota de `PROPIEDAD_DE_FICHEROS.md` (¿está el
paquete del jueves aplicado?) toca **mañana viernes**, no hoy. **No hay
entrega pendiente que bloquee `05_calendario/` hoy** — se procede con
normalidad.

## Paso 1 — revisión editorial

Guiones españoles pendientes de producir, cruzando `parrilla.json` con
`registro_publicaciones.json`: `MDS-009` (hoy, 03/09), `MDS-010` (mañana,
04/09) y `MDH-005` (sábado 05/09). `MDH-006/007/008` siguen en backlog sin
adaptar al formato nuevo, fuera de `parrilla.json` — no se revisan hoy,
como todos los días desde el 31/08.

Revalidados los tres con `validar_guion.py`: sin errores en ninguno.

- **MDS-009** — publica hoy. La corrección de ayer (excepción de 48 h,
  cian retirado del `pie` de la escena 3) sigue en el fichero — confirmado
  con `git diff` contra el commit de ayer: **cero cambios** en el guion
  desde la revisión de ayer, la única diferencia en el árbol es el propio
  registro de producción (`registro_publicaciones.json`, `qa/`, que no
  tocan esta tarea). Nota en `revisiones/MDS-009.md` sigue correctamente
  cerrada. No hay nada editorial nuevo que anotar en el guion — pero sí un
  hallazgo de **render** al mirar el vídeo ya producido, ver Paso 3: no es
  un defecto de guion, así que no se toca `MDS-009.es.json` de nuevo ni se
  abre una segunda nota en `revisiones/`.
- **MDS-010** — releído entero (no solo el resumen de ayer). Serie
  «Desmonta el chiste», 6 escenas, 50,7 s. Tres primeros segundos: la
  escena 1 es el chiste completo, sin rótulo ni logo. `pausa_despues_s` de
  la escena 1 en 1,4 (dentro de 1,2–1,5). Remate propio en el cierre
  («Y falla con quien no llega a la segunda fase»), con `fuente` A07.
  Personaje en 2, 4 y 6 — nunca en la 1 ni en la 3 (que se queda sola a
  propósito, según `notas_humor`) — y siempre después de que el chiste ya
  se ha contado. Un ámbar por escena, cayendo en la palabra que sostiene
  el chiste o el giro («acompaño», «morir», «venganza», «tuya»,
  «segunda»). Ningún cian. Audio autosuficiente: la comparación de la
  escena 3 dice en voz alta tanto «lo que parece» como «lo que es», y el
  diagrama de la escena 5 nombra las dos fases con narración, no solo en
  pantalla. Cifra sin cifra numérica inventada (A05, A07 con fuente). C17:
  A05 y A07 no aparecen en ningún guion ya producido — sin aviso, y
  `validar_guion.py` lo confirma. **Sin hallazgos.**
- **MDH-005** — fichero sin cambios desde el 27/08 (confirmado por
  `git log`). Las tres notas de `revisiones/MDH-005.md` (cifra «4 estilos»
  que no se dice en su escena, cian decorativo en tres escenas `dato`, y
  C17 con B01/B02 repitiendo de `MDS-004` como fuente central hace 9 días)
  siguen abiertas y correctas tal cual están. Publica el sábado 05/09: a
  las 11:30 de hoy faltan ~53,5 h, sigue fuera de la ventana de 48 h — nota
  para el jueves que viene (esta misma planificación, cuando aplique el
  paquete de esta noche), no edición directa.

**C17 — cruce de `fuente` contra el corpus producido**, hecho a mano
además del aviso ya automático de `validar_guion.py` (confirmado arriba,
sin novedades): mismo resultado que ayer. D05 (MDS-009) no es repetición
real (una sola aparición previa, de pasada, en `MDH-004`); A05/A07
(MDS-010) son estreno; B01/B02 (MDH-005) siguen siendo la repetición real
ya anotada.

`01_bibliografia/BIBLIOGRAFIA_CURADA.md`: sin cambios desde el 28/08 (visto
en `git log`), no he intentado verificar fichas nuevas hoy.

## Paso 2 — ¿salió la producción?

Sin incidencia. `MDS-009.es` (hoy, jueves) se subió en el primer intento
del cron (`subido_utc: 2026-09-03T01:35:15Z`), `estado: private`,
`publicar_en: 2026-09-03T17:00:00Z` (19:00 Madrid) — correcto para modo
`automatico`, YouTube lo hará público solo dentro de unas horas. `video_id:
JODqu6yEPI8`. Nada que investigar hoy.

## Paso 3 — revisión del vídeo (esta semana, todos los días: motor C15)

### Hallazgo nº 1, con número: escena 3 de MDS-009, la cifra «Más generosos» se corta contra el borde del lienzo

`ficha.json` de `MDS-009.es`: técnico todo en regla — LUFS −14,45 (±1),
pico −1,11 dBTP (≤−1,0), sin fragmento antes de la narración, subtítulos
no quemados (correcto), 97 líneas `.ass` (canario sano).

**Visual, fotograma `f3_22.7s.jpg` (escena 3, tipo `dato`, `cifra: "Más
generosos"`):** el titular «Más generosos» se pinta a 300 px (CSS de
formato vertical, `escena.html` línea 164: `html[data-fmt="v"]
.cifra{font-size:300px}`) y la palabra «generosos» no cabe en el ancho del
lienzo (1080 px). Recorté y amplié la zona del texto sobre el fotograma
real (no un renderizado mío, el `.jpg` que ya subió el propio workflow) y
el corte queda claro: se ve «Más» completo y luego «gener» seguido de una
letra partida justo en el borde derecho del fotograma — que es el borde
real del vídeo, no un recorte artificial de mi captura (el `.jpg` mide
640×1138, la miniatura completa de QA, y el corte cae en su columna final).
El resto de la palabra («-osos») no existe en ningún fotograma: no es un
efecto transitorio del acercamiento de C15.3 (que solo suma un 2,2 %
acumulado a lo largo de la escena) — la palabra ya es más ancha que el
lienzo desde que aparece completa, el zoom solo lo empeora un poco hacia
el final.

**Causa, con el código delante:** a diferencia de `.enunciado` — que sí
encoge con `txt-xs`/`txt-s` según el número de palabras de la escena
(líneas 557-561, el mismo mecanismo que ya se documentó el 01/09 para el
defecto de `ul.lista`) — **`.cifra` no tiene ningún ajuste por longitud**:
es un tamaño fijo (280 px en horizontal, 300 px en vertical) sea cual sea
el texto de `cifra`. Mientras esa cifra fuera un número corto («24», «8»,
«1.200») nunca se notó. Pero el guion ya usa `cifra` para frases
cualitativas más largas cuando no hay una cifra que enseñar sin
inventarla (`REGLAS.md`, regla 2 — es literalmente lo que dice
`notas_humor` de MDS-009: «la escena dato no lleva ninguna cifra a
propósito... poner un número aquí sería inventarlo»), y **«Más generosos»
es la primera vez que ese patrón se usa en un guion `formato: corto`** —
en los guiones largos (`MDH-002`, con «Incluso fallando», «El mismo
circuito») el lienzo es de 1920 px de ancho y nunca ha llegado a
desbordar. Comprobado con `grep` sobre todos los guiones: `cifra` con
texto en vez de número aparece también en `MDH-005` («El peor de los
cuatro»), que todavía no se ha producido — mismo riesgo pendiente ahí si
se mantiene el texto tal cual.

**No he tocado `escena.html`.** El arreglo natural es extender a `.cifra`
la misma lógica de clases `txt-xs`/`txt-s` que ya escala `.enunciado`, pero
elegir el umbral de caracteres y el tamaño de sustitución exige verlo con
la tipografía real de marca (Archivo Black): este contenedor solo tiene
`DejaVu Sans`, con métricas de ancho distintas, así que cualquier número
que fije aquí sería adivinar — la misma razón por la que no toqué el
defecto de `ul.lista` de `MDS-007` el 01/09. Lo dejo anotado con precisión
para quien tenga `vista.yml` o una sesión con las tipografías de marca:

- Fichero: `03_produccion/pipeline/escena.html`, selector `.cifra` (líneas
  75 y 164) y el bloque de clases `txt-xs`/`txt-s` (líneas 557-561, que hoy
  solo tocan `.enunciado`, `h1`, `h2`).
- Caso reproducible con datos reales: `05_calendario/qa/MDS-009.es/f3_22.7s.jpg`,
  escena 3 del guion, `cifra: "Más generosos"`.
- Riesgo ya identificado en otro guion pendiente: `MDH-005`, escena con
  `cifra: "El peor de los cuatro"` — formato largo, lienzo más ancho, no
  urgente pero vale la pena revisarlo el mismo día que se toque `.cifra`.

**¿Es esto una INCIDENCIA?** No la marco como tal. El vídeo ya está subido
y en cola para publicarse hoy a las 19:00 (Madrid) sin que yo pueda
cancelarlo ni recomponerlo desde aquí, y el contenido de la escena sigue
siendo comprensible por audio (la narración dice «lo sintieron más
cercano. Y le dieron más», que transmite el mismo hallazgo con otras
palabras) y por el resto del vídeo. Es un defecto real de legibilidad en
una escena de un Short de cinco, no algo que rompa el vídeo entero — mismo
criterio que se aplicó el 01/09 con la lista tapada de `MDS-007`, que
tampoco se marcó como incidencia.

### Resto de las preguntas de la prioridad 1

- **Palabra resaltada y acento de la frase:** en las otras cuatro escenas
  el ámbar cae bien — «Amigos» (escena 1, el chiste), «mesa» (escena 2, el
  detalle concreto del experimento), «atajo» (escena 4, el argumento),
  «amistad» (escena 5, el cierre). Solo la escena 3 tiene el problema de
  arriba, y es de ancho, no de qué palabra se resalta (el propio `.cifra`
  no usa asteriscos: todo el titular sale en ámbar por diseño, no hay
  «palabra resaltada» que elegir ahí).
- **Ritmo de revelado, deriva de la retícula, cruce del remate de marca
  con el cierre:** no se pueden juzgar bien con cinco fotogramas estáticos
  — hace falta el vídeo en movimiento. El último fotograma capturado
  (38,56 s) queda a 3,64 s del final (42,2 s), así que el remate de marca
  (el último 1,25 s) vuelve a quedar fuera de lo que hay para revisar,
  igual que todos los días desde el 31/08.
- **Capturas y minutos de render:** sigue sin poder consultarse — probado
  otra vez hoy contra la API de GitHub, sigue devolviendo 403 desde este
  contenedor. Mismo motivo que todos los días anteriores.

## Paso 4 — encargo de código

Antes de elegir, comprobado el estado de la lista completa:

1. **`metricas.py`** — resuelto en sesión anterior, sigue en pie (no
   tocado hoy).
2. **`cola.py`, el defecto de `modo`** — resuelto el 01/09, sigue en pie.
3. **`validar_guion.py`, aviso C17** — resuelto el 02/09, sigue en pie
   (usado hoy mismo en el Paso 1).
4. **La música (C18) — parcial hoy.**

**Lo que pedía el encargo tiene dos partes independientes: ampliar el
número de pistas (necesita descargar audio nuevo) y blindar la rotación
contra duplicados (no necesita red).** Antes de tocar nada comprobé si
este contenedor puede llegar a una fuente de música real:

```
curl -sI https://incompetech.com/...   -> 403 Forbidden (del proxy del entorno, no del servidor)
curl -sI https://freepd.com            -> 403 Forbidden (mismo proxy)
curl -sI https://github.com            -> 200 (dominio permitido)
```

El entorno de esta tarea sale a internet por un proxy con lista blanca de
dominios (`github.com`, los gestores de paquetes, poco más); ni
Incompetech ni FreePD están en ella. **No se puede descargar música nueva
desde esta sesión — lo digo aquí y en `ESTADO.md`, y no lo fuerzo**, tal
como pide el encargo. Ampliar a diez o doce pistas queda pendiente de una
sesión con salida a internet general, o de que Silvestre las añada él
mismo con la atribución literal de Incompetech y yo indexe los hashes.

**La segunda parte sí la he hecho hoy, sin red:** en `pistas()`
(`03_produccion/pipeline/cola.py`) la exclusión de `cama.mp3` (la copia
byte a byte) era manual y funcionaba solo porque nadie lo había vuelto a
listar en `creditos.json`. Añadido `_sha256_de()` y un filtro que calcula
el hash real de cada fichero en disco y descarta cualquiera cuyo contenido
ya haya aparecido antes en la lista (se queda el primero por orden
alfabético) — así, el día que se añadan pistas nuevas, un duplicado por
error (una pista subida dos veces con nombres distintos, o con la clave de
`creditos.json` mal copiada) se filtra solo, sin que nadie tenga que
acordarse de excluirlo a mano como pasó con `cama.mp3`. No cambia el
comportamiento de hoy: comprobado que con las tres pistas actuales la
rotación por `musica_de()` da exactamente lo mismo que antes y lo mismo
que ya está grabado en `registro_publicaciones.json` (MDS-002→cama_02,
MDS-004→cama_01, MDS-006→cama_03, MDS-008→cama_02, MDS-009→cama_03, todas
coinciden). `ast.parse` sin errores. La rotación por número de episodio ya
garantizaba «no repetir hasta que hayan sonado todas» dentro de cada serie
(MDS/MDH), así que esa parte del encargo no necesitaba cambio de lógica,
solo el blindaje contra duplicados que sí se ha añadido.

No he tocado la prioridad 1 (el hallazgo de `.cifra` de arriba, que
necesita tipografías reales) ni el resto de la cola de encargos.

## Ficheros tocados hoy

- `03_produccion/pipeline/cola.py` — encargo 4 (parcial): `pistas()`
  descarta duplicados por sha256 real del fichero.
- `05_calendario/bitacora/2026-09-03-revision.md` — este fichero.
- `05_calendario/ESTADO.md` — reescrito entero, `OK`.

No toqué ningún guion, ni con la excepción de 48 h (el hallazgo de hoy es
de render, no de guion — no hay una corrección de texto que lo arregle).
No toqué `parrilla.json`, `registro_publicaciones.json`,
`05_calendario/qa/`, `CALENDARIO.md`, `demanda.json`, `escena.html`,
`render.py`, `montaje.py`, `voz.py` ni ningún workflow. No he descargado
ni añadido ninguna pista de música nueva (bloqueado por red, ver arriba).

## Para Silvestre

1. **MDS-009 (hoy, 19:00) va en hora.** No hace falta nada.
2. **Defecto visual nuevo, sin arreglar:** en `MDS-009`, la escena 3 (la
   cifra «Más generosos») se corta contra el borde derecho del vídeo — solo
   se lee «Más gener…». Detalle completo y la vía de arreglo en el Paso 3;
   no lo he tocado porque necesita verse con las tipografías reales de
   marca. El mismo riesgo existe en `MDH-005` («El peor de los cuatro»),
   por si se quiere revisar a la vez.
3. **La ampliación de música (C18) sigue bloqueada por red desde esta
   sesión** — Incompetech y FreePD no están en la lista blanca del
   contenedor. Si se quiere avanzar sin esperar a que cambie eso, las dos
   vías son: añadir las pistas a mano (con su atribución literal) y que
   yo las indexe, o encargarlo a una sesión con salida a internet general.
   Mientras tanto, la rotación ya está blindada contra duplicados por
   hash, así que cuando lleguen pistas nuevas no hace falta acordarse de
   excluir a mano ninguna copia repetida.
4. Nada nuevo en la revisión editorial: `MDS-010` sin hallazgos, `MDH-005`
   sigue esperando al paquete de esta noche.
5. Bibliografía: sin cambios hoy.

## Paquete

`mdh-revision-2026-09-03.tar.gz`, con exactamente estos ficheros:

- `03_produccion/pipeline/cola.py` (editado)
- `05_calendario/bitacora/2026-09-03-revision.md` (nuevo, este fichero)
- `05_calendario/ESTADO.md` (reescrito — `OK`)

No lleva `registro_publicaciones.json` ni `05_calendario/qa/`. No lleva
`parrilla.json`, `CALENDARIO.md`, `demanda.json` ni ningún guion.
