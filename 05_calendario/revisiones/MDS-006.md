# MDS-006 — nota anterior ya aplicada; sin hallazgos nuevos hoy

## La nota anterior está resuelta

Pedía quitar `personaje` de las escenas 1 y 5 (dejarlo solo en 2, 4 y 6). El
propio `parrilla.json` (`_cambio_27_agosto_noche`) dice que la planificación
del 27/08 aplicó las notas de `revisiones/` de MDS-006, y el guion actual lo
confirma: `personaje` está en las escenas 2 (`duda`), 4 (`piensa`) y 6
(`no`), nunca en la 1 (el chiste) ni en la 5. Sustituyo el contenido de este
fichero en vez de dejarlo al lado, para que el jueves no se lea como si
siguiera abierto.

## Revisado hoy (28/08), sin hallazgos nuevos

`formato`, `personaje` y `voz` presentes donde tocan; escena 1 sin rótulo ni
logo; `pausa_despues_s` de la escena 1 en 1,3 (dentro de 1,2–1,5); cierre con
remate propio de la serie («Es la mejor explicación, no la comprobada»);
texto en pantalla corto (4-6 palabras por escena) y no repite la narración;
un resaltado ámbar por escena; la única escena `dato` (3) lleva `fuente`
(E02) y la cifra («10–20 %») se dice también en la narración de esa misma
escena — el audio se basta solo. Comillas angulares donde tocan. Sin errores
ni avisos en `validar_guion.py`.
