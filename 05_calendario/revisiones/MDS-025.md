# MDS-025

**De la dirección (reescrito el 23/09/2026). No se edita el guion, ni con la excepción de las
48 horas.** Estado hoy: `private`, `publicar_en: 2026-09-25T17:00:00Z`, `DVKoEJDPCqM`, subido
06:19:48 UTC (correcto, aún no toca publicarlo).

## C50 no se activó — tarjeta de marca en las seis escenas

Mismo caso exacto que `MDS-024` (ver `revisiones/MDS-024.md` para el detalle completo): `ficha.json`
→ `visual: null` pese a tener manifiesto (`05_calendario/visuales/MDS-025.json`); el manifiesto
trae `errores_ia: ["module 'cv2' has no attribute 'CascadeClassifier'"]` y todos los candidatos
de todas las escenas descartados por el mismo motivo. Es el segundo (y hasta ahora último) Short
producido desde que C50 entró en marcha, y también le ha fallado el 100% de las veces.

Causa y arreglo: los mismos que en `MDS-024.md` — `opencv-python-headless` sin versión fijada
en `.github/workflows/visuales.yml` trae ahora OpenCV 5.0, que quitó `cv2.CascadeClassifier`;
arreglado hoy en `03_produccion/pipeline/visual.py` (`caras()` captura el `AttributeError` y se
degrada en vez de descartar el candidato). El arreglo de raíz —fijar versión en el workflow— no
es mío.

Sin más hallazgos: audio correcto (`arranque.fragmento_antes_de_la_narracion: false`), lufs y
pico dentro de rango, dos canales consistentes en las seis escenas, `validar_guion.py` sin
errores.
